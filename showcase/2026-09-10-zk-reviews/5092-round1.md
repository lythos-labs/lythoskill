# ZK review — TASK-20260910110545092 (round 1)

Reviewer: zero-knowledge pass. I did not write this work and had no prior context on it.

## 1. Object + environment

- **Object under review**: `cortex/tasks/03-review/TASK-20260910110545092-cli-layout-follow-ups-unfixed-b-class-findings-from-2026-09-10-inbox-debate.md`
- **Repo**: `/Users/chariots/Downloads/lythoskill-main`
- **Pinned commit**: `6ac2324d60730d444c3eb8d3afdce4a4cc02361c`
- **Tree at start**: clean. `git status --porcelain` produced **no output** before I ran anything.
- **Tree at end**: still clean (re-checked after every run, including after the repo-wide test suite and the cortex BDD runner).

Environment (quoted, not paraphrased):

```
$ bun --version
1.3.11
$ uname -s -r
Darwin 24.6.0
```

Every number below is from this machine under that bun. `/tmp` on this machine is a symlink to `/private/tmp`; I hit that once and re-ran the affected experiment under `/private/tmp` — see §3.13.

---

## 2. Comprehension check (WHAT / WHY / HOW)

**What it was for** (restated from the card alone + `AGENTS.md`): a 2026-09-10 "inbox-debate" put the previous card's claims (`TASK-20260909155425926`, P2 CLI-adapter hardening) through an adversarial challenge. The judge's verdict split the defects into three classes; this card collects only the **B class** — "facts exposed in the debate, proven true, not yet fixed". It is explicitly *not* the C class (unverified proposals). It closes 21 numbered items (B1–B21, some added mid-flight) plus owner rulings on B17/B18.

**Why it existed**: the card's own framing is that nothing was *broken* — `deck link` worked before and after. What it changes is that mechanisms which were silently wrong now speak. Three of the items are the interesting half: B2 (a missed `data-loss` warning on `~/.config/goose/skills`), B20 (a build artifact that could ship documenting a retired flag) and B21 (`probe`'s empty-shell detector could never fire on an ADR).

**What it changed** (verified against the tree, not against the card):
1. `packages/lythoskill-deck/src/cli-layout.ts` — goose `triggerDirs` widened; `verifiedBy` field; `skillsDirsUnder`.
2. `packages/lythoskill-deck/src/layout-policy.ts` — `severity: "info"`, `FanOutOptions`, `collectUnlistedTargets`, `collectWrongLevelTargets`.
3. `packages/lythoskill-deck/src/link.ts` — `acknowledged_unlisted` parsing, three-level rendering, warning block moved before the reconciliation summary.
4. `packages/lythoskill-deck/src/per-run.ts` — `targetsFromDeck` extracted as a pure function.
5. `.husky/pre-commit` — rebuild trigger widened to `src/` of skill-shipping packages + staleness announcement.
6. `.github/workflows/test.yml` — a machine-decidable gate (`build --all && git diff --quiet skills/`).
7. `.github/workflows/{test,release,deploy-pages}.yml` — 8 × `bun-version: latest` → `1.3.11`.
8. `AGENTS.md` §2 rules 10, 11; §9 "Bun version pin".
9. `packages/lythoskill-project-cortex/src/commands/probe.ts` + `probe.test.ts` — two ADR placeholder patterns.
10. `showcase/2026-09-09-p2-cli-layout-zk-retrial/` — a 5-file ZK evidence bundle.
11. `packages/lythoskill-deck/test/scenarios/also-link-to-bdd/` — PHASE 5 + a real verdict.
12. Four ADRs (`...120047122`, `...120047160`, `...113730375`, `...113534807`) and edits to `ADR-20260910112404500`.

**Did I understand it without the author?** Mostly yes — the card is unusually self-contained for its size. Three things I could not have reconstructed without the author:

- **What "B-class" actually denotes.** The card defines it in one parenthetical, but the numbering is not in the card; it points at `playground/2026-09-10-inbox-debate/oracle/0001-judge-verdict.md` §4-B. I read that file to map B1…B21 back to the judge's list. Without it, the item numbers are opaque labels.
- **Why B19 exists at all.** The card's original B19 text is *left in place* and then contradicted ~40 lines later ("本条的立论已被实测推翻"). Reading top-down, the card asserts a problem and then refutes it. The refutation is correct (I re-tested — §3.11), but the layout makes a linear reader believe PHASE 2 is still broken unless they reach line 95.
- **The owner's authority.** Many items are marked "归 owner" and then immediately "✅ 裁决(owner)=" with quoted Chinese wording. Nothing in the card establishes who the owner is or where those rulings are recorded beyond prose.

**Contradictory / self-serving reads**, flagged as such:

- **AC line 317 contradicts AC line 306-313.** The B9 acceptance criterion names the extracted function **`targetsFromDeck`** ("见 Progress Log") while the Requirement B9 body says the extraction is `loadPerRunTargets(deckPath, workdir)`. Both appear in the same document. The tree has `targetsFromDeck`. The card knows and reconciles this in the AC, but a reader who only reads Requirements will look for a function that does not exist.
- **The card quotes a test baseline that does not reproduce** (see §3.1). This is the failure mode the card's own B5 exists to fix, and it does it in its own Acceptance Criteria. That reads as self-undermining rather than self-serving, but it is the same class.
- **"本卡累计:227 → 249 pass"** (AC line 371) — the true end state is 253. See §3.1.

---

## 3. Claim-by-claim verification

### 3.1 Test baselines — **REFUTED**

Card (Acceptance Criteria line 371-372 and Progress Log line 525):

> `bun test packages/lythoskill-deck/` 全程保持 `0 fail` … (本卡累计:`227 → 249 pass`,`0 fail` 未破)
> 本批实测值:deck `249 pass / 1 skip / 0 fail / 657 expect / 250 tests / 16 files`

Command and raw output at the pinned commit:

```
$ bun test packages/lythoskill-deck/    # run twice
 253 pass
 1 skip
 0 fail
 661 expect() calls
Ran 254 tests across 16 files. [1.77s]
```

(Second run identical: 253 / 1 / 0, 254 tests, 2.51s.)

This is not a drift-from-later-commits explanation. The commit that quotes `249` is `04b193f1`, and **the deck test sources are byte-identical between `04b193f1` and HEAD**:

```
$ git diff 04b193f1..HEAD --name-only -- 'packages/lythoskill-deck/**/*.test.ts' | wc -l
       0
$ git diff 04b193f1..HEAD --stat -- packages/lythoskill-deck/
 packages/lythoskill-deck/README.md                 |  5 +++
 packages/lythoskill-deck/src/cli-layout.ts         |  6 ++++   # comment-only, verified
 .../test/scenarios/also-link-to-bdd/decision-log.jsonl  | 13 ++++++++
 .../test/scenarios/also-link-to-bdd/judge-verdict.json  | 37 ++++++++++++++++++++++
 .../test/scenarios/also-link-to-bdd/judge.md            | 18 +++++++++--
```

And the count is a pure function of the sources — the literal number of `it(`/`test(` declarations across the 16 deck test files is **254**:

```
$ grep -cE "it\(|test\(" packages/lythoskill-deck/src/*.test.ts | awk -F: '{s+=$2} END {print s}'
254
```

No deck test reads outside its package (`grep -rn "examples/\|showcase/" packages/lythoskill-deck/src/*.test.ts` → nothing), and `examples/` did not change in the range. So `250 tests` was never achievable from the sources `04b193f1` shipped: the author measured before finishing their own test edits in that commit.

**What survives**: the load-bearing half of the criterion — `0 fail` — holds (253 pass / 0 fail). The quoted magnitude is wrong by 4.

**Environment labelling**: the card *does* include `@ Bun 1.3.11 / macOS` on its baseline lines, which is what B5 demanded. Partial credit is real here.

**Cortex baseline** — card: `138 pass / 0 fail / 3xx expect / 7 files`. Actual:

```
$ bun test packages/lythoskill-project-cortex/
 138 pass
 0 fail
 280 expect() calls
Ran 138 tests across 7 files. [339.00ms]
```

`138`/`0`/`7 files` verified. `3xx expect` is **refuted as a measurement** — it is 280, and "3xx" is a shape, not a number. No cortex `.ts` file changed between `04b193f1` and HEAD, so there is no drift excuse here either.

**Repo-wide** — card: "全仓 `bun --filter='*' run test` **零失败**". **VERIFIED**:

```
$ bun --filter='*' run test
# every package: "Exited with code 0", every package: "0 fail"
# (agent-adapter 51, claude-sdk 3, codex 8, deepseek-harness 13, deepseek-serve 27,
#  cold-pool 175, infra 3, project-cortex 138, skill-arena 165, skill-creator 65,
#  skill-curator 112, skill-deck 254, test-utils 122, hello-world — all code 0)
```

### 3.2 Default deck emits zero warnings / dormancy — **VERIFIED**

Scratch deck `.claude/skills` + `.agents/skills` (the card's stated default shape), run through the real CLI:

```
$ bun packages/lythoskill-deck/src/cli.ts link --deck /tmp/zk-dorm/project/skill-deck.toml --workdir /tmp/zk-dorm/project \
    | grep -iE "warn|info|⚠|ℹ|no layout|not a skills"   # exit 1 = zero matches
=== policy lines: 1 (grep exit 1 = zero matches) ===
```

Zero policy output. A list of standards-shaped targets (`.claude/skills` + `.agents/skills` + `~/.qwen/skills`) also yields `0` matching lines. Dormancy holds.

### 3.3 Mutation test: does the new B2 assertion actually go red on the old value? — **VERIFIED**

Scratch copy (never the repo):

```
$ cp -R packages/lythoskill-deck /tmp/zk-mut/deck
$ perl -pi -e 's/triggerDirs: \[".goose\/skills", "~\/.config\/goose\/skills"\]/triggerDirs: [".goose\/skills"]/' src/cli-layout.ts
$ bun test src/cli-layout.test.ts
(fail) cli-layout data integrity > goose carries the data-loss hazard with triggerDirs (#11600)
error: goose/recursive-unlink-delete must cover exclusive target ~/.config/goose/skills
(fail) cli-layout data integrity > a data-loss hazard covers every EXCLUSIVE fan-out target of its own layout (B2, property)
 16 pass
 2 fail
```

**Exactly the two reds the card claims** ("把值改回旧写法 → 2 条红"). And the invariant test is a genuine class-level check, not a literal-value check:

```
packages/lythoskill-deck/src/cli-layout.test.ts:88-92
  const exclusive = a.fanOutTargets.filter(t => layoutsScanning(t).length === 1)
  ...
  expect(h.triggerDirs, `${a.id}/${h.id} must cover exclusive target ${t}`).toContain(t)
```

Note: the AC (line 310-313) says the fix would change `toEqual(['.goose/skills'])` to `toContain` **"并新增"** an invariant test. The tree has **both** — explicit `toContain` for the two goose dirs *and* the property test. The Progress Log (line 489-492) says the `toContain` was *replaced* by the invariant. Both exist, so neither description is exactly right, but both claims' substance is satisfied.

**Behaviour, end to end** — `~/.config/goose/skills` as an `also_link_to` target now emits:

```
⚠️  [data-loss] Goose: Removing a project-linked skill via Goose recursively deletes the symlink TARGET ...
```

The pre-fix behaviour (no warning at all) is what the card describes; I did not re-run the pre-fix binary, but the mutation test above establishes the code path.

### 3.4 `probe` now detects an unfilled ADR — **VERIFIED**

Scratch project (no repo writes):

```
$ bun .../cortex/src/cli.ts init
$ bun .../cortex/src/cli.ts adr "throwaway shell detection probe ZK"
$ bun .../cortex/src/cli.ts probe
🏛️  ADRs:
  ✅ cortex/adr/01-proposed: 1 consistent
📭 Empty shells (template not filled):
     ⚠️  ADR-20260910153653505: cortex/adr/01-proposed/ADR-20260910153653505-...
⚠️  Found 1 empty shell(s): ADR-20260910153653505
```

The card's quoted output shape (`Found 1 empty shell(s)` instead of `0`) reproduces exactly. The patterns are at `probe.ts:38-45`; `adrFiles` is passed at `:579`.

**EPIC side of the same criterion** (AC: "并核对 EPIC 模板是否同病") — **VERIFIED**: an unfilled epic created by the CLI *is* flagged:

```
📭 Empty shells (template not filled):
     ⚠️  EPIC-20260910154057521: cortex/epics/01-active/EPIC-20260910154057521-...
     ⚠️  ADR-20260910153653505: ...
⚠️  Found 2 empty shell(s)
```

**Rejected candidate** — the card claims `/^-\s*$/` would have fired on **2 hand-filled ADRs**:

```
$ find cortex/adr -name '*.md' | wc -l
113
$ grep -rlE '^-\s*$' cortex/adr/ | wc -l
2      # both in 02-accepted/, i.e. both accepted (filled) ADRs
```

**VERIFIED in substance.** The card says "112 篇" — at the pinned commit it is 113. `ADR-20260910152957509` was added at `c4b8f918`, after the line was written, so 112 was probably right at authoring time; the count is now stale. LOW.

### 3.5 CI-repair claim: the `CLI_TABLE` drift tripwire — **VERIFIED**

```
$ bun test scripts/check-site-commands.test.ts
 17 pass
 0 fail
 36 expect() calls

$ grep -n "per-run" scripts/check-site-commands.ts
95:    // `per-run` added here 2026-09-10: the drift tripwire caught it missing (deck's
96:    // per-run shipped in the P2 batch without updating this table) — same miss class
98:    commands: ['link', 'add', 'refresh', 'update', 'validate', 'remove', 'to-symlink', 'to-snapshot', 'per-run', 'migrate-schema'],

$ git show 04b193f1 --stat -- scripts/check-site-commands.ts
 scripts/check-site-commands.ts | 5 +-
```

The claim "守卫按设计工作,是表错了" is **verifiable and verified**: the guard's table omitted a subcommand that the CLI actually ships; the fix is in the table with a provenance comment, matching the same miss class as the `update` omission.

**"本地复跑 CI 全部步骤零失败"** — I re-ran the workflow steps individually:

| CI step (`test.yml`) | Card claim | My run |
|---|---|---|
| `bun --filter='*' run test` | zero failures | all packages exit 0 |
| `bun packages/lythoskill-project-cortex/test/runner.ts` | 13/13 | `13/13 passed` |
| `bun scripts/validate-example-decks.ts` | 29/29 | `Decks: 29  Pass: 29  Fail: 0` |
| `bun scripts/check-site-deck-snippets.ts` | 61 | `61 validated locators` |
| `bun packages/lythoskill-creator/src/cli.ts align` | 75 passed | `75 passed, 0 failed` |

All five **VERIFIED** against the pinned sha.

**The new CI gate** — card: "CI 新增机器可判定门(`build --all && git diff --quiet skills/`)":
`.github/workflows/test.yml:47-55` contains exactly that, with `::error::` and `exit 1`. **VERIFIED.**

**The pre-commit widening (B20)** — `.husky/pre-commit:117-145`:
- `SRC_PKGS` collects `packages/<p>/src/` changes, filters to packages with a `skill/` dir.
- `if [ -n "$STAGED" ] || [ -n "$SRC_PKGS" ]; then ... bun ... build --all`
- followed by `if ! git diff --quiet -- skills/` → prints the count and the names of stale files, and marks them `STALE relative to the source in this commit (B20, …)`.

**VERIFIED**, and the "dormancy-style self-check" wording matches the code.

### 3.6 BDD verdict artifacts — **VERIFIED**, with one residual

**`reviewed_commit` is a real commit** — `judge-verdict.json` carries `"reviewed_commit": "30cda3b62c5fbb11227f01d312cbf3f7e530441a"`:

```
$ git log -1 --format="%H %s" 30cda3b62c5fbb11227f01d312cbf3f7e530441a
30cda3b62c5fbb11227f01d312cbf3f7e530441a test(deck-bdd): judge criteria for the ownership boundary in also-link-to-bdd
```

Real, and it is the commit that landed the PHASE 5 criteria the verdict applies — the pin is coherent, not decorative.

**Criteria line up** — `judge.md` declares 23 criteria (`initial_*` ×6, `toml_*` ×2, `*_a_removed` ×3, `*_b_preserved` ×3, `*_a_restored` ×3, `cold_pool_intact`, `decision_log`, `foreign_dir_preserved`, `foreign_symlink_preserved`, `foreign_reported`, `foreign_target_intact`). `judge-verdict.json` reports exactly those 23 keys, one-to-one, no extras, no omissions. **VERIFIED.** The verdict's `summary` also correctly explains why criteria 1-17 (states that no longer exist in the end-state fixture) were re-derived by replay.

**Four-file shape** — `also-link-to-bdd/` now has `decision-log.jsonl`, `judge-verdict.json`, `judge.md`, `reproduce.sh`, matching `deck-remove-bdd/` and `to-symlink-snapshot-bdd/` exactly. The card's claim that only the latter two had verdict artifacts *before* this card is consistent with the tree (this is the third). **VERIFIED.**

**`decision-log.jsonl`** — 13 non-empty lines; every `"ts"` is the single value `"2026-09-10T07:19:29Z"`, i.e. one clock source. **VERIFIED** against the card's "13 行,单一时钟源".

**Residual**: `judge-verdict.json`'s `summary` caveat (2) still states the rogue symlink "was silently deleted on the next link with no advisory". The card says it corrected that. The correction lives in `ADR-20260910112404500` § "已知边界" — **and the ADR is right** (see §3.13). But the verdict JSON, a landed artifact a future reader will open first, still asserts the wrong word with nothing in the file pointing to the correction. LOW.

### 3.7 ZK evidence bundle — **VERIFIED**, including the ADV-7 claim

**The bundle runs and reports what it says**:

```
$ bash showcase/2026-09-09-p2-cli-layout-zk-retrial/reproduce.sh
 9 pass
 1 fail
 18 expect() calls
Ran 10 tests across 1 file. [262.00ms]
exit=1 (expected non-zero: ADV-7 is known-red, see README.md)
```

"9 pass / 1 fail" — exact match. Note the script reads the repo read-only and writes only to `mktemp -d`; the tree stayed clean.

**ADV-7 is red against the commit it pins** — this is the hard claim, and it holds. I extracted `adapter-policy.ts` from `c8ebcc76` and ran ADV-7's assertion against it unmodified:

```
$ git show c8ebcc76:packages/lythoskill-deck/src/adapter-policy.ts > /tmp/zk-old/adapter-policy.ts
$ bun test adv7-retired.test.ts
dup count = 11 ["Roo Code scans 3 fan-out dirs (…)", "Gemini CLI scans 3 …", …]
error: expect(received).toHaveLength(expected)
Expected length: 1
Received length: 11
(fail) ADV-7 at c8ebcc76
```

**VERIFIED exactly as claimed** (11 ≠ 1; the README says "11", the earlier reading said 1). `collectDuplicateScans` emits one line per scanning CLI (11 CLIs scan `.agents/skills`), so the case's expectation never matched the code it was written for. This is the single strongest piece of evidence in the card — it is a *finding*, not a completion claim, and it is reproducible from the tree.

**Verbatim landing** — the card says the three files were copied "逐字" from `/tmp/arena-p2-adapter-retrial/`, which it says is the only copy. That directory **still exists on this machine**, so the claim is checkable:

```
$ for f in adversarial.test.ts decision-log.jsonl self-report.md; do diff -q /tmp/arena-p2-adapter-retrial/$f showcase/2026-09-09-p2-cli-layout-zk-retrial/$f; done
--- adversarial.test.ts   IDENTICAL
--- decision-log.jsonl    IDENTICAL
--- self-report.md        IDENTICAL
```

**VERIFIED** — byte-identical. (The card's time-sensitivity note was real; it happened not to have expired.)

**B16's timestamp contradiction** — every element checks out:

| Claim | Observation |
|---|---|
| decision-log file mtime = 22:52 | `Sep 9 22:52` |
| internal timestamps = 23:20–23:45 | 10 distinct values, `23:20Z` … `23:45Z` |
| fold-in commit timestamp = 22:56:31 | `2686e0d4 … 2026-09-09 22:56:31 +0800` |
| fold-in diff = 6 files / +56 / −8 | `6 files changed, 56 insertions(+), 8 deletions(-)` |

**VERIFIED in full.** "`ts` is provenance, not a clock" is landed in the contract at `packages/lythoskill-arena/skill/references/reproduce-sh-bdd-contract.md:51` and in the built copy `skills/lythoskill-arena/references/reproduce-sh-bdd-contract.md`. **VERIFIED.**

### 3.8 The two ADR amendments — **VERIFIED**

**(a) `ADR-20260910113534807 §5` — "已 accept 的 ADR:其 Follow-up 由该 ADR 自身承载(不另开卡)"**

- Rule text at §5, line 210, of `cortex/adr/02-accepted/ADR-20260910113534807-…md`.
- Three boundary rows present and readable (line 231-233): follow-up spawns a new obligation / needs its own plan / blocked on an owner ruling → **still open a card**. This is precisely the "界线三条" the AC demands, and it is what stops the rule from degenerating into "anything can skip a card".
- Status History line 11 records the amendment, including the owner's quoted wording.
- Writing-guide section present at `skills/lythoskill-project-cortex/references/writing-guide.md:85-109` (and the source under `packages/…/skill/references/`), with the same boundary table.
- `template.ts:198-199` carries the `Follow-up:` line comment.

Card says the rule is "落在 `ARD-…34807 §5` + writing-guide 一节 + `template.ts` 的 `Follow-up:` 行注释". **All three verified where the card says they are.**

**(b) `ADR-20260910112404500` § 已知边界** — present at line 118, `## 已知边界:判据是"形状",不是"出身"`. It states the false-positive surface, corrects the judge's "silently" (line 130-132), and registers the HATEOAS-density gap as a **follow-up rather than a defended asymmetry** (line 133-137), quoting the owner. **VERIFIED**, and §3.13 shows the ADR's factual content is correct while the judge's original wording was not.

### 3.9 B17 — **VERIFIED**

```
$ grep -rn "bun-version" .github/workflows/ | wc -l
       8
$ grep -rn "bun-version: latest" .github/workflows/ | wc -l
       0
```

All eight are `1.3.11`: `test.yml` ×5 (`:22/:74/:107/:133/:159`), `release.yml` ×2 (`:32/:123`), `deploy-pages.yml` ×1 (`:32`). The card's count of 8 (correcting the judge's 7 by finding `deploy-pages.yml`) is **right**. `AGENTS.md §9` → `#### Bun version pin — CI and local must name the same interpreter` at line 516, with the four-step upgrade window at line 530. The card's claim that `deploy-pages.yml` uses `setup-bun@v1` while the others use `@v2` is recorded as explicitly out of scope — consistent with "pin the interpreter, not the action".

### 3.10 B18 — **VERIFIED**, all five acceptance points

Run against a scratch deck with `also_link_to = [".some-new-cli/skills", "~/.config/goose/skills"]`:

```
--- RUN 1 (no exemption) ---
⚠️  [data-loss] Goose: Removing a project-linked skill via Goose recursively deletes the symlink TARGET …
ℹ️  [info] /tmp/zk-b18/p/.some-new-cli/skills: no layout data — hazards unknown

--- RUN 2 (acknowledged_unlisted = [".some-new-cli/skills"]) ---
⚠️  [data-loss] Goose: Removing a project-linked skill …      # info line gone, data-loss SURVIVES
```

- ① `layoutsScanning(t) === []` → exactly 1 info line: **verified**.
- ② declared → zero output for that target: **verified**.
- ③ exemption does **not** suppress data-loss: **verified** (this is the反例 the AC demands).
- ④ default deck + docs-tier targets → zero output: **verified** (§3.2).
- ⑤ orientation written into deck README: **verified** at `packages/lythoskill-deck/README.md:62-63, 81-105` (*Unlisted targets*, *Wrong-level targets*).

**追加边界 (wrong-level)** — `also_link_to = ["~/.claude", ".agents"]` **with both declared in `acknowledged_unlisted`**:

```
⚠️  [warning] /Users/chariots/.claude: not a skills dir — it contains .claude/skills / ~/.claude/skills. Fan-out creates skill entries directly inside the dir named here; name the skills dir instead.
   ref: packages/lythoskill-deck/README.md#safety-guards — …
⚠️  [warning] /tmp/zk-b18/p/.agents: not a skills dir — it contains .agents/skills. …
📁 working_set: .claude/skills
```

- ① `~/.claude`, `.agents` both hit, including the `~`-form: **verified**.
- ③ exemption does not apply: **verified** (both fired despite being declared).
- ④ one verdict per target (no info line alongside): **verified** (no `no layout data` line for either).
- ⑤ wording names the directory to use instead, and lists both spellings for absolute targets: **verified** (`.claude/skills / ~/.claude/skills`).
- ⑦ dormancy intact: **verified** (§3.2).
- ⑥ `~` / `.` not judged — I did not test `also_link_to = ["~"]`; `link.ts:510` is claimed to reject it on the `working_set` side. Not checked, see §5.

**Warning block moved before the reconciliation summary** — **verified**: the two `⚠️ [warning]` lines appear *above* `📁 working_set:` in stdout.

### 3.11 B19 — **VERIFIED**, including the retraction

- The scenario's `reproduce.sh` now has PHASE 5 (`:77`) plus an explicit note at `:88-91` recording that PHASE 2 assertions 9-11 hold unchanged under the ownership boundary.
- Its verdict reports 23/23 PASS, and the summary explains the replay method. The four `foreign_*` criteria exist in `judge.md`.
- The card's *self-refutation* of the original B19 premise is corroborated by the code path: `link.ts:625` reclaims only `verdict.via === "symlink-into-coldpool"`, and the scenario's entries are symlinks into the deck's own cold pool — so they are deletable, and PHASE 2's "must not exist" assertions do hold.

### 3.12 B1 / B8 / B9 / B10 / B11 / B12 / B13 / B3 — **all VERIFIED**

- **B1**: `cli-layout.ts:16-18` records the 2026-09-10 withdrawal of the opencode "Windows non-discovery" rumour and states the bar for re-entry ("在拿到一手 ref 之前不得重新入库"); the surviving opencode `source` (`https://opencode.ai/docs/skills/`) and hazard `ref`s (issues 46327 / 45961) are real URLs. **VERIFIED.**
- **B8**: `buildPrunePlan` / `executePrunePlan` exist in `packages/lythoskill-cold-pool/src/prune-plan.ts:86,117`, exported from its `index.ts:46`, used by its `cli.ts`. The deck README now reads "**Prune** (`@lythos/cold-pool`): … so the functions live there, not here" at `:193`. The card's "归位而不是删行" is accurate. **VERIFIED.**
- **B9**: `export function targetsFromDeck(deck: any, projectDir: string): string[]` at `per-run.ts:105`; the entry does `const deck = parseToml(readFileSync(...))` then `targetsFromDeck(deck, PROJECT_DIR)` — one read line at the entry, per the card. `renderPerRun(cliId: string, targets: string[])` at `:39` is untouched by `04b193f1`'s diff (the diff adds `targetsFromDeck` above the CLI entry and replaces the three inline lines). Four new tests in `per-run.test.ts:75-95`, all calling the pure function with literal objects. **VERIFIED.** (Caveat: the AC names the function `targetsFromDeck` while Requirement B9 names `loadPerRunTargets` — see §2.)
- **B10**: `safe-remove.test.ts:133-143` builds `snap/nested/deeper`, writes `SKILL.md` into the deepest level, and asserts both `join(deep, 'SKILL.md')` and `join(snap, 'nested')` are gone. Nested two levels, as claimed. **VERIFIED.**
- **B11**: `AGENTS.md:137` rule 11 ("`node:*` is Bun's compat layer — prove behavior with a test, never with Node docs"), naming the live instance. **VERIFIED.**
- **B12**: `safe-remove.ts:23-28` cites the two named `safe-remove.test.ts` cases ("cold-pool target content survives byte-identical" / "removes broken symlinks (existsSync-based code leaked these)") and explicitly disclaims Node documentation semantics. No Node-doc citation remains. **VERIFIED.** (The card says `:9-10` was the original location; the file has since grown, the content claim holds.)
- **B13**: `PER_ROLE_SCOPING_MAX = 200` at `cli-layout.ts:105`, non-empty + length check at `:535-537`. **VERIFIED.** (I did not independently confirm the "current longest is 140" provenance for the 200 — see §5.)
- **B3**: `VERIFIED_BY = ["survey", "restored"]` closed set at `:97`; exactly one row carries `verifiedBy: "restored"` (line 420, qwen-code) — `grep -n "verifiedBy:" cli-layout.ts` returns one hit; the `restored`-requires-note check at `:531`; 16 rows unchanged (`SURVEY_CLAIMED_COUNT` test at `cli-layout.test.ts:25`). **VERIFIED.** The qwen note does record the restoration ("Row restored by zero-context verification agent (original survey report unpersisted)").
- **B4/B5/B6/B7**: the original card's Notes carry a per-line correction table. I opened it: `:64` Goose-#11600 wording, `:71` bare test count with environment, the two-layer "独立" correction, the 8.5 coverage-scope row (`8.5 @ c8ebcc76`, fold-in `2686e0d4`, 6 files / +56 / −8), and the ADV-7 row. `daily/2026-09-09.md:20` is edited in place with the Goose wording correction, and `:29` carries the two-layer "独立" correction. **VERIFIED** — the AC's specific test ("注记里出现 commit sha,而不是只出现数字 8.5") is met at line 114.

### 3.13 The rogue-symlink reclamation: the card corrects the judge, and the card is right — **VERIFIED**

The judge's verdict claims a hand-made symlink pointing into the deck's own cold pool is "silently deleted on the next link with no advisory". The card says that wording is wrong: stdout carries a `🗑️ Removed:` line. I reproduced it.

*First attempt failed and is worth recording:* with the deck not declaring `cold_pool`, the default is `~/.agents/skill-repos` (`link.ts:359`), so my "cold pool" wasn't the deck's and the entry was correctly reported foreign. Under `/tmp` alone, macOS's `/tmp → /private/tmp` symlink also made a first variant non-comparable. With `cold_pool = "./cold-pool"` declared, under `/private/tmp`:

```
$ ln -s /private/tmp/zk-rogue3/p/cold-pool/localhost/me/rogue-handmade .agents/skills/rogue-handmade
$ bun …/deck/src/cli.ts link --deck … --workdir …
  🗑️  Removed: rogue-handmade
$ ls .agents/skills/
skill-a
```

**The deletion is real; the silence is not.** The card's correction ("差的不是'有没有出声',是'出声够不够'") is exactly right: the reclaim path emits one action line and no what/why/fix three-part advisory, while the foreign-entry path emits all three. `link.ts:625-634` vs `:609-613` shows why. **VERIFIED**, and this is a case where the review target corrected its own evidence in the right direction.

### 3.14 Card self-consistency — **VERIFIED**

`cortex probe` at the pinned commit reports the card as consistent:

```
✅ cortex/tasks/03-review/TASK-20260910110545092-cli-layout-follow-ups-unfixed-b-class-findings-from-2026-09-10-inbox-debate.md
```

No empty-shell flag for it. (Probe is not wholly green — 9 stale backlog cards, 1 drifted epic, 2 unrelated status issues, 1 wiki drift — but none of those are this card, and the AC only claims "本卡非空壳".)

The judge's finding #3 was registered as a card, as the Progress Log claims: `cortex/tasks/01-backlog/TASK-20260910152029904-deck-add-and-remove-destroy-every-comment-in-skill-deck-toml-via-an-iarna-toml-parse-stringify-round-trip.md`. **VERIFIED.**

---

## 4. Gaps

1. **HIGH — the quoted test baseline does not reproduce, in the card's own Acceptance Criteria.** `249 pass / 250 tests / 657 expect` vs the actual `253 / 254 / 661`, and the deck test sources are provably identical between the commit that quotes the number (`04b193f1`) and the pinned commit. A reader who treats the AC as the acceptance test would believe 4 tests are missing. This is the B5 failure mode the card exists to fix, reproduced inside the card. *Fix:* re-measure and re-quote with the sha, as the card's own Notes demand ("引用任何测试计数时必须带'哪个 commit + 什么环境'").

2. **LOW — cortex expect count quoted as `3xx`.** Actual 280. Shape-instead-of-number in a document whose thesis is "claims, not scores".

3. **LOW — B9's function is named two different things in the same card.** Requirements say `loadPerRunTargets(deckPath, workdir)`; the AC says `targetsFromDeck`; the tree has `targetsFromDeck`. A reader following Requirements will grep for a name that does not exist.

4. **LOW — "20 个 showcase 条目" is 19.** `git ls-tree --name-only 04b193f1 showcase/ | wc -l` = 19, and 19 at HEAD. Off by one at both points, so not a drift artefact.

5. **LOW — `judge-verdict.json` still asserts the refuted wording.** Its summary says the rogue symlink was "silently deleted … with no advisory"; the correction lives only in `ADR-20260910112404500`. Leaving evidence frozen is defensible, but the frozen file is the one a reader opens first and nothing in it points at the correction. A one-line pointer inside the verdict (or in `TESTING.md`) would close it without breaking evidence hygiene.

6. **LOW — the card's own "Added" list places the card in `02-in-progress/`.** It is in `03-review/`. Stale path in a list a reader may follow.

7. **LOW — the B21 provenance count is stale.** "本仓 112 篇 ADR" is 113 at the pinned commit. The substantive claim (2 hits) still verifies.

8. **LOW — B19's original text sits un-retracted above its own refutation.** The card asserts the PHASE 2 conflict, then ~40 lines later says the premise was overturned. Both are on the page; the second is correct. A reader who stops early gets the wrong answer. A one-line pointer at the top of the B19 entry would fix it without deleting the history the card deliberately keeps.

I found **no** gap in the areas the card is actually load-bearing on: the behaviours (B2, B18, dormancy), the mechanisms (B20, B21, CI gate, pre-commit), the ADR amendments, and the ZK bundle's ADV-7 finding all reproduce exactly as described.

---

## 5. What I could not check, and why

1. **The player and judge runs themselves.** These are agent invocations. I verified the artifacts they produced, that `reviewed_commit` names a real commit, that the criteria line up one-to-one, that the decision log has one clock source, and that the fixture is internally consistent with the verdict's detail strings. I cannot re-run "an independent agent wrote this without seeing `judge.md`" — the independence claim is unverifiable from the tree by construction.
2. **The PHASE 1-5 assertions of `also-link-to-bdd` end to end.** `reproduce.sh` is an IoC handoff script that prints instructions for an agent rather than executing assertions itself, so there is no non-agent way to replay it. I verified the script's structure, the criteria file, and the verdict's account of the run; I did not re-derive the 23 results.
3. **The claim that ADR-20260910113534807's Follow-up was carried by the ADR and not by a card.** I checked that the rule text, the three boundary rows and the three landing sites exist and that no backlog card mentions the four prompt-injection sites. I did not audit the ADR's `## Impact → Follow-up` list item-by-item for completion.
4. **The provenance of the "200" in `PER_ROLE_SCOPING_MAX`.** The card says 200 = longest current row (140) + 60 margin. I confirmed the constant and the check, not the 140 measurement.
5. **`~` / `.` as fan-out targets (B18 acceptance point ⑥).** The card calls these a known boundary written into the README rather than handled. I read the claim at `link.ts:510` for the `working_set` side but did not exercise `also_link_to = ["~"]`.
6. **Whether the pre-fix tree actually emitted zero warnings for `~/.config/goose/skills`.** The card claims it did. I proved the *current* code warns and that the mutation makes the test red, which is the functional half; I did not check out the pre-fix code and run it.
7. **The `deck BDD runner` step.** `bun packages/lythoskill-deck/test/runner.ts` loads scenarios via a player CLI and returned `0/0 passed` with "0 个 CLI 场景" — it needs an external agent CLI, so it is not a locally meaningful signal in this review. Everything I verified about the scenario comes from its files, not from that runner.

---

## Summary

**HIGH gaps: 1. LOW gaps: 7.**

**Most serious finding**: the acceptance criteria quote a deck test baseline of `249 pass / 250 tests / 657 expect`, and that number cannot be produced from the sources the quoting commit shipped — the actual figure at the pinned sha is `253 pass / 254 tests / 661 expect`, and the deck test sources are byte-identical between `04b193f1` and HEAD (0 files changed), with the literal `it(`/`test(` count in those sources being 254. It is the exact "bare, unreproducible test count" failure (B5) this card exists to fix, sitting in the card's own Acceptance Criteria.

**Refuted outright**:
- The deck baseline above (§3.1). Its substantive half — `0 fail` — survives.
- `cortex … 3xx expect` — it is 280 (§3.1).
- "20 个 showcase 条目" — it is 19, at `04b193f1` and at HEAD (§3.13/§4.4).
- The B19 premise *as originally written* is refuted by the card's own later evidence, and I confirmed the refutation independently (§3.11).

**Also worth reporting, in the card's favour**: I attempted to falsify the ADV-7 claim — that one of the gate's own adversarial cases was red against the very commit the `8.5/10` gate pinned — and could not. Extracting `c8ebcc76`'s `adapter-policy.ts` and running the assertion unmodified yields 11 where the case expects 1, red. The ZK bundle is byte-identical to its `/tmp` original (which still exists on this machine). And the card's correction of the judge's "silently deleted" wording is right: the rogue symlink *is* reclaimed, but stdout prints `🗑️ Removed: rogue-handmade`.
