{
  "name": "lythoskill-site",
  "private": true,
  "scripts": {
    "dev": "bun scripts/inject-version.ts && vitepress dev .",
    "build": "bun scripts/inject-version.ts && vitepress build .",
    "preview": "vitepress preview ."
  },
  "dependencies": {
    "vitepress": "^1.6.4"
  }
}
