# EPIC-20260503010218940: Cortex 流转自动化 + epic 双轨纪律落地

> Cortex 流转自动化 + epic 双轨纪律落地

## Status History
<!-- machine-parseable table: directory = current status, last row = latest record -->

| Status | Date | Note |
|--------|------|------|
| active | 2026-05-03 | Created — 合并 ADR-20260503003314901 + ADR-20260503003315478 实施 |

## Lane / Granularity 标记(per ADR-20260503003315478)

- **Lane**: `main`(当前迭代主线)
- **Workflowy 心智**: 当前 zoom in 节点;同时无 emergency lane epic
- **首发示例**: 本 epic 自身正好用作"main lane = 1 个聚焦 epic"的首个实例

## 背景故事

合并 ADR-20260503003314901(git-coupling 自动流转)与 ADR-20260503003315478(epic 双轨纪律)的实施。两份 ADR 处理 cortex 这个 agent OS 雏形上的两件互补事情:

- **ADR-A** 让治理流转自动化(commit trailer + post-commit hook + 跟随 commit)
- **ADR-B** 让 epic 创建受双轨 + checklist 约束(workflowy focus + admission control)

两件事在同一个迭代里落地,outcome 一致:**cortex agent OS 雏形从手操升级为有 admission control 与 scheduling discipline 的状态机驱动系统**。

## 需求树(workflowy zoom-in 心智)

### 主题A: Cortex CLI 状态机扩展 #backlog
- **触发**: ADR-A 后续 T1 + ADR-B 后续 1 的硬前置
- **需求**: ADR / epic 状态机命令对齐 task 的 start/review/done/...
- **实现**: TASK-20260503010227902(T1)
- **产出**: `cortex adr accept|reject|supersede` + `cortex epic done|suspend|resume`
- **验证**: 三类文档都能用 CLI 流转,不再 git mv

### 主题B: Epic 创建期双轨 + checklist #backlog
- **触发**: ADR-B 后续 1+2+5
- **需求**: epic create 加 lane 选择 + 5 题 checklist + probe lane 检查
- **实现**: TASK-20260503010228602(T2)
- **产出**: `cortex epic create --lane`、lane-full 拒绝、`--override` 应急口、probe lane warn
- **验证**: 第 3 个 active epic 创建被拒(除非 override)

### 主题C: Git hook 自动流转 #backlog
- **触发**: ADR-A 后续 T2 + T3
- **需求**: post-commit 解析 trailer 触发流转;pre-commit 软提醒
- **实现**: TASK-20260503010229362(T3)+ TASK-20260503010230554(T4)
- **产出**: `.husky/post-commit` 跟随 commit + `.husky/pre-commit` 软提醒
- **验证**: commit 带 `Closes: TASK-XXX` 自动产生跟随 commit

### 主题D: 三层文档镜像 + skill 同步 #backlog
- **触发**: ADR-A 后续 T4+T5 + ADR-B 后续 4
- **需求**: AGENTS / CLAUDE / memory 三层 + cortex skill SKILL.md / README 同步
- **实现**: TASK-20260503010231389(T5)
- **产出**: 跨压缩可恢复的 trailer 与 lane 约定文档
- **验证**: 新 agent 不读历史也能从文档获知 trailer 语法 + lane 规则

### 主题E: BDD 覆盖 #backlog
- **触发**: ADR-A 后续 T6 + ADR-B 后续 7
- **需求**: 用 `@lythos/test-utils` + subagent 覆盖 trailer FSM + lane FSM
- **实现**: TASK-20260503010231988(T6)
- **产出**: 12+ scenarios 覆盖三类文档 verb + lane 空/满/override + checklist skip/全过
- **验证**: 子 agent 能基于 scenarios 重现完整流转(agent + skill + CLI 控制回路验证)

## 技术决策

| ADR | 标题 | 状态 |
|-----|------|------|
| ADR-20260503003314901 | git-coupling for cortex governance documents via commit trailer | proposed |
| ADR-20260503003315478 | epic granularity discipline — one outcome per iteration | proposed |
| ADR-C(自然推演,暂不实现) | Cortex CW snapshot 与 AGENTS.md 渲染管道 | — |

## 关联任务

| 任务 | 状态 | 描述 |
|------|------|------|
| TASK-20260503010227902 | backlog | T1: 扩展 cortex CLI 状态机命令(ADR + epic 流转动词) |
| TASK-20260503010228602 | backlog | T2: 实现 cortex epic create 双轨 + checklist + probe lane |
| TASK-20260503010229362 | backlog | T3: 实现 husky post-commit trailer 解析 + 跟随 commit |
| TASK-20260503010230554 | backlog | T4: 实现 husky pre-commit 软提醒 |
| TASK-20260503010231389 | backlog | T5: 三层文档镜像 + cortex skill/README 同步 |
| TASK-20260503010231988 | backlog | T6: BDD 覆盖 cortex trailer + lane FSM |

依赖:T1 是 T3 的硬前置;T2 独立;T4 独立;T5 软依赖 T1+T2;T6 软依赖 T1-T4。

## 经验沉淀

(实施过程中追加 — 例如 trailer 解析 corner case、lane 实际操作中的 friction、checklist 5 题的真实命中率等)

## 归档条件

- [ ] ADR-20260503003314901 移到 `02-accepted/`
- [ ] ADR-20260503003315478 移到 `02-accepted/`
- [ ] T1-T6 全部到 `04-completed/`
- [ ] `cortex probe` 跑过无 warning(三类文档状态一致)
- [ ] 跑通至少一次 commit 带 trailer → 自动产生跟随 commit
- [ ] BDD scenarios 全绿
- [ ] 至少 1 名 subagent 通过 task = bootloader 模式独立完成 1 张 task(验证 control-loop 假设)
