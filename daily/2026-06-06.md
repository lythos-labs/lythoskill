# Daily — 2026-06-06

## Ground Truth

**Git**: HEAD = `8f5feb7` (final ground truth 2026-06-04)
**Version**: v0.16.0
**Deck**: 15 skills
**Branch**: main, clean working tree (working tree has uncommitted changes from this session)
**Active Epic**: 0
**Active Task**: 0

## Session Handoff — AGENTS.md v2 重构 + ZK Review 方法论落地 + 三轴记忆模型

### Completed

1. **AGENTS.md v2 draft** (`drafts/AGENTS-v2-draft.md`, 430 lines)
   - 从 1283 行压缩到 430 行，4-zone 结构（Foundation / Frameworks / Operations / Reference）
   - **Activation map 设计原则**：每个 section 触发一个明确行为
   - **"Mechanical first, understanding later" onboarding**：Start of Session 第一步是 `deck link`，不先理解再行动
   - **Bootstrap 自愈**：顶部检查 `.claude/skills/` 是否空 → 跑 `deck link`
   - **Compaction 定义回流**：顶部 warning 自含 "compaction = context window overflow"
   - **Skills branch push race** 回流到 Critical Gotchas
   - **ZK Review Gate** 完整 prompt 模板 + gap 处理决策表（Fill / Challenge / Ignore）
   - **未替换当前 AGENTS.md** — 等 ZK review 通过后再切换

2. **5 个 ref docs 创建**（从当前 AGENTS.md 剪切重构）
   - `release-auth-workflow.md`：发布流水线、auth 边界、lock-step versioning
   - `thin-skill-pattern.md`：三层分离 + build pipeline + "为什么 commit build output"
   - `intent-plan-execute.md`：fractal 模式 + IO 注入表 + Plan 必须包含 research
   - `arena-runtime.md`：timeout 映射、CWD 行为、judge 设计、HATEOAS 错误——已清理 `.agent.md`/`TASK.agent.md` 残留
   - `project-hotspots.md`：高风险文件 + 改前必读 + Prerequisites 段（locator/deck/trailer/cold-pool 定义）

3. **ZK Review 方法论落地 + dogfooding**
   - 外部项目反馈 → cortex 体系完整沉淀
   - 3 个 cortex tasks 创建 → ZK Review（2 agents, 4 real gaps found and fixed）→ 完成归档
   - `references/zk-review.md` 加 frontmatter + SSOT 记忆管线图 + 收敛循环 + fork 语义降级路径
   - cortex SKILL.md 加 ZK Review description + when_to_use 触发器 + refs 表
   - AGENTS.md 加 ZK Review Gate subsection + ZK Validation 双向引用

4. **scribe SKILL.md 重写**
   - description 从 "Session memory writer" → "Session context dump for things WITHOUT structured carriers"
   - Value Boundary 图：有 task/adr/epic carrier → 写 carrier；无 carrier → scribe

5. **skill-deck.toml 自我文档化**
   - `[deck]` 段加 cold_pool / working_set 注释（agent 读 toml 时就理解）

6. **weekly/2026-W24.md**
   - core_thread: "1000+ commits 后，瓶颈不在 agent 能力而在 agent 记忆——项目发展三条互补线：外化/压缩/清零"

7. **BDD 迁移状态确认**
   - `.agent.md`：0 残留，完全退役
   - `reproduce.sh`：37 个（showcase/ 16 + packages/ 21）
   - 迁移完成

### Key Decisions

- **Zone 标注去掉更新频率**：Z1/Z2/Z3/Z4 是组织分隔符，不是稳定性约束。Gotchas 是发现即补充，不受任何频率限制
- **Activation map > knowledge base**：AGENTS.md 的职责是触发行为，不是承载知识。每个条目都对应一个 "遇到 X → 做 Y"
- **Ref doc 回流原则**：ref doc 引用的概念必须在 AGENTS.md 有立足点（compaction、skills branch 已回流）
- **`skills/` commit 原因**：面向 agent 的分发产物，和 README 同类。Vercel skills.sh 等生态也是这个模式——纯消费者只看 skills/ 就是一个独立 skill collection
- **Plan 层必须包含 research**：不搜索 = 臆想的 Plan。Agent 自己搜索 MDN/Bun docs/GitHub issues

### Pitfalls

- **Agent spawn 系统性失败**：当前 session 的 `reasoning_effort` 配置阻止了所有 subagent spawn（所有 model + subagent_type 都报 "reasoning options type cannot be disabled"）。最后的 ZK review 未能执行——用户将安排其他 agent 完成
- **ref doc 写好后未交叉验证 git log**：arena-runtime.md 最初包含 `.agent.md`/`TASK.agent.md` 残留——这些已被 BDD 迁移标记为废弃。教训：从当前 AGENTS.md 剪切内容到 ref doc 时，必须先 `git log --oneline -5 <package>` 检查是否有相关迁移
- **喊缺失前未查已有文档**：combo/transient/fork 被标为 "missing" 但 deck SKILL.md 已有完整文档。教训：no source, no claim
- **画靶子打**：zone 更新频率标签（months/weeks/days）是我自己加的，然后 ZK agent critique 我的设计，我再把 critique 当问题来修

### 0. Verify Current State

```bash
git diff --stat              # AGENTS.md, INDEX.md, SKILL.md × 2, skill-deck.toml modified
git status --short             # 5 new ref docs + drafts/ + weekly/ + tasks/ untracked
bun --filter='*' run test    # 714+ pass, 0 fail
```

### Next Steps

1. **AGENTS.md v2 替换**：用户将安排其他 agent 做 ZK review 验证 `drafts/AGENTS-v2-draft.md` → 通过后替换当前 `AGENTS.md`
2. **5 个 ref docs 的 source 同步**：`.claude/skills/` 下已就位，`packages/*/skill/references/` 下部分同步（release-auth 和 thin-skill 的 creator source 可能缺 references 目录）
3. **commit 策略**：draft AGENTS.md + 5 ref docs + skill-deck.toml + scribe SKILL.md + zk-review.md + weekly + tasks → 一个大型 "docs: AGENTS.md v2 refactor" commit，还是拆成多个？

### Temp Artifacts

- `drafts/AGENTS-v2-draft.md`：保留到 ZK review 通过
- `drafts/` 目录：新建，生命周期未定——后续需要决定是正式目录还是临时 workspace
