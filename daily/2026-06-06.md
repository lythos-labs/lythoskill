# Daily — 2026-06-06

## Ground Truth

**Git**: HEAD = `1170d99` (chore(cortex): complete TASK-20260606222341617)
**Version**: v0.16.0
**Deck**: 15 skills
**Branch**: main, clean working tree
**Active Epic**: 0
**Active Task**: 0

## Session Handoff — AGENTS.md v2 落地 + ZK Review dogfooding + Guard 增强

### Completed

1. **AGENTS.md v2 替换正式文件**（47a820e）
   - 从 1283 行压缩到 465 行，4-zone 结构（Z1 Foundation / Z2 Frameworks / Z3 Operations / Z4 Reference）
   - 所有引用路径从 `.claude/skills/` 改为 `packages/*/skill/references/`（source of truth）
   - 新增：ZK Review Gate 完整操作指南、ZK Validation Pattern、Pass-by-reference 元方法论、Side Decks 模式
   - 新增：Troubleshooting 小节、命令路径统一（full path + shorthand parenthetical）

2. **ZK Review dogfooding**（两轮收敛）
   - Round 1: ZK agent 发现 4 broken links + 5 critical gaps（inconsistent paths、undefined terms、missing examples）
   - Round 2: reference integrity 验证通过（25/25 links resolve）
   - 关键洞察：ZK agent 的"没找到"不等于"不存在"——可能是 agent 没 follow 引用（如 skill-deck.toml 已自描述 cold pool，但 agent 没读）
   - **Persona 修正**：ZK agent = "第一天入职的 senior"，不是"关闭世界知识的老油条"

3. **5 个 ref docs 创建并同步**
   - packages/lythoskill-arena/skill/references/arena-runtime.md
   - packages/lythoskill-creator/skill/references/release-auth-workflow.md
   - packages/lythoskill-creator/skill/references/thin-skill-pattern.md
   - packages/lythoskill-deck/skill/references/intent-plan-execute.md
   - packages/lythoskill-project-cortex/skill/references/zk-review.md
   - 同步到 skills/ 和 .claude/skills/

4. **Guard 增强**（d6c5b03）
   - adr-check.sh: 新增 test 文件 import/call 一致性检查
   - pre-commit-test.ts: 同时检查 fail 和 error 数量（之前只检查 fail）
   - AGENTS.md: 新增 "see a bug, fix a bug" 硬规则

5. **Incident 修复**（bff346a）
   - 发现 33 个测试文件的 `test()` / `it` import 不匹配
   - 其中 2 个是 pre-existing bug（Claude Opus 4.7, 2026-06-03, commit 659c026）
   - 31 个是我用 sed 批量替换时引入的——验证了 AGENTS.md "sed 是 detector，不是 scalpel"
   - 全量修复，13/13 packages 测试通过

6. **Cortex 治理**
   - TASK-20260606220626030: AGENTS.md v2 refactor → completed
   - TASK-20260606222341617: bun test silent failure incident → completed
   - Wiki lesson: cortex/wiki/03-lessons/2026-06-06-bun-test-error-exit-code-0-silent-test-failure-lesson.md

### Key Decisions

- **"Pass by reference, not by value"**：给 subagent 传文件路径而非内容，包括 task card、AGENTS.md、deck configs
- **Source paths > build paths**：AGENTS.md 引用指向 `packages/*/skill/references/`，不依赖 `.claude/skills/` 已填充
- **Convergence is coordination, not elimination**：ZK Review 的目标不是"零 gaps"，而是"gaps 不再产生新信息"
- **"See a bug, fix a bug"**：发现缺陷即修复，不因为 "git log 显示是别的 agent 干的" 就忽略

### Pitfalls

- **sed 批量替换破坏 31 个文件**：明知 AGENTS.md 写了 "sed 是 detector，不是 scalpel"，还是用了 `sed -i`。自食其果，花了 30+ 分钟修复。
- **bun test "error" 不算 fail**：exit code 0 但有 "Unhandled error between tests" 时，pre-commit 旧逻辑会放行。已修复。
- **ZK agent 的盲区也是信号**：agent 报告 "cold pool 未定义" 但 AGENTS.md 明确引用了 skill-deck.toml——这说明 agent 没 follow 引用，不是 doc 的 gap

### Next Steps

1. **Cleanup temp artifacts**: `drafts/AGENTS-v2-draft.md` 和 `AGENTS.md.bak.*` 可以删除（v2 已正式替换）
2. **Daily/scribe 格式**: 本次 session 的 daily 是追加还是覆盖？当前文件是覆盖模式
3. **Push**: 8 个 commit 待 push

### Temp Artifacts

- `drafts/AGENTS-v2-draft.md`：v2 已正式替换 AGENTS.md，此 draft 可删除
- `AGENTS.md.bak.20260606-220608`：备份可删除
