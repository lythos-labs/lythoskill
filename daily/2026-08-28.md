# Daily — 2026-08-28

## Session Handoff — 复健 session：site UX 对齐 + 3 ADR + 三个月预判对账沉淀

### 0. Verify Current State

**Git**: HEAD = 本会话最后一个提交（以 `git log -1` 为准），已推送
**Version**: v0.17.11（无新版本动作）
**Branch**: main，clean working tree（提交后）
**Active Epic**: 0
**Active Task**: 0 in-progress，0 in review（TASK-20260828002450069、TASK-20260828011012367 用户 LGTM 验收关闭）
**Tests**: `bun --filter='*' run test` EXIT=0，全绿
**Proposed ADRs awaiting user decision**: ADR-20260828004129143（host-agent handoff）、ADR-20260828004129233（adapter 生命周期策略）、ADR-20260828005453077（独立 ZK skill 孵化）

```bash
# Verify: if output diverges from the fields above, this handoff is stale
git status --short && git log --oneline -5
```

### Completed

1. **站点 UX/叙事对齐（TASK-20260828002450069 → review）** — ZK review 干净（3 个 P3 已修）。抓到和 08-27 curator 事件同类的 P1：guide Level 3 的 `arena vs --deck-a/--deck-b` 是不存在的 flag（真实形式 `vs --config arena.toml`）；"auto-detect player" 是编造的特性。另修：deck 数 22→24、`/articles/` 无 sidebar、guide 无 Level 锚点。
2. **三份 ADR（proposed，待用户拍板）** — host-agent handoff（主力 agent × 不同 deck 是自然 UX，CLI 只在 cross-player 时出场）、adapter 生命周期策略（version range + alias 表，应对 kimi-cli→kimi-code、deepseek-tui→DSH 这类 upstream 更迭）、独立通用 ZK skill 孵化（方法论通用/门禁属项目的切分已写入）。
3. **三个月预判对账沉淀** — `cortex/wiki/02-research/2026-08-28-early-predictions-vs-august-reality-audit.md`：45 篇 4–5 月文档的外部预判 × 2026-08 现实（搜索 + 本 session 实测 + git 取证）。11 命中 / 4 失手 / 若干未到期。**最抗衰老的是怀疑层**（05-07 research-quality-audit）。含用户评论假说 H1–H8。
4. **agent-UX 反馈落盘** — `cortex/wiki/03-lessons/2026-08-28-agent-ux-feedback-fresh-model-session.md`：5 条 worked + 3 条 friction（模板中文小标题诱导非 ASCII 标题被拒、handoff 锚点落后、version.json 本地陈旧）。
5. **cortex wikiSubdirs drift 登记（TASK-20260828011012367）** — config.ts 的 wiki 目录默认值是化石：`04-legacy` 从未有过一个文件，`02-research`/`04-ssot`/`05-archived` 在盘上但不在配置里 → 生成的 wiki/INDEX.md 静默丢掉 SSOT 层。git 取证否定了"agent 改名"假说（04-ssot 是 dreaming epic 新生，af585375，用户确认）。
6. **DSH 调研 + 站点守卫机械化登记** — TASK-20260828004417068（双向结合调研）、TASK-20260828003758156（命令/数字守卫进 CI，验收标准内含两个历史 bug 的负测试）。
7. **user-sim 校准转正** — `.private/decision-profile.md` 4 条 pending 全部评级入 Calibration log；新增 v3 段落：P-async-inbox-fifo (95)、A-assume-upstream-churn (85)（`.private/` gitignored，不入库）。

### Pitfalls

- **`bun test | tail -5` 的退出码是 tail 的**——第一次 test gate 险些变成 `|| true` 类错误（pipeline 无 pipefail 时只看最后一个命令）。重跑完整输出确认 EXIT=0。教训：任何"管道 + 截断"都要显式确认退出码来源。
- **bash 双引号内 `$()` 里的撇号（`docs'`）会破坏解析**——commit message 含撇号时用 `git commit -F <file>`，别用 `$(cat <<'EOF')`。
- **post-commit hook 会顺带提交未跟踪的 cortex 工件**——本次 Review trailer 的 follow-up commit 把 3 个 ADR + 3 个任务卡一起带进去了。行为正确（都是该入库的），但下次想要更细粒度时先提交工件再写 trailer。

### Key Decisions

- **站点叙事不动骨架**——08-27 那轮 ZK 走查过的结构（Guide→Architecture→Ecosystem→Philosophy→Articles）保持，只修事实层和导航缺口。
- **三 ADR 全部 proposed 而非 accept**——用户处于探索模式（"拉一个adr讨论"），决策权留给用户。
- **编号 drift 不重编号**——`02-faq`/`02-research` 重号保留，config 对齐现实；重命名会破坏 ADR/daily 里的引用。

### Next Steps

1. **用户验收 TASK-20260828002450069**（03-review）→ 确认后 `Closes:` trailer。
2. **用户逐个拍板 3 个 proposed ADR**——拍板后各自产生实现任务（ADR-233 拍板 → kimi-code alias；ADR-077 拍板 → ZK skill 抽取）。
3. （可选）跑 DSH 调研任务：`arena single --deck examples/decks/deep-research.toml --brief "..."`（TASK-20260828004417068 有验收标准）。
4. 推送后盯 CI + Pages：`gh run list --workflow="Deploy VitePress site to Pages"`（site/ 有改动）。

### Temp Artifacts

- `/tmp/test-gate-20260828.log`（完整测试输出）、`/tmp/commit-msg.txt`、`/tmp/commit-msg2.txt`——均为临时，无需保留。

---

## Addendum — cortex wikiSubdirs 修复（daily 写完后发生）

- **TASK-20260828011012367 → review**：config.ts 的 wikiSubdirs 对齐盘上真实结构（六个目录，砍掉从未有文件的 04-legacy）；generate-index/wiki/stats/init 四个消费方全部更新；probe 新增 wiki 结构双向 drift 检查（盲 guard 补上，含负测试）。129 tests 绿。stats.ts 顺带改成遍历 config——同类 drift 结构性免疫。
- 实现由 coder subagent 完成，P1 声明我亲自复核（测试数、INDEX 章节、probe 输出）。
- 提交 `0d7c2c90` + hook follow-up，已推送（HEAD `66af9c28`）。

## Addendum 2 — 外部 K3 agent 报告 → epic + 任务登记

- 用户转来外部 Kimi K3 agent 的 mirror probe 误报报告（受限网络，ghfast.top）。**逐条核实 + 本机实测四路对照全部属实**（HEAD 403/405 vs GET info/refs 200 vs ls-remote OK；代码位置 mirror.ts:118-127、add.ts:283-291）。
- **EPIC-20260828111425886**（lane main，external agent accessibility and onboarding）聚合外部反馈类任务：本 bug（TASK-…54804）+ 08-27 ZK 入驻三任务 + 站点守卫机械化 + 已完成的站点对齐。
- 已推送（`6016f2ff`）。下一步候选：按任务卡修 mirror probe（报告自带可直接评审的分级探测设计）。

## Addendum 3 — ADR 批量落地 + 5 个在途任务提交 + 9 张衍生卡过 ZK（goal session）

- **5 个 in-progress 任务按卡提交**（上个 session 中断的批次，测试门 EXIT=0 先行验证）：curator fail-loud（`d9237381`）、deck link 切换旧工作集警告（`c7a9dafb`，决策 b：只警告不自动删）、link 输出去重+目的地标注（`a7d088c8`）、站点命令守卫进 CI（`9403f1f4`）、mirror probe 双层改造（`6ff09d89`，ls-remote 为 ground truth，probe 变 advisory）。
- **5 个 proposed ADR 全部按时间顺序接受**（用户拍板）：ADR-20260613190449007（方案A）、ADR-…657（cortex 就是 SSOT，Option A + B/C pilot）、ADR-…143（host-agent handoff，Option B）、ADR-…233（adapter 生命周期，Option B）、ADR-…077（孵化独立 ZK skill，Option B）。
- **ADR-20260613190449007 方案A 执行一次**：`cortex/wiki/01-patterns/INDEX.md` 初始分类（`7de92b8c`，subagent 完成）：56 篇 pattern → 3 P0（thin-skill-pattern、workspace-protocol、zk-review-cognitive-foundations）/ 15 P1 / 38 P2。维护接线 = TASK-…425（backlog）。
- **9 张衍生任务卡**建好并过两轮 ZK：round 1（4 NEEDS-WORK / 5 PASS-WITH-NITS）→ 修复 → round 2 复查最差 4 张 → 全 PASS / PASS-WITH-NITS（nits 已 inline 修掉）。两张有硬性依赖：…502 等 …425 的证据积累；…828 等 …777 的契约落地。
- **ZK round 1 抓到的最重要事实错误**（已验证修正）：kimi-code 的 binary 就叫 `kimi`（v0.38.0），不是 `kimi-code`；legacy 是 `kimi-cli`（v1.45.0，自报 "kimi"——版本判别要看 major 版本号）。kimi 适配器真正的静默退化路径是 `parseKimiStreamJson` 从不 throw → 空文本当成功返回；kimi.ts:126-130 的 catch-all 是死代码。
- **实测**：kimi-code session 没有任何 `KIMI*` env 标记，只有 `CLAUDE_CODE_SSE_PORT`（fork 自 claude-code）——host 检测只能区分"有无 agent host"，宿主无关的 handoff 文案是正确设计（TASK-…671 已写入）。gh token（keychain `lythos-agent-pat`）实测可用：api user + issue list OK，当前 0 open issues。

### Pitfalls
- **post-commit hook 的 follow-up commit 会扫进所有未跟踪/未提交的 cortex 工件**——ADR decision 编辑和 9 张新卡被 `83081a05` 提前卷走，导致 4 个 ADR 的 accept 只能补空 trailer commit。教训（昨天那条的强化版）：ADR decision 编辑要和 accept trailer 同一 commit，工件先提交再写 trailer。
- ZK reviewer 说"文件不存在/状态不符"时先核对时间点——本轮 3 个 P1 其实是"评审发生在 accept commit 落地之前"，接受提交后自然消解。时序类误报要与真实错误分开处理。

### Next
- 5 个已提交任务可按工作流走 review（ZK skeptic 审实现 → Review trailer → 用户验收）。
- backlog 里 9 张卡可执行性已验证；建议顺序：…425（dreaming 接线，解锁 …502）→ …671（host handoff）→ …777（kimi 探针，解锁 …828）→ 其余。
