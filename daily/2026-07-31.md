# Daily — 2026-07-31

## Session Handoff — 双卡关闭 + epic 归档 + 0.17.3 修复发布（workspace:* 泄漏）

### 0. Verify Current State

**Git**: HEAD = `fa4fb264` (chore(cortex): TASK-1284 to review) — pushed to origin（注：trailer 后 hook 可能还有一个 follow-up commit，以 `git log -1` 为准）
**Version**: **v0.17.3** — 已发布到 npm，13 包全部外部可解析
**Deck**: 14 skills linked;冷池无 drift(07-27 愈合）
**Branch**: main, clean working tree
**Active Epic**: 0（molting epic 已归档 99-done)
**Active Task**: 0 in-progress, **1 in review**(TASK-…1284,等用户标记 done)
**CI**: STILL UNVERIFIED — `.github-token` 过期（gh 401,07-20 起挂起）；发版后 validate-example-decks 未确认

```bash
# Verify: if output diverges from the fields above, this handoff is stale
git status --short && git log --oneline -3 && ls cortex/tasks/03-review/
```

### Completed

1. **Goal 关闭**(07-27 goal:双卡推进验收 + ZK ≤3 轮）— 用户 LGTM 后 `Closes: TASK-…610` + `Closes: TASK-…556` + `Epic: EPIC-20260717161516583 done` 单提交三 trailer,hook 自动搬运完毕，probe 一致。v1 卡 TASK-…693 早已 completed。
2. **TASK-20260730140801284(npm workspace:* 泄漏）→ 03-review** — 用户报告 `bunx @lythos/skill-deck@latest link` 炸 (`workspace:* failed to resolve`)。调查：0.17.2(07-10 发布）所有含内部依赖的 8 包 manifest 漏改写；publish.sh 改写步骤自 05-18 完好（当日实测有效）,06-15 的 0.16.1/0.17.0 干净 → 07-10 那次发布偏离了脚本（具体路径无档可查）。处置：guard 脚本（`scripts/check-published-manifests.ts`，问 npm 不问 bunx 缓存）+ publish.sh 末步接线 + AGENTS.md [LEAK] gotcha;**0.17.3 发布成功**(13 包 E2E + guard 双绿）；隔离 HOME 干净环境验证通过。ZK 2 轮 APPROVE(round 1 抓出 guard 断网假绿 + 只查 dependencies 两个 P2，已修：fail-closed + 三段扫描，rewriter 补 devDependencies)。
3. **两次 token 事故**：第一次替换的 .npm-access token 缺 publish 2FA-bypass → E403；换 granular + bypass-2FA 后成功。`.github-token` 仍过期。

### Key Decisions

- **发布顺序按 [VERSION] gotcha**:test → bump → publish → push(npm 先于 github，外部用户先能装才能看到文档）。0.17.3 纯 manifest 修复 = patch，符合 SEMVER。
- **guard 问 npm 而非 bunx**:bunx 全局缓存在发布机上会掩盖漏出（E2E 可能因此假绿）；新 guard 直查 registry,fail-closed（查不到 ≠ 干净）。
- **0.17.2 不可修复只能覆盖**：超 72h 无法 unpublish;0.17.3 latest 已指向干净 manifest，用户侧 `bunx @latest` 自动痊愈。

### Pitfalls

- **npm granular token 的 UI 坑**：选 scope 时 checkbox 会自动取消（用户原话）；发布需要 "Bypass two-factor authentication" 选项，否则 E403。更稳的长期方案已给用户：npm Trusted Publishing(OIDC，无长期 token)。
- **管道吃退出码（又一次）**:`bun guard.ts | tail -1` 后 `$?` 是 tail 的——live 验证 guard 时必须重定向到文件再看 EXIT。这正好是该 guard 存在的同类错误，属惯犯。
- **发布相关会话没记录执行细节**:0.17.2 的泄漏之所以无法复盘，是因为 07-10 的 daily 只记了 "publish all packages" 没记怎么跑的。教训：**release 类操作，daily 里要记执行的命令本身**。
- **"本地绿" ≠ "CI 绿"的环境依赖型测试**:R5 fixture 在 macOS（默认分支 main）绿、在 ubuntu CI（master）红——涉及 git 默认分支、时区、locale 的 fixture 一律显式钉死（`-b main`)，不依赖 runner 环境。以及：gh 的 401 要先看 `gh auth status`——keyring 里的旧 token 优先于文件，"换了 token 还 401" 先怀疑读取源而不是权限。

### Next Steps

1. **用户标记 done**:`Closes: TASK-20260730140801284`（过目后）。
2. **CI 验证（挂起 11 天）**：更新 `.github-token` 后 `gh run list`（尤其 validate-example-decks + 0.17.3 发布后的 CI 状态）。
3. **user-sim 校准**(`.private/decision-profile.md` Pending calibration 区，07-27 留的 4 条 guesses)。
4. **Follow-up 候选（未登记）**:bump 流程不刷新 bun.lock（pre-existing;`--frozen-lockfile` 仍过，但文档与实际不一致）;5 个 @lythos org 包在发布管线外（mermaid-*/design-system/skill-journalist，今日均 clean)。
5. (P2 legacy) 2 个 07-10 stale backlog 任务。

### Temp Artifacts

- `/tmp/guard-0172.log`、`/tmp/gate-*.log` — 验证输出（/tmp 自动清理）
- 无其他；本次全部证据已入库（任务卡 Progress Log + AGENTS.md [LEAK])

---

## Work Log

- 07-30 — 用户报 bunx 炸；调查定位 0.17.2 全系列 workspace:* 漏改写；登记 TASK-…1284;guard 脚本 + 测试 + 接线 + AGENTS.md gotcha
- 07-31 — 用户 LGTM 双决策：Closes 双卡 + epic done（单提交三 trailer,hook 搬运，probe 一致）;bump 0.17.3;publish 第一次 token E403 → 用户换 bypass-2FA token → 发布成功（13 包 + E2E + guard)；隔离 HOME 干净验证；ZK 2 轮 APPROVE（修 fail-open + 段覆盖）;Review trailer 推送；daily
- 07-31 — 用户更新 .github-token；定位 401 根因 = **gh keyring 旧 token 优先于文件**（新 token 本身有效，`GH_TOKEN=$(<.github-token)` 前缀即可用）；发现 main 自 07-27 起 CI 红 = R5 fixture 依赖本机默认分支（macOS=main / CI ubuntu=master）→ `git init --bare -b main` 钉死；fixture 在 /tmp 复验通过；推送后 **CI 转绿**(run 30608402270)
