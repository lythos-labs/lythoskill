# Daily — 2026-05-20

## Ground Truth

**Git**: HEAD = 42c01aa (named combos)
**Version**: v0.15.2 (published)
**Deck**: 13 skills
**Kanban**: TASK-20260520143950404 completed (combo prompt), EPIC-20260520124010693 completed
**Uncommitted**: none — working tree clean

## Session Handoff — curator find CLI + Search Precision Ladder + 3 BDD scenarios

### Completed

1. **curator find CLI** (`cli.ts`)
   - `runFind()` — bare name → full path lookup, 3 modes: HIT / MISS / MULTIPLE
   - local path → locator path via `catalog_meta.pool_path`
   - Tests F1-F4: single hit, miss with guidance, multiple disambiguation, missing args
   - ADR-20260519225831495 accepted design

2. **curator SKILL.md — major documentation upgrade**
   - Search Precision Ladder: gh search code > gh repos > WebSearch site: > WebSearch
   - gh CLI Auth Bootstrap: install → token → fallback (gh is backbone, not optional)
   - Three Discovery Scenarios: 知名 / 知人 / 知 repo
   - Repo Exploration / peek pattern: gh api peek without cloning (monorepo-friendly)
   - Real external examples: MiniMax-AI fullstack-dev, JimLiu/baoyu, op7418/guizang, lijigang/ljg

3. **Three Agent BDD scenarios** (all under `packages/lythoskill-curator/test/scenarios/`)
   - `curator-find-bdd` — bare name → find → deck add (HIT path)
   - `curator-find-fuzzy-bdd` — WebSearch → gh code for fuzzy person search
   - `curator-find-repo-bdd` — gh api peek for monorepo exploration
   - All three verified with zero-knowledge haiku subagents (parallel run)

4. **Medium model execution advantage validated**
   - 3 BDD scenarios run with haiku: 14s / 114s / 42s
   - BDD-2 (fuzzy) found 9 repos — 4 more than opus baseline
   - Memory entry: `feedback_medium_model_execution_advantage.md`

5. **Cold pool enrichment**
   - Added: MiniMax-AI/skills, op7418/guizang-ppt-skill, lijigang/ljg-skills
   - Tagged: fullstack-dev, ljg-think, ljg-book, ljg-qa, ljg-rank, baoyu-translate
   - thinker niche: 4 skills documented

6. **Hermes Ralph loop ≈ cortex kanban pull alignment**
   - Same GTD pull pattern, different scales
   - Decision: prefer community multi-agent invoke over building own scheduler
   - Memory entry: `project_hermes_ralph_loop_cortex_kanban_alignment.md`

### Key Insight: SMART task card = stable atom

Task card granularity doesn't change with execution layer. `TASK-20260519205953163` works
as cortex kanban item, Ralph loop item, or future scheduler item without translation.
The stable contract is the task card itself — the pull mechanism is replaceable.

### Pitfalls

- **Cold pool has 5 broken SKILL.md files**: YAML parse errors in guizang-ppt-skill,
  sober (localhost), think-before-act (localhost), gstack, design-doc-mermaid.
  scanSkill() catches but the runCurator loop doesn't skip them cleanly — scan crashes.
- **also_link_to string format**: project's own deck was using `".agents/skills"` (string),
  silently ignored by Array.isArray(). ADR L76 backward compat never implemented.
  Fixed via parseAlsoLinkTo() helper with deprecation warning.

### Uncommitted

- `packages/lythoskill-curator/` — find CLI + SKILL.md + tests + 3 BDD scenarios
- `cortex/adr/01-proposed/ADR-20260519225831495-` — curator find design
- `cortex/tasks/01-backlog/TASK-20260519224912252-` — bare name lookup task
- `cortex/wiki/` — epistemic gaps + head-skill verification (other agent)
- `cortex/adr/02-accepted/` + `cortex/epics/99-done/` + `cortex/tasks/04-completed/` — curator add --output work (other agent)
- `showcase/2026-05-20-head-skill-self-claim-verification/` — (other agent)

### Temp Artifacts

- `/tmp/curator-find-bdd-*/` — BDD test directories, can be cleaned
- `/tmp/also-link-to-bdd-*/` — previous session BDD test dirs

### Next Steps

- User decides

### Verify Current State

```bash
git diff 9f22d49 --stat                    # Should show 4 modified + many untracked
git status --short                          # M curator/*, ?? BDD dirs, ?? cortex/*
git log --oneline -3                        # 9f22d49, 76c6f4f, 233b686
bun packages/lythoskill-curator/src/cli.ts find ljg-think --db ~/.agents/lythoskill/curator/catalog.db  # Should HIT
```

## Session Handoff — curator robustness, catalog co-location, ZK pipeline discovery, combo evolution

### Completed

1. **Curator scan robustness** — YAML parse error capture (6 found), status tagging, inferSource fix, skip reporting
2. **Catalog co-location** — 3 data silos → 1: scan/add output to `<pool>/.lythoskill-curator/`, find prefers populated DB
3. **Curator find enhanced** — multi-candidate hub/domain tags, deck link collision warning, isReadOnlyQuery blacklist fix
4. **skills.sh cross-ref** — 4/17 top skills in cold pool tagged; 155 wshobson + 305 antigravity skills tagged with domain/*
5. **ZK agent discovered 3 pipeline decks** — promo, site-content, research-archaeo; all in examples/decks/
6. **[combo] evolution** — old [combo] skills section → new [combo.<name>] prompt orchestration; all docs aligned
7. **BDD + docs** — scenarios bundled in skill/scripts/bdd/; niche taxonomy in SKILL.md; zk-subagent-patterns.md reference
8. **Version** — 0.15.0 → 0.15.1 → 0.15.2, published to npm

### Pitfalls
- Shell `$(bun ... 2>/dev/null)` in loops corrupts PATH — use SQLite directly for batch ops
- skills.sh ecosystem barely overlaps with cold pool (Vercel/Microsoft/Anthropic vs indie devs)
- 141 name collisions: antigravity (generic) vs wshobson (domain-specialized) — same capability slots

### Next
- Push to remote
- Maybe: ZK agent fills lythoskill site content using site-content deck

### Verify Current State
```bash
git status --short        # Should be clean
git log --oneline -5      # 42c01aa, d14ec43, 6eaa36e, f60f79c, c93fdfe
```
