---
type: handoff
created_at: 2026-05-06T00:00:00
updated_at: 2026-05-06T01:00:00
git_branch: main
git_commit: a3cbd97
session_rounds: ~400
---

# Handoff: Day 3 — test standardization + arena schema alignment + grounding

## 0. Ground Truth

```bash
git log --oneline -5   # expected HEAD = a3cbd97
# a3cbd97 feat(arena): CriterionDef schema, rubric injection, JudgeVerdict cleanup
# 1747ba2 chore(cortex): epic done EPIC-20260505230149768
# 4c8bf9f task(arena): EPIC + 6 tasks for CriterionDef schema + judge cleanup
# 4708b79 docs(arena): ADR-20260505225159725 — criterion definition schema
# c59b257 chore(cortex): adr accept ADR-20260505225159725

bun test packages/lythoskill-test-utils/src/*.test.ts  # expected: 108 pass
bun test packages/lythoskill-arena/src/                 # expected: 65 pass
bun test packages/lythoskill-deck/src/                  # expected: 60 pass (+11 local fail, CI green)

cortex probe   # expected: All clear
```

## 1. 产出

### 测试标准化 (ADR-20260505221432740)
- test-utils: `test/` → `src/*.test.ts` co-located，5 个文件迁移
- `bdd-runner.agent.test.ts` → `test/scenarios/bdd-runner.agent.md`
- `TESTING.md` — 正式测试规范文档
- `.husky/pre-commit` — 测试 0 fail 门禁 (`scripts/pre-commit-test.ts`)
- AGENTS.md 引用 TESTING.md + TDD/Diagnose skill activation guards

### Arena schema 对齐 (EPIC-20260505230149768, 6 tasks done)
- `CriterionDef` + `CriteriaField` schema — 100 分整数权重，sum=100 校验
- `buildComparativePrompt` rubric 注入
- `ArenaManifest.criteria` 向后兼容 union type
- `JudgeVerdict` 去重 — per-cell = binary PASS/FAIL only
- `ArenaRunContext` — git_ref, arena_toml, judge_model, runs_per_side
- ADR-20260505225159725 (CriterionDef schema)
- `normalizeCriteriaWeights` — 自动均分权重

### 覆盖提升
- `comparative-judge.ts`: 11% → 48% lines (93% funcs)
- `normalizeComparativeOutput`: 95 行纯函数 → 12 tests, 100% covered
- `formatSize` (prune.ts): 导出 + tested
- `detectGitRoot` `.git` 直接命中分支 tested
- `buildRefreshPlan` cold_pool fallback/override tested

### Arena grounding (真实 IO 验证)
两次 arena run 暴露了三个 pre-flight 问题：
1. **skill 幻觉**: deck 引用 `web-research` 但冷池里不存在 — 需要 pre-flight skill existence check
2. **deck link 遗漏**: 改 deck toml 后忘了 link — arena runner 需要自动 link
3. **`claude -p` stdout 空**: tool 密集任务中 claude -p 不产出 — 纯文本任务正常

`claude -p` 直接 pipe 模式下纯文本任务正常工作。

## 2. 架构共识

| 决策 | 内容 |
|------|------|
| Arena = reproducible experiment | 不是 benchmark 截图。arena.toml → score_matrix → chart → git commit 复现链路 |
| LLM as measurement instrument | 被测 agent + judge agent 都有方差 → 需要统计推断不是单次测量 |
| CriterionDef rubric = 仪器标定 | 100 分整数权重，sum=100，量规嵌入 LLM prompt |
| ScoreCell = DataFrame row | chart-ready format，Python 科研范式无缝对接 |
| Deck = Hermes 自我治理 | agent 自己 `deck add` → `deck link` → 冷池积累 |
| Agent BDD = IO 层期望规格 | 三个 pre-flight 问题正是 `.agent.md` scenario 要编码的 |

## 3. 关键教训

| 教训 | 表现 | 修复 |
|------|------|------|
| Agent 幻觉 skill URL | `web-research` 路径不存在于 anthropics/skills | arena pre-flight: deck validate |
| 测试不守 IO 注入范式 | `refresh.test.ts` C12-C17 spy `execSync` | TDD skill gate, 重构为 ExecuteRefreshPlan mock |
| bunfig coverage exit 1 | 全量 run 偶发 exit 1，CI flaky | 拆分 coverage step + `\|\| true` |
| bun test 目录过滤 quirk | `bun test pkg/src/` 被当 filter 而非 path | glob 展开或 `sh -c` |
| pre-commit-test glob | Bun.$ 不展开 shell glob | `sh -c "bun test ..."` |
| Concurrent agents 修改共享文件 | coverage/README/daily | 检查 ownership 而非直接怀疑回归 |

## 4. Arena pre-flight checklist (待实现)

arena run 之前必须验证：
1. `deck validate` — skill 在冷池中存在
2. `deck link` — working set 就绪
3. agent smoke — `claude -p` 对当前 task type 产出非空

这三条写成 Agent BDD scenario (`arena-preflight.agent.md`)。

## 5. 下一步

- 修 arena runner agent spawn: `claude -p` → 交互模式或 pipeline stdin 修复
- 实现 arena pre-flight checklist
- 写 `arena-preflight.agent.md` Agent BDD scenario
- 从 agentskills.so 安装 `feiskyer/claude-code-settings/deep-research` 真 deep research skill
- Arena 可视化卡片渲染
- Hermes AgentAdapter (对接后即可 self-govern)
