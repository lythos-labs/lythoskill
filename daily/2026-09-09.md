# Daily — 2026-09-09

> 本会话从 2026-09-08 深夜跨午夜延续。08 日的发布 + scribe 约束 + ZK 狗粮见 `daily/2026-09-08.md`(该文件已被 ZK reviewer 7 findings 修过,可信)。

## Session Handoff — 09-09 全天:arena A/B → name 对齐 → 纯英 sweep → coach 复审 → 全部推送

### 0. Verify Current State

**Git**: HEAD = `bc2053c7` (docs(skills): apply coach review findings — cortex diet + 7 skill fixes) — 本文件提交后更新为实际 HEAD
**Version**: v0.19.1
**Deck**: 14 skills linked,工作树干净
**Tests**: 本日无 src/ 变更;09-08 handoff 的 EXIT=0 记录仍有效

```bash
git status --short && git log --oneline -3
ls .claude/skills | wc -l          # 应为 14
```

### Completed(按时间序)

1. **cortex desc A/B arena 全跑完**(详见下方"凌晨段"存档节):judge 分 side-b 24.6 vs side-a 22.4,新 desc 是 margin-shaper 不是 capability unlock。归档 `playground/2026-09-09-arena-cortex-desc-ab/`。
2. **push + refresh 链验证端到端**:push 后 `deck refresh` discover(12 behind)→ `--exec` → working set 与 cold pool 同步显示 `name: lythoskill-project-cortex`;lock hash-only diff 同步提交(ef7f9f14)。
3. **SKILL.md name == 目录名对齐**(外部反馈):repo 全量 13/14 原本 OK,只有 cortex 一个错(project-cortex)。修了源文件 + 加 `scripts/adr-check.sh` 提交时 guard(case 路径分发;正路径真树绿、负路径合成 mismatch 咬人,手工 QA 并披露,没跑 arena qa-sweep)。
4. **纯英 sweep**(c3edaf7f,22 文件 +324/-230):skill md 正文全英文;用户聊天短词保留为 trigger hook 但每个加"是什么"gloss(3-5W1H);造词(卖家秀/买家秀/夜有所梦/组卡审美)保留一次加括号注解;引号数据原文保留加类别注记。三条用户中途修正已吸收:(a) 不假定特定操作者身份/语言;(b) 产品文档禁止内部笔记体(dreaming 的"我们听说→田野调查"段重写为非人称);(c) trigger 可留中文但禁止"原话摘抄"备忘录风格。
5. **coach 复审(用户提议,fork agent,166k tokens)12 findings → 全部处理完**:
   - HIGH cortex desc 3,140 > 1,536 截断线 + 正文 599 > 500 行 → desc 压缩 + 正文 433 行(output examples 移到 `references/command-output.md` 113 行;删 3 个 mermaid 状态图,state-machines.md reference 已覆盖转移)
   - scribe 示例表加"合成样本,非特定人物原话"框 + 对话脚本转逗号列表
   - curator when_to_use 1,056 → ~750 字符(原 91% cap)
   - writer 34 词 gloss 黑名单移到 `references/banned-lexicon.md`,正文留一行触发
   - deck 体验 gloss 改 "try it out / UX-test";coach L200 前导空格;dreaming desc 动作先行、诗名留作 hook
   - 已提交 bc2053c7 并推送,build 14 skills + adr-check 全绿

### Key Decisions

- **污染如实记录而非重跑**:S1b 拿空实验 toml 跑 `deck link` wipe 了 side-b working set;judge 确认 doctrine 结论成立,披露在 verdict §4。
- **guard 脚本编辑不走 arena qa-sweep**:改 adr-check.sh 触发敏感性 banner,用合成 mismatch 手工 QA(正负两路径),披露。
- **daily 顶部覆盖不追写**:本文件已两次按此纪律重写。

### Dogfood 使用观察(用户原话,未行动)

> "现在我自己吃狗粮给其他项目用,用最多的场景果然是分享预制deck和cortex和记忆系的技能。其他的如果这些用不起来是轮不到的。"

含义:deck(分享预制 deck)+ cortex + 记忆系(scribe/scribe-weekly/dreaming/onboarding)是**楔子**;其他技能(writer/coach/sober/arena/red-green)是下游,楔子不好用下游免谈。推论:examples/onboarding 应把"预制 deck 分享 + 记忆管线"当一等公民路径呈现。已向用户提议登记 task,等确认。

### Next Steps

1. **writeLock EACCES 修复,需 ADR 选型**——`packages/lythoskill-deck/src/link.ts:156` bare `writeFileSync`,crash 在 reconcile 之后 → lock 与 working set 静默分叉。两套互补设计在桌上(S1a catch+deferred-exit / S1b fail-fast preflight+atomic tmp-rename)。登记卡 **TASK-20260909004700000**。做完信号:ADR accept + 修复 + 回归测试。
2. **probe empty-shell 检测补 TBD 字面量**——已登记 **TASK-20260909010058114**。做完信号:TBD 卡报 empty-shell + dormancy 测试零误报。
3. **arena decision-log 并发修复**——已登记 **TASK-20260909010121918**。做完信号:双写者不互相覆盖 + collect 合并 + protocol 文档同步。
4. **dogfood 楔子观察 → 等用户确认后登记 task**(预制 deck 分享 + 记忆系在 examples/onboarding 一等公民化)。
5. 若重跑 desc A/B:S3 变体要让被截断 ID 的卡**不可 tail-match 找到**(只有 daily 引用)——no-truncation 规则真正应该咬人的条件本次没被测到。

### Temp Artifacts

- `/tmp/arena-desc-ab/` — 运行现场(/tmp 可清,playground 已归档)
- `playground/2026-09-09-arena-cortex-desc-ab/` — 824K 全归档(gitignored)

---

## 凌晨段存档 — cortex desc A/B arena 跑完 + judge verdict
## Session Handoff — cortex desc A/B arena 跑完 + judge verdict

### 0. Verify Current State

**Git**: HEAD = `b0226592` (docs(skills): frontmatter self-description pass across 5 skills) — 本文件提交后更新为实际 HEAD
**Version**: v0.19.1
**Deck**: 14 skills linked,工作树干净(实验全在 /tmp + gitignored playground)
**Tests**: 本 addendum 无代码变更;上一批 EXIT=0 见 09-08 handoff

```bash
git status --short && git log --oneline -3
ls .claude/skills | wc -l          # 应为 14
```

### Completed

1. **cortex desc A/B arena 全跑完**(10/10 cell + judge)。变量 = cortex SKILL.md frontmatter(side-a OLD jargon-dense / side-b NEW glossed + no-truncation rule),5 场景 × 2 侧零上下文 cell。Judge(零上下文 fork)打分:**side-b 24.6 vs side-a 22.4**,side-b 每个配对场景都赢但幅度小——**新 desc 有效,靠"教条承载"而非"术语扫盲"**:S2/S3 接近平局证明 competent agent 本来就能 gloss 找得到的术语;真正拉开的是 S1 register-before-fix(产出可跨 cell 引用的持久 task 卡)、S4 probe-as-canonical、S5 ZK-protocol 字段。desc 是 margin-shaper,不是 capability unlock。归档:`playground/2026-09-09-arena-cortex-desc-ab/`(report.md + judge-verdict.md + reproduce.sh + 双侧产物,824K)。

### Key Decisions

- **污染如实记录而非重跑**:S1b 拿空实验 toml 跑 `deck link`,deny-by-default wipe 了 side-b 的 `.claude/skills/`;后续 cell 靠 backup tar 恢复。Judge 确认每个 side-b cell 都实际读到了自己的 variant,doctrine 结论成立,污染只影响 economy 分和 S4b 的 drift 签名。披露在 verdict §4,不掩饰。
- **run 里挖出的真实产品发现登记成卡,不留在报告里**(见 Next Steps 的 raw ref)。

### Pitfalls

- **arena decision-log 并发 last-writer-wins**:side-a 并发 cell 互相覆盖(只剩 S1a 的 9 条)。protocol 应强制 per-cell log 或原子 read-then-append。已登记 **TASK-20260909010121918**。
- **实验脚手架的空 toml 本身就是地雷**:任何 cell 跑 `deck link` 都会 wipe working set。arena protocol 的 per-cell workdir 设计就是为了防这个——本次为省 scaffold 没做,代价兑现了。

### Next Steps

1. **writeLock EACCES 修复,需 ADR 选型**——两侧独立复现的真实 bug:`packages/lythoskill-deck/src/link.ts:156` bare `writeFileSync`,crash 在 reconcile 之后触发 → lock 与 working set 静默分叉,无 agent-actionable 消息;`writeState()` 同隐患。两套互补设计在桌上:S1a 的 catch + deferred-exit(报告打全再退 1)/ S1b 的 fail-fast preflight + atomic tmp-rename。登记卡 **TASK-20260909004700000**(S1b 已填)。做完信号:ADR accept + 修复 + 回归测试。
2. **probe empty-shell 检测补 TBD 字面量**——卡上写满 "TBD" 但没 `⚠️ PLACEHOLDER_` marker 时 probe 不报 empty shell。已登记 **TASK-20260909010058114**。做完信号:TBD 卡报 empty-shell + dormancy 测试(内容充实的卡零误报)。
3. **arena decision-log 并发修复**——已登记 **TASK-20260909010121918**。做完信号:双写者不互相覆盖 + collect 步合并 + protocol 文档同步。
4. 若重跑 desc A/B:S3 变体要让被截断 ID 的卡**不可 tail-match 找到**(只有 daily 引用)——no-truncation 规则真正应该咬人的条件本次没被测到。

### Temp Artifacts

- `/tmp/arena-desc-ab/` — 运行现场(与 playground 归档同内容,/tmp 可清)
