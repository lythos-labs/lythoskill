# Daily — 2026-09-09

> 本会话从 2026-09-08 深夜跨午夜延续。08 日的发布 + scribe 约束 + ZK 狗粮见 `daily/2026-09-08.md`(该文件已被 ZK reviewer 7 findings 修过,可信)。本文件只记 09-09 凌晨的 arena A/B。

## Session Handoff — cortex desc A/B arena 跑完 + judge verdict

### 0. Verify Current State

**Git**: HEAD = `b0226592` (docs(skills): frontmatter self-description pass across 5 skills) — 本文件提交后更新为实际 HEAD
**Version**: v0.19.1
**Deck**: 14 skills linked,工作树干净(实验全在 /tmp + gitignored playground)
**Tests**: 本 addendum 无代码变更;上一批 EXIT=0 见 09-08 handoff

```bash
git status --short && git log --oneline -3
ls .claude/skills | wc -l          # 应为 14
```

### Completed

1. **cortex desc A/B arena 全跑完**(10/10 cell + judge)。变量 = cortex SKILL.md frontmatter(side-a OLD jargon-dense / side-b NEW glossed + no-truncation rule),5 场景 × 2 侧零上下文 cell。Judge(零上下文 fork)打分:**side-b 24.6 vs side-a 22.4**,side-b 每个配对场景都赢但幅度小——**新 desc 有效,靠"教条承载"而非"术语扫盲"**:S2/S3 接近平局证明 competent agent 本来就能 gloss 找得到的术语;真正拉开的是 S1 register-before-fix(产出可跨 cell 引用的持久 task 卡)、S4 probe-as-canonical、S5 ZK-protocol 字段。desc 是 margin-shaper,不是 capability unlock。归档:`playground/2026-09-09-arena-cortex-desc-ab/`(report.md + judge-verdict.md + reproduce.sh + 双侧产物,824K)。

### Key Decisions

- **污染如实记录而非重跑**:S1b 拿空实验 toml 跑 `deck link`,deny-by-default wipe 了 side-b 的 `.claude/skills/`;后续 cell 靠 backup tar 恢复。Judge 确认每个 side-b cell 都实际读到了自己的 variant,doctrine 结论成立,污染只影响 economy 分和 S4b 的 drift 签名。披露在 verdict §4,不掩饰。
- **run 里挖出的真实产品发现登记成卡,不留在报告里**(见 Next Steps 的 raw ref)。

### Pitfalls

- **arena decision-log 并发 last-writer-wins**:side-a 并发 cell 互相覆盖(只剩 S1a 的 9 条)。protocol 应强制 per-cell log 或原子 read-then-append。已登记 **TASK-20260909010121918**。
- **实验脚手架的空 toml 本身就是地雷**:任何 cell 跑 `deck link` 都会 wipe working set。arena protocol 的 per-cell workdir 设计就是为了防这个——本次为省 scaffold 没做,代价兑现了。

### Next Steps

1. **writeLock EACCES 修复,需 ADR 选型**——两侧独立复现的真实 bug:`packages/lythoskill-deck/src/link.ts:156` bare `writeFileSync`,crash 在 reconcile 之后触发 → lock 与 working set 静默分叉,无 agent-actionable 消息;`writeState()` 同隐患。两套互补设计在桌上:S1a 的 catch + deferred-exit(报告打全再退 1)/ S1b 的 fail-fast preflight + atomic tmp-rename。登记卡 **TASK-20260909004700000**(S1b 已填)。做完信号:ADR accept + 修复 + 回归测试。
2. **probe empty-shell 检测补 TBD 字面量**——卡上写满 "TBD" 但没 `⚠️ PLACEHOLDER_` marker 时 probe 不报 empty shell。已登记 **TASK-20260909010058114**。做完信号:TBD 卡报 empty-shell + dormancy 测试(内容充实的卡零误报)。
3. **arena decision-log 并发修复**——已登记 **TASK-20260909010121918**。做完信号:双写者不互相覆盖 + collect 步合并 + protocol 文档同步。
4. 若重跑 desc A/B:S3 变体要让被截断 ID 的卡**不可 tail-match 找到**(只有 daily 引用)——no-truncation 规则真正应该咬人的条件本次没被测到。

### Temp Artifacts

- `/tmp/arena-desc-ab/` — 运行现场(与 playground 归档同内容,/tmp 可清)
