# 2026-05-01

## Session Handoff ⭐（下一个 agent 先看这里）

```bash
# 1. 查看当前 working tree 状态
git status --short

# 2. 查看最近 commit
git log --oneline -3

# 3. 检查 cortex 状态
bun packages/lythoskill-project-cortex/src/cli.ts list
```

### 项目身份

- **项目名称**: lythoskill — thin-skill monorepo scaffolding tool
- **技术栈**: Bun + TypeScript + pnpm workspaces + ESM-only
- **当前分支**: `main`
- **最近 commit**: （与 2026-04-30 相同，本次为纯审计 session，无代码修改）

### Ground Truth State

| 文件/状态 | 说明 |
|-----------|------|
| working tree | **有修改**（新增 ADR + task，daily 日志，旧 ADR 状态更新） |
| ADR 总数 | 16 个（15 accepted + 1 部分作废） |
| 新 ADR | ADR-20260501090811296 — 追认 pre-commit hook 为正式方案，作废旧 ADR 的 CI 部分 |
| 新 Task | TASK-20260501090806543 — 修复 align.ts 9 处 require() |

### 关键上下文（文件探索无法恢复）

1. **本次 session 核心产出：ADR 全量一致性审计**
   - 按时间顺序读取全部 14 个 accepted ADR + 1 个 proposed ADR
   - 发现 2 个需要修正的 governance 问题：
     - `align.ts` 有 9 处 `require()` 违反 ESM-only（已创建 task）
     - 旧 ADR 承诺了 CI 但实际用 husky（已写新 ADR 追认）

2. **新 ADR-20260501090811296 的核心作用**
   - 正式作废 ADR-20260423124812645 中的 CI（方案 G）部分
   - pre-commit hook 从"过渡期临时方案"升格为正式方案
   - 旧 ADR 的"提交 skills/ 到 Git"部分仍然有效

3. **combo / evaluator 的真实状态**
   - ADR 已 accepted，概念已同意
   - 但 epic 尚未排期实现（优先级不足，等核心组件稳定后再启动）
   - 这不是"不一致"，而是"已规划但未排期"

---

## ADR 一致性审计日志

> 审计方法：按 ADR 时间戳升序读取，新决定覆盖旧决定，逐条核对当前系统状态。

### 完全一致的 ADR（✅）

| ADR | 决定摘要 | 验证结果 |
|-----|---------|---------|
| ADR-20260423101938000 | Thin Skill Pattern 三层分离 | ✅ `packages/*/skill/` → `skills/*/` 结构完整 |
| ADR-20260423130348396 | deck 治理移植，deck_ 前缀 | ✅ skill-deck.toml + deck_ frontmatter 已落地 |
| ADR-20260423182606313 | 模板变量替换 + CLI Help 委托 | ✅ build.ts 有 `{{PACKAGE_*}}` 替换逻辑 |
| ADR-20260423191001406 | npm 包名改为 `@lythos/skill-*` | ✅ `@lythos/skill-deck` / `@lythos/skill-creator` |
| ADR-20260424000744041 | curator 产出属用户环境 | ✅ 无 CATALOG.md/REGISTRY.json 在项目根目录 |
| ADR-20260424013849984 | 防腐层定位 + Go-mod 冷池 | ✅ README 明确两层价值主张，不做商店 |
| ADR-20260424113352614 | scribe 保持独立 | ✅ skills/ 下 project-scribe 独立存在 |
| ADR-20260424113917838 | red-green-release heredoc patch | ✅ heredoc 格式已落地；"版本号改时间戳"指的是 **patch 文件** 而非 npm 版本 |
| ADR-20260424120936541 | player-deck 分离 | ✅ playground/ 有 `player-*.toml` |
| ADR-20260424125637347 | handoff 改为 daily-first | ✅ daily/ 下为日期文件，无 HANDOFF.md |

### 已追认/修正的 ADR（📝）

| ADR | 问题 | 处理方式 |
|-----|------|---------|
| ADR-20260423124812645 | 承诺了 CI 但实际用 husky | 📝 新 ADR-20260501090811296 追认：pre-commit hook 成为正式方案，CI 部分作废 |

### 已接受但尚未排期实现的 ADR（⏳）

> 概念已同意，epic 已创建或待创建，但优先级不足，待核心组件稳定后启动。

| ADR | 概念状态 | Epic/Task 状态 | 备注 |
|-----|---------|----------------|------|
| ADR-20260424114401090 | combo skill 混合模式 ✅ 同意 | 无 epic/task | 依赖生态成熟度，等独立 skill 足够多后再启动 |
| ADR-20260424115621494 | evaluator swarm 自适应并发 ✅ 同意 | Epic 已创建（EPIC-20260429234732479）但无 task 排期 | 前瞻性设计，等 arena/curator 稳定后再实现 |

### 明确违规（❌）

| ADR | 问题 | 跟踪 |
|-----|------|------|
| ADR-20260423101950000 | `packages/lythoskill-creator/src/align.ts` 有 **9 处** `require()` | ✅ Task 已创建：TASK-20260501090806543 |

### 状态漂移（⚠️）

| ADR | 问题 | 说明 |
|-----|------|------|
| ADR-20260430174746744 | 状态仍为 `proposed` 且内容为空 | deck add 已实现（`packages/lythoskill-deck/src/add.ts` + SKILL.md 文档）。需要补全 ADR 并改为 accepted，或召开 retrospective 追认。 |

---

## 统计摘要

| 分类 | 数量 | 说明 |
|------|------|------|
| 完全一致 | 10 | 概念已落地，系统状态匹配 |
| 已追认修正 | 1 | CI → husky 决策变更已记录为新 ADR |
| 已接受但未排期 | 2 | combo / evaluator，概念同意，等优先级 |
| 明确违规 | 1 | align.ts require()，已创建 task |
| 状态漂移 | 1 | deck add ADR 为空但实现已存在 |

**最紧急修复项**：TASK-20260501090806543 — 将 `packages/lythoskill-creator/src/align.ts` 中的 9 处 `require()` 改为 ESM `import`。

---

## Next

1. **(ready) 提交本次 session 所有修改** — 新 ADR、旧 ADR 更新、task、daily 日志
2. **(ready) 修复 `align.ts` ESM 违规** — 9 处 `require()` → `import`
3. **(backlog) 补全 ADR-20260430174746744** — deck add 已实现，ADR 需追认
4. **(backlog) combo / evaluator 排期** — 等核心组件稳定后再启动

---

## Session Handoff (late afternoon) — Hermes 源码审计 + ADR 术语评估

### 0. Verify Current State
```bash
git diff HEAD --stat
git status --short
git log --oneline -3
```

### Ground Truth State
- **Branch**: main
- **HEAD**: （与上午 handoff 相同，本次为调研 + 文档产出，无代码修改）
- **Uncommitted**:
  - `cortex/wiki/03-lessons/hermes-agent-skill-evolution-and-deck-governance.md`（已更新，加入 Hermes 源码一手发现）
  - `cortex/adr/01-proposed/ADR-20260501091724816-*.md`（新 ADR：cold pool → skill_library 术语评估）
  - `playground/hermes-deck.example.toml`（新文件：面向 Hermes 的 deck 配置示例）
- **Untracked**: 无新增 untracked（均在 git 跟踪目录下）

### What Happened This Session
- **Hermes Agent 源码审计**（`/tmp/hermes-agent-shallow`，独立目录，未污染本 repo）
  - 读取 `tools/skill_usage.py`：发现 Hermes 内置完整的 curator 生命周期管理（active/stale/archived、pinned、usage tracking、.archive/ 目录）
  - 读取 `tools/skills_tool.py`：Hermes skill 采用 agentskills.io 标准格式，支持 progressive disclosure
  - 读取 `tests/tools/test_skill_usage.py`：确认 skill 来源分层 —— bundled / hub-installed / agent-created，curator 只操作最后一类
  - 遍历 `skills/` 和 `optional-skills/`：共 ~180 个内置 SKILL.md，按领域嵌套布局（`<category>/<skill>/SKILL.md`）

- **ADR 创建**：通过 cortex CLI 创建 ADR-20260501091724816，评估 cold pool → skill_library 术语变更
  - 结论：采用方案 C（引入分层术语，cold_pool 降级为 implementation detail）
  - `skill-deck.toml` 的 `cold_pool` 字段未来重命名为 `library`
  - 精确对齐 Hermes 的 "skill library" 概念，但保留 lythoskill 独特的物理隔离设计

- **Deck Example 编写**：`playground/hermes-deck.example.toml`
  - 演示如何将 `~/.hermes/skills/` 直接作为 deck 的 library
  - 包含 transient 管理 Hermes auto-created skills 的最佳实践
  - 明确 curator 与 deck 的分层协作边界

- **Wiki 报告更新**：在报告中加入「附录：Hermes 源码一手发现」章节
  - A.1 Skill 目录结构双层设计
  - A.2 Curator 生命周期管理详解
  - A.3 Hermes ↔ lythoskill 精确概念对照表

### Pitfalls
- **不要把 Hermes 的 `~/.hermes/skills/` 直接当 working set**：该目录包含 180+ skills，全部加载会瞬间撑爆 context window。必须通过 deck 选取子集。
- **Hermes 的 `optional-skills/` 是另一个独立池**：deck 目前只支持单个 cold_pool/library。如果要同时引用 `skills/` 和 `optional-skills/`，需要创建两个 deck 文件，或手动 symlink。
- **Agent-created skill 的路径是扁平的**：与内置 skill 的 `<category>/<skill>/` 嵌套不同，Hermes 自动创建的 skill 直接放在 `~/.hermes/skills/<name>/`。deck.toml 中的 path 引用需要区分这两种布局。

### Next Steps (prioritized)
1. **(ready) 提交本次 session 所有文档产出** — wiki 报告、ADR、deck example、wiki INDEX 更新
2. **(backlog) ADR 推进**：若用户认可 ADR 方案 C，实施 deprecation shim（`cold_pool` → `library` 别名）
3. **(backlog) curator 功能设计**：参考 Hermes `skill_usage.py`，设计 lythoskill-curator 的使用统计与生命周期管理
4. **(backlog) 双 library 支持**：评估 deck 是否应支持多个 cold_pool/library 路径（同时引用 Hermes skills/ 和 optional-skills/）

### Warnings for Next Agent
- 本次 session **无任何代码修改**，全部产出为文档和配置示例。
- `/tmp/hermes-agent-shallow/` 是临时 shallow clone，可随时删除，不纳入 git。
- ADR 当前状态为 `proposed`，尚未 accepted。若需要修改术语决策，直接在 `cortex/adr/01-proposed/` 下编辑即可。

---

## Session Handoff (afternoon) — Hermes Agent 自主进化调研

### 0. Verify Current State
```bash
git diff HEAD --stat
git status --short
git log --oneline -3
```

### Ground Truth State
- **Branch**: main
- **HEAD**: （与上午 handoff 相同，本次为纯调研 session，无代码修改）
- **Uncommitted**: 新增 wiki 报告 `cortex/wiki/03-lessons/hermes-agent-skill-evolution-and-deck-governance.md`
- **Untracked**: 无

### What Happened This Session
- **Web 搜索**: Hermes Agent（Nous Research）的自主进化机制、GEPA 引擎、社区 issues 中的 skill 爆炸/内存爆炸/上下文淹没等实证
- **第一性推演**: 从架构不变量出发，推演出自主进化必然引发的 6 个系统性痛点（产率危机、context drowning 2.0、不可复现性、隐式依赖脆弱性、Harness 逃逸、eval decay）
- **对接分析**: 论证 lythoskill-deck 的现有机制（cold pool、deny-by-default、lock 文件、max_cards）恰好构成应对此危机的基础设施
- **产出**: wiki 调研报告一篇，提出"动态 Deck 治理"和"进化可观测性"两个前瞻性 Epic

### Pitfalls
- **不要只搜新闻稿**：Hermes 的 GitHub issues（#21965 内存爆炸、#38719 子 agent skill 调用、#336 Darwinian Evolver）比博客文章更能揭示真实痛点
- **不要低估自主进化的速度**：GEPA 每次运行 $2-10，纯 API 调用，一个重度用户一天可以产生 50+ 候选变体 —— 治理必须自动化，人类审核不可行

### Next Steps (prioritized)
1. **(ready) 提交 wiki 报告** — `cortex/wiki/03-lessons/hermes-agent-skill-evolution-and-deck-governance.md`
2. **(backlog) 更新 wiki INDEX.md** — 将新报告加入索引
3. **(backlog) 提炼 Epic** — 若用户认可，可将"动态 Deck 治理"和"进化可观测性"正式写入 cortex/epics/
4. **(backlog) 与 arena/curator 对接** — 评估 curator 的 eval 基础设施是否可复用为 GEPA 的 gatekeeper

### Warnings for Next Agent
- 本次 session **无代码修改**，只有文档产出。不要预期有任何测试需要跑。
- 报告中的"第一性推演"部分不是已观察到的现象，而是从架构出发的必然推论。若需要引用，请区分"已证实的社区问题"和"推演的理论痛点"。

---

## Session Handoff (evening) — ADR audit + onboarding UX fixes committed

### 0. Verify Current State
```bash
git log --oneline -3
git status --short
```

### Ground Truth State
- **Branch**: main
- **HEAD**: `5d2bdb7` — docs: onboarding friction reduction
- **Uncommitted**（2026-04-30 session 遗留）:
  - `packages/lythoskill-creator/src/align.ts`（ESM 违规，已创建 TASK-20260501090806543）
  - `packages/lythoskill-deck/skill/SKILL.md` + `references/toml-format.md`（add 命令文档）
  - `packages/lythoskill-creator/skill/SKILL.md`（when_to_use 补充）
  - `packages/lythoskill-project-scribe/skill/SKILL.md`（handoff 格式更新）
  - `packages/lythoskill-project-cortex/skill/references/session-handoff.md`
  - `cortex/wiki/03-lessons/hermes-agent-skill-evolution-and-deck-governance.md`（新 wiki）
  - `cortex/adr/01-proposed/ADR-20260501091724816-*.md`（Hermes 术语 ADR）
  - `.husky/post-commit`, `.husky/pre-commit`（hook 修复）
  - `.gitignore`, `README.zh.md`, `daily/HANDOFF.md`（删除）, `daily/README.md`
  - `scripts/adr-check.sh`（新脚本）
- **Untracked**: 无

### What Happened This Session
- **ADR 全量一致性审计**：读取 14 accepted + 1 proposed ADR，按时间顺序覆盖核对系统状态
- **发现 2 个 governance 问题**：
  1. `align.ts` 9 处 `require()` 违反 ESM-only → 已创建 task
  2. 旧 ADR 承诺 CI 但实际用 husky → 已写新 ADR-20260501090811296 追认
- **Onboarding subagent 测试**：全新 agent 模拟上手，评分 6.5/10
- **文档修复已提交**：
  - README.md: Quick Start 保持用户 first，Development 章节扩充
  - AGENTS.md: HANDOFF-TEMPLATE 路径补全 + troubleshooting hint
  - deck link 输出: `8/10 skills` → `8 skill(s) linked (max_cards: 10)`
- **关闭 2 个误报 task**：init 副作用（SKILL.md 已覆盖）、status CLI（脚本已存在）

### Pitfalls
- **不要把 subagent 测试报告当真理**：subagent 会自己"幻想"问题（如 status CLI、init 副作用），需要人工复核文档是否已覆盖
- **combo / evaluator 未实现 ≠ 不一致**：ADR 已 accepted，但 epic 未排期，这是正常的"概念同意、实现待定"
- **daily/2026-05-01.md 被多 session 复用**：文件已有 214 行（含 Hermes 调研 handoff），追加时请直接写到末尾

### Next Steps (prioritized)
1. **(ready) 提交 2026-04-30 session 遗留修改** — align.ts 除外（等 task 修复）
2. **(backlog) 修复 align.ts ESM 违规** — TASK-20260501090806543
3. **(backlog) 补全 ADR-20260430174746744** — deck add 已存在，ADR 为空需追认
4. **(backlog) 重新运行 onboarding subagent 测试** — 验证本次修复是否把评分从 6.5 提升到 8.5+

### Warnings for Next Agent
- working tree 中有大量 2026-04-30 遗留修改未提交。若需要 clean state，先 `git status` 确认。
- `skills/.lythoskill-curator/` 已加入 `.gitignore`，但 commit hook 已删除已跟踪的 REGISTRY.json/catalog.db。
