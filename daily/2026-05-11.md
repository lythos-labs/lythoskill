---
type: handoff
created_at: 2026-05-11T20:00:00
git_branch: main
git_commit: <pending — commit after version bump>
---

# Handoff: v0.9.51 — namespace alignment, Claude SDK, deck variant coverage

> 地面真值: README multi-CLI对齐 · curator路径 ~/.agents/lythoskill/curator/ · arena players.json ~/.agents/lythoskill/arena/ · Claude SDK验证(ANTHROPIC_API_KEY,非which claude) · 2个deck add bug修复(github: prefix+@skill, dir≠name fallback) · ADR namespace consolidation · deck variant 32场景覆盖测试 · onboarding cortex ls增强

## 0. Verify Current State
```bash
git log --oneline -5
# Expected: top commit = chore(release): v0.9.51

git status --short
# Expected: clean (after commit)
```

## 1. What Was Done (May 11 afternoon, → v0.9.51)

### Bugs fixed (deck add)
- `github:` prefix dropped `@skill` extraction → fixed in normalizeSkillsSh (add.ts:138-147)
- `findSkillByName` dir-name fallback → 4/7 Vercel skills have dir ≠ frontmatter name (add.ts:65-84)

### Path namespace consolidation
- Arena: `~/.agents/lythoskill/arena/players.json`
- Curator: `~/.agents/lythoskill/curator/` (REGISTRY.json + catalog.db + additions.jsonl)
- CLI default output changed from `{pool}/.lythoskill-curator/`
- Old paths kept as backward-compat fallback with `legacy` tag in CLI code
- ADR-20260511210000000 (proposed): resolves vacillation in ADR-20260424000744041

### Arena SKILL.md — Claude SDK + multi-player alignment
- Claude: SDK not CLI — verify via `echo $ANTHROPIC_API_KEY`, no `which claude` or `claude -p`
- DeepSeek added to auto-detection and priority table
- Install commands updated for all 4 players
- "Headless reliability" → "Headless mode" (less judgmental)

### README
- Achievement language: dropped "Zero human-written code" and stale test count
- Test count: 215 → 649 (matches CI badge)
- Multi-CLI working_set paths listed for all 6 agents
- Stale version `0.9.19` reference removed
- Kimi-only Arena prerequisite → multi-player
- `## Development## Development` typo fixed

### Other
- Onboarding SKILL.md: cortex `ls -lt` added to degraded exploration (adr/wiki/epics/tasks)
- Wiki: deck add variant coverage (32 parsing + 4 CLI, 35/36 pass)
- REGISTRY.json role confirmed: human-readable snapshot, not consumed by any automation; catalog.db is the real query engine

## 2. Files Touched (non-version-bump)
- `packages/lythoskill-deck/src/add.ts` — 2 bug fixes
- `packages/lythoskill-curator/src/cli.ts` — output dir + additions.jsonl + fallback paths
- `packages/lythoskill-arena/skill/SKILL.md` — player discovery SDK/claude alignment
- `packages/lythoskill-curator/skill/SKILL.md` — default output path
- `skills/lythoskill-arena/SKILL.md` — same (built copy)
- `skills/lythoskill-curator/SKILL.md` — same (built copy)
- `packages/lythoskill-project-onboarding/skill/SKILL.md` — cortex ls in degraded exploration
- `.claude/skills/lythoskill-project-onboarding/SKILL.md` — same
- `README.md` — multi-CLI, test count, achievement language, Arena prereq
- `cortex/wiki/02-research/2026-05-11-deck-add-variant-coverage.md` — new
- `cortex/adr/01-proposed/ADR-20260511210000000-...` — new namespace consolidation ADR
- `playground/deck-variant-test-2026-05-11.md` — test report

## 3. Key Decisions
- `~/.agents/lythoskill/` is canonical personal config namespace (curator + arena live here)
- Cold pool `.cold-pool-meta.db` stays inside cold pool (operational metadata)
- Curator output = personal environment scan, not cold pool metadata
- REGISTRY.json kept as snapshot; catalog.db is the query engine; cold pool skills table is operational ground truth

## 4. Next Session
- npm publish v0.9.51
- Agent BDD: run bunx scenarios in playground to validate user-facing behavior
- If all pass → bump to 0.10.0
- If bugs found → fix → retest → bump
- Triage 15 proposed ADRs in `cortex/adr/01-proposed/`
- Arena: `players detect` CLI command still not implemented (SKILL.md describes aspirational behavior)

---

# Handoff: v0.9.50 — skills.sh syntax sugar + @skill discovery + #ref support + Codex adapter + source URL

> 地面真值: skills.sh syntax sugar (owner/repo → normalizeSkillsSh → FQ locator, 17 top skills parse OK) · @skill post-clone name discovery (findSkillByName, precise FQ path in deck.toml) · #ref suffix support (parseLocator + normalize + executeFetchPlan checkout) · git option injection prevention (ref starts with - rejected) · source URL field (path + source 双字段) · Codex adapter 首次上线 npm (@lythos/agent-adapter-codex@0.9.50) · SQL parser (regex→node-sql-parser) · QA sweep on syntax sugar code (3 new injection points fixed) · SKILL.md pushiness (arena/curator/cortex) · ADR×3 + wiki×4 · examples/decks 翻修 · v0.9.48→0.9.49→0.9.50 发布 (13 packages, 3 bumps) · 88+228 tests 全过 · 15+ commits push

## 0. Current State

```bash
git log --oneline -5
# 81d00ea feat(deck): source URL field in deck.toml
# 0f446bb feat(deck): add web URL to add success output
# a85990e docs(adr): ADR — skills.sh syntax sugar
# 6d151e8 docs(task): TASK — deck add multi-skill discovery warning
# 3da2241 fix(deck): owner/repo@skill normalizes to repo level

git status --short
# (clean)
```

## 1. What Was Done (May 11)

### Codex adapter
- `@lythos/agent-adapter-codex` 包创建 (8 tests, 0 fail)
- `buildCodexCommand()`: array args, Bun.file(stdin) — 零 shell 注入
- `parseCodexJsonl()`: 解析 item.completed + thread.turn.completed
- 注册到 arena auto-import
- Smoke test: CLI 链路通 (OpenRouter 地区限制，非 adapter 问题)

### skills.sh syntax sugar
- `normalizeSkillsSh()`: owner/repo@skill → FQ locator 边界归一化
- 17 个 skills.sh top skill 全部解析通过
- `@skill` 归一化到 repo 级别 (runtime 发现，不假设 skills/ 路径)
- `source` URL 字段: deck.toml 双字段 (path + source)
- deck add 成功时输出 GitHub web URL
- ADR: boundary normalization pattern (Hono adapter 类比)

### SKILL.md pushiness
- arena: player discovery (which check), lock file location, deck restore 强调
- curator: smart-agent-dumb-tool 重构，L1/L2/L3 信任模型，cold-pool CLI 交叉引用
- cortex: "register before fix" 触发器 (bug found → task → fix)

### SQL parser fix
- isReadOnlyQuery: regex → node-sql-parser (AST 解析)
- 修复: 'update-docs' 不再被误判为 UPDATE SQL
- guard.ts 重构，移除手搓 regex

### Site builder deck
- examples/decks/site-builder.toml (find-skills + frontend-design + theme-factory + vitepress + mermaid)
- find-skills → curator add → curator scan → curator query → deck add 全流程验证

## 2. Active Epics/Tasks

- TASK-20260510234339990: P2 sweep (29 remaining items, backlog)
- TASK-20260511091105701: regex→parser migration (backlog)
- TASK-20260511093956018: deck add multi-skill discovery warning (backlog)

## 3. Key Decisions

- Codex: CLI wrapper pattern, 等 smoke test 通过后再正式启用
- skills.sh: 语法糖在边界，内部 FQ locator 不变
- 发现层归 skills.sh，治理层归 lythoskill — 互补不替代
- 路径安全范式已收敛: 四个 guard 模块 + Vercel sanitizeSubpath 都是同一套路

## 4. Pending

- Codex smoke test (需要可用 provider)
- Site builder deck → arena 验证
- Examples/decks 翻修 (批量加 [deck] header)
- Cortex BDD CI 稳定性 (CI Safety Rule 已文档化)
