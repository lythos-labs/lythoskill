---
type: handoff
created_at: 2026-05-11T01:00:00
git_branch: main
git_commit: 81d00ea
---

# Handoff: v0.9.49-dev — Codex adapter + skills.sh syntax sugar + arena/cortex/curator SKILL.md pushiness + site-builder deck

> 地面真值: Codex adapter 实现 (codex exec CLI wrapper, injection-safe) · skills.sh syntax sugar (owner/repo@skill → normalizeSkillsSh → FQ locator) · deck add 自动生成 source URL · arena SKILL.md player discovery + lock file guidance · curator SKILL.md smart-agent-dumb-tool 重构 · cortex SKILL.md 触发器加强 (register before fix) · SQL parser (node-sql-parser 替换 regex) · guard.ts 重构 · site-builder side deck (find-skills → curator → deck add 全流程) · ADR×2 (skills.sh syntax sugar + deck git-only) · wiki×3 (discovery vs governance, skills.sh interop, Hono adapter analogy) · 480 tests 全过 · 10 commits push

## 0. Current State

```bash
git log --oneline -5
# 81d00ea feat(deck): source URL field in deck.toml
# 0f446bb feat(deck): add web URL to add success output
# a85990e docs(adr): ADR — skills.sh syntax sugar
# 6d151e8 docs(task): TASK — deck add multi-skill discovery warning
# 3da2241 fix(deck): owner/repo@skill normalizes to repo level

git status --short
# (clean)
```

## 1. What Was Done (May 11)

### Codex adapter
- `@lythos/agent-adapter-codex` 包创建 (8 tests, 0 fail)
- `buildCodexCommand()`: array args, Bun.file(stdin) — 零 shell 注入
- `parseCodexJsonl()`: 解析 item.completed + thread.turn.completed
- 注册到 arena auto-import
- Smoke test: CLI 链路通 (OpenRouter 地区限制，非 adapter 问题)

### skills.sh syntax sugar
- `normalizeSkillsSh()`: owner/repo@skill → FQ locator 边界归一化
- 17 个 skills.sh top skill 全部解析通过
- `@skill` 归一化到 repo 级别 (runtime 发现，不假设 skills/ 路径)
- `source` URL 字段: deck.toml 双字段 (path + source)
- deck add 成功时输出 GitHub web URL
- ADR: boundary normalization pattern (Hono adapter 类比)

### SKILL.md pushiness
- arena: player discovery (which check), lock file location, deck restore 强调
- curator: smart-agent-dumb-tool 重构，L1/L2/L3 信任模型，cold-pool CLI 交叉引用
- cortex: "register before fix" 触发器 (bug found → task → fix)

### SQL parser fix
- isReadOnlyQuery: regex → node-sql-parser (AST 解析)
- 修复: 'update-docs' 不再被误判为 UPDATE SQL
- guard.ts 重构，移除手搓 regex

### Site builder deck
- examples/decks/site-builder.toml (find-skills + frontend-design + theme-factory + vitepress + mermaid)
- find-skills → curator add → curator scan → curator query → deck add 全流程验证

## 2. Active Epics/Tasks

- TASK-20260510234339990: P2 sweep (29 remaining items, backlog)
- TASK-20260511091105701: regex→parser migration (backlog)
- TASK-20260511093956018: deck add multi-skill discovery warning (backlog)

## 3. Key Decisions

- Codex: CLI wrapper pattern, 等 smoke test 通过后再正式启用
- skills.sh: 语法糖在边界，内部 FQ locator 不变
- 发现层归 skills.sh，治理层归 lythoskill — 互补不替代
- 路径安全范式已收敛: 四个 guard 模块 + Vercel sanitizeSubpath 都是同一套路

## 4. Pending

- Codex smoke test (需要可用 provider)
- Site builder deck → arena 验证
- Examples/decks 翻修 (批量加 [deck] header)
- Cortex BDD CI 稳定性 (CI Safety Rule 已文档化)
