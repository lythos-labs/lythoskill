# 2026-05-03

## Session Handoff ⭐(下一个 agent 先看这里)

```bash
# 1. 验证 working tree 状态
git status --short

# 2. 验证最近 commit
git log --oneline -5

# 3. 验证 HEAD 与 handoff 一致
git diff b2ae87c --stat

# 4. cortex 状态一致性检查
bun packages/lythoskill-project-cortex/src/cli.ts probe 2>/dev/null

# 5. 验证 align 全绿
bun packages/lythoskill-creator/src/cli.ts align
```

### 项目身份

- **项目**: lythoskill — thin-skill monorepo scaffolding + cortex governance OS
- **技术栈**: Bun + TypeScript + Bun workspaces + ESM-only
- **当前分支**: `main`
- **最近 commit**: `b2ae87c` — docs(daily): debt epic cleanup completion + align governance pattern
- **当前 monorepo 版本**: `0.8.0`

### Ground Truth State

| 文件/状态 | 说明 |
|-----------|------|
| working tree | **干净** — daily 文件已入库 |
| `main` vs `origin/main` | **同步** |
| `skills` branch | 由 pre-commit hook 自动同步 |
| `.npm-access` / `.github-token` | 都在 root,gitignored,未碰 |
| 自动 memory | 本 session 新增 4 条(见下) |
| pnpm 残留 | **已清除** — 无 `pnpm-lock.yaml`,无 `pnpm-workspace.yaml` |
| 根 runtime deps | **已清除** — 根 `package.json` 无 `dependencies` |
| align 状态 | **47 passed, 0 failed** |

### 本 Session 做了什么(三段工作)

#### 1. 跨 agent / 跨压缩的 onboarding 加固(核心交付)

用户痛点:**之前 Claude 失忆后改坏了 git remote URL**。修法是在三层冗余上同时镜像 guardrail:

- **`AGENTS.md`**: 顶部加 `⚠️` callout + 新增 `## Release & Auth Workflow` 整段(认证状态表 + lock-step bump pipeline + skill 产品识别 + SKILL.md 模板规则 + commit policy + publish 流程 + handoff 路径)。SSOT,所有 CLI 都读。
- **`CLAUDE.md`**: 整体重写,顶部 compaction-safe 提醒,显式 deprecate `daily/HANDOFF.md`。Claude Code 每次 session 加载,跨压缩存活。
- **auto-memory**: 新增 `feedback_compaction_safe_doc_visibility.md` —— 把"guardrail 类规则要三层镜像,光写 ADR 不够"作为 feedback rule 锁住。

ADR 更新:`ADR-20260502234833756`(skill 包识别)从 proposed 移到 accepted,Status History 补上 accepted 行。

#### 2. 第一次端到端跑通 `bump`(0.7.2 → 0.7.3)

实跑 `bun packages/lythoskill-creator/src/cli.ts bump patch` 验证 ADR-20260502233119561 + ADR-20260502234833756 完整工作:

- ✅ root + 7 个 packages 同步 → `0.7.3`
- ✅ **`lythoskill-test-utils` `0.7.0` → `0.7.3`** 一次追上(verifies "lock-step is monorepo-wide" 这条)
- ✅ 6 个 skill 产品 SKILL.md 重新渲染 `{{PACKAGE_VERSION}}` 占位符,模板没被烧死
- ✅ `skills/lythoskill-creator/references/COMMANDS.md` 自动带上新 `bump` 帮助文本
- ⚠️ align 顺手给 test-utils 加了 `license: "MIT"` + `publishConfig.access: "public"` —— **不是 bug**: 用户明确 test-utils 也能发布(MIT 开源),这两个字段是预备字段不是矛盾

提交分两个 commit:
- `24c985e chore(release): v0.7.3` — 15 个 release artifacts(8 个 package.json + 6 个 SKILL.md + 1 个 COMMANDS.md)
- `0d39bb7 chore(governance+docs): ...` — 6 个文档/治理改动(AGENTS / CLAUDE / INDEX / bump.ts existsSync guard / ADR 接受 / TASK 移到 04-completed)

两次 commit 都过了 pre-commit ADR 合规检查。

#### 3. 设计范式捕获:test-utils = agent+skill+CLI 控制回路验证

Session 后段用户思考链(零散 → 收敛):
1. test-utils 本质是内置 BDD,不用 Cucumber 是因为 **LLM 训练里 BDD 语法 (Given/When/Then) 见过太多**,自研轻量 > 复杂框架;
2. agent+skill+CLI 是一个**控制回路**(读 SKILL.md → 调 CLI → 看结果 → 继续),BDD scenario 就是这个回路的 checkpoint;
3. deck-link 是第一个落地的 BDD 覆盖场景;
4. **cortex 是下一个自然目标 —— 因为 cortex 任务流转本质是 FSM,FSM 的边集天然映射到 BDD 场景集**;
5. test-utils 有可能进一步孵化成可发布基建(`private: true` 但 `license` + `publishConfig.access: public` 已就位,翻一个字段就能发)。

存为 memory:`project_test_utils_bdd_control_loop.md`。

### 关键决策

| 决策 | 选项 | 选择 | 理由 |
|------|------|------|------|
| onboarding 加固落点 | 仅 ADR / 仅 AGENTS / 三层镜像 | **三层(AGENTS+CLAUDE+memory)** | ADR-only 历史上反复失效;CLAUDE.md 跨压缩存活;memory 自动加载 |
| commit 分组 | 一个大 commit / 三个 / 两个 | **两个**(release artifacts vs governance+docs) | 语义清晰,bisect 友好 |
| test-utils 是否要发布 | 留 private / 准备发布 | **保持 `private: true` 但字段就位** | 用户:"我无所谓的,反正开源的";`publishConfig.access: public` 提前到位 |
| cortex BDD 覆盖 | 现在做 / 留到下次 | **留到下次** | session 已经长,先收尾;但已识别为下一目标 |

### 踩过的坑

1. **误判 align 添加字段是矛盾**
   - 错误尝试: 把 `license` / `publishConfig.access: public` 加到 `private: true` 包描述为"语义矛盾"
   - 正确判读: 用户开源,test-utils 随时可翻 `private: false`,字段是预备状态
   - 教训: 涉及 align 行为时,先想"用户的全局意图"再下结论
   - 浪费: 1 轮往返

2. **一次误操作的 No 拒绝**(用户主动声明误操作)
   - `git diff skills/.../COMMANDS.md` 第一次被否决,用户随后说"刚才是误操作 no 了,继续吧"
   - 教训: 收到 No 不要立刻换思路,先简短确认(用户在这个 case 主动澄清了)

### 关键 memory 更新(本 session)

| memory file | 类型 | 摘要 |
|-------------|------|------|
| `feedback_compaction_safe_doc_visibility.md` | feedback | 三层镜像规则:auth/version/handoff guardrail 必须 AGENTS+CLAUDE+memory 同步 |
| `project_test_utils_bdd_control_loop.md` | project | test-utils 真实定位 + 为什么不用 Cucumber + cortex FSM 是下个目标 |
| `project_handoff_daily_first.md` | project (update) | 注明 CLAUDE.md 已于 2026-05-03 修复对 daily/HANDOFF.md 的过期引用 |
| `feedback_cortex_trailer_lane.md` | feedback | commit 含 trailer 时 hook 自动 dispatch;失败时 `cortex probe` 兜底 |
| `MEMORY.md` (index) | — | 加上面 4 条索引 |

### 下一步(具体可执行)

1. **T6 剩余 scenarios** — 9+ 额外 BDD 场景(trailer 格式错、状态非法、多条 trailer 串行、checklist skip、checklist 全过、ADR accept/reject、Epic done/suspend 等)。runner 框架已就绪,只需写 markdown scenario 文件。
2. **Subagent 批量验证** — 用 Agent tool 跑至少 3 条新增 scenarios,验证 agent+skill+CLI 控制回路。
3. **SKILL.md 引用 scenario** — 在 `packages/lythoskill-project-cortex/skill/SKILL.md` 中引用至少 1 条 scenario 作为使用示例。
4. **(可选)发布到 npm** — 用 `./scripts/publish.sh`,会读 `.npm-access` 自动认证。**不要 `npm login`**。
5. **(观察)align 增量增强** — `engines`/`files`/`exports`/`bin` 字段尚未纳入 align 检查,可作为未来 creator 的子技能扩展。

### 接手自检

- [ ] `git status --short` 输出干净
- [ ] `git log --oneline -3` 顶端是 `b2ae87c`
- [ ] `bun packages/lythoskill-creator/src/cli.ts align` → 47 passed, 0 failed
- [ ] `bun packages/lythoskill-project-cortex/src/cli.ts probe` 没 warning(或历史大小写问题已知)
- [ ] 读完本 handoff 后能直接判断"要不要继续 T6 scenarios / 要不要 publish"

### 为下个 session 留的 reference 句

- 如果你不知道 bump 怎么用 → `AGENTS.md → Release & Auth Workflow → Lock-step versioning`
- 如果你怀疑 git remote / .ssh 配置 → **不要动**,问用户
- 如果你想加测试 → 不要装 Cucumber/Vitest,看 `packages/lythoskill-project-cortex/test/runner.ts` + 用 `@lythos/test-utils`
- 如果你看到 `daily/HANDOFF.md` 字眼(任何旧文档里) → 那是 deprecated 的,实际用 `daily/YYYY-MM-DD.md`
- 如果你想改 package.json 结构 → 先跑 `bun packages/lythoskill-creator/src/cli.ts align`,不要手工改
- 如果你看到 pnpm 相关文件 → 这是 debt,align 会自动报错并 fix
- 如果你要改 tsconfig → **先问用户**,历史上 tsconfig base 吃过苦头,用户明确认为是过度优化

---

## Session 续接(00:18 → 01:1X,中间发生过一次压缩)

> **元观察**:本段是 ADR-20260503003315478 设计的失败模式的真实实例 —— 上半场写完 ADR 草稿后 session 压缩,新接手 agent 只能从 daily(已过期)+ working tree 重建上下文。**daily 没及时追写 = 失忆点**。下个 agent:长 session 关键节点要追写 daily,不要等收尾。

### 后段做了什么

#### 1. 两条 ADR 起草到 proposed

- **`ADR-20260503003314901-git-coupling-...trailer.md`**(选项 C 推荐):Jira 风格 commit trailer(`Closes:` / `Task:` / `ADR:` / `Epic:`)→ husky post-commit 解析 → 调 cortex CLI 触发流转 → 跟随 commit 把状态变更入库。第 6 条决策原因显式说明 `cortex probe` 是失败兜底(自动化失败 ≠ 数据损坏)。
- **`ADR-20260503003315478-epic-granularity-...iteration.md`**(选项 E 推荐):Epic 双轨 lane(main 工作流 + emergency 紧急轨,各 ≤ 1)+ 5 题 checklist 软门禁 + `cortex probe` lane 检查。背景段把 Workflowy zoom-in / 会话影子 / 游戏任务链 / Kanbanize / ADHD 多发散→Epic 严焦点的漏斗 / Cortex = agent OS 雏形 / CW = 6502 寻址空间 / 引用 Hermes Agent + Manus context engineering 外部佐证全部串成一条线。

两条 ADR 都还在 `01-proposed/`,等 Epic + 6 task 实现完一起 accept。

#### 2. 实现 Epic + 6 task(本次 commit 的核心 untracked)

- **`EPIC-20260503010218940`** — `lane: main` 首发示例;主题 A-E 映射 6 task;归档条件含"至少 1 个 subagent 用 task 卡 bootloader 跑通 1 task"作为控制回路验证。
- **`TASK-...227902 (T1)`** — `cortex adr accept|reject|supersede` + `cortex epic done|suspend|resume` 状态机命令(T3 硬前置)
- **`TASK-...228602 (T2)`** — `cortex epic --lane` + `--override` + checklist + probe lane 扩展
- **`TASK-...229362 (T3)`** — `.husky/post-commit` trailer 解析 + 跟随 commit + 防递归 `Triggered by:` 标记
- **`TASK-...230554 (T4)`** — `.husky/pre-commit` in-progress 软提醒(不阻塞)
- **`TASK-...231389 (T5)`** — 三层文档镜像(AGENTS/CLAUDE/memory)+ cortex skill SKILL.md/README 同步
- **`TASK-...231988 (T6)`** — BDD 覆盖 trailer + lane FSM,subagent 跑样本(用 `@lythos/test-utils`)

每张 task 都按 ADR-B 的 "task = subagent bootloader" 原则写:简短主体 + 引用段全部反链(关联 ADR / Epic / sibling task / 模板参考 / memory),subagent 读 AGENTS.md + 这张卡就能开工。

#### 3. 设计推演的高密度概念(只落在 ADR body,不进 memory)

- Cortex = agent OS 雏形:task ID = handler,CW 只携引用,文件系统是磁盘
- CW 像 6502 寻址空间,task 卡引用 100MB 资源也不炸 CW
- AGENTS.md 长期可能要走 build 渲染(类比 SKILL.md 模板替换 / 前端 SSR)
- 短期还是"先拼"(KV cache 友好、稳定 prefix 靠近顶部)
- 自然推演新 ADR-C(CW snapshot + AGENTS.md build pipeline)— 暂不实现,留给后续

(用户主动澄清"ADHD 不是 memory" → 没存 user memory,只在 ADR-B 背景段引用一次作为发散与聚焦张力的解释)

#### 4. 工作树状态(commit 前)

```
?? cortex/adr/01-proposed/ADR-20260503003314901-...md
?? cortex/adr/01-proposed/ADR-20260503003315478-...md
?? cortex/epics/01-active/EPIC-20260503010218940-...md
?? cortex/tasks/01-backlog/TASK-20260503010227902-...md  (T1)
?? cortex/tasks/01-backlog/TASK-20260503010228602-...md  (T2)
?? cortex/tasks/01-backlog/TASK-20260503010229362-...md  (T3)
?? cortex/tasks/01-backlog/TASK-20260503010230554-...md  (T4)
?? cortex/tasks/01-backlog/TASK-20260503010231389-...md  (T5)
?? cortex/tasks/01-backlog/TASK-20260503010231988-...md  (T6)
M  INDEX.md  (cortex index 重生成)
M  cortex/tasks/03-review/TASK-...233741335-...bump.md  (上半场遗留,待确认)
M  packages/lythoskill-creator/src/bump.ts  (上半场遗留)
```

**`cortex probe` 6 issue** 全部是历史 `in-progress` vs `inProgress` 大小写不一致,**与今日工作无关**,本 session 不修。

### 续接段下一步

1. **commit 9 个 cortex 新文件 + INDEX.md**(本段写完之后立刻做);commit message 用 `feat(cortex): adr+epic for git-coupling and epic-granularity discipline`,在 body 列出 ADR/Epic/Task ID。
2. **本 daily 文件**(包括本续接段)同 commit 一起入库 —— 防止再次压缩失忆。
3. **T1-T6 实现** 留给后续 session;每个 task 卡已经是 self-contained bootloader,subagent 拿到卡 + AGENTS.md 即可开工。
4. **ADR-A / ADR-B 的 accept** 等 T1-T6 跑通后再做(probe + BDD 通过即可 accept)。
5. **(可选)push** main 比上半场又多 9+ untracked 待 commit;push 时机由用户决定。

### 续接段元教训

- 长 session 中段 + 设计密度高时,**必须每完成一个里程碑就追写 daily 一段**,不能等收尾;否则压缩后下个 agent 看不到。
- ADR-B 里写的"task = subagent bootloader"原则在本 session 实跑了一次:6 张 task 卡都按这个写,下次找 subagent 实现时就能验证假设是否成立。
- "daily-first handoff"在压缩面前是脆弱的(只能记到压缩前最后一次写盘),**真正的弹性来自 working tree 本身就是 self-describing**(task 卡引用反链齐全 + ADR 背景段 self-contained)。这一点本身就是 ADR-B 的副产品 —— 实际上 ADR-B 的"snapshot 机制"远期目标就是补强这层弹性。

---

## Session 再续(01:1X → 现在,再次压缩过)

> **元观察**:又发生了压缩。验证 daily 追写策略有效 —— 新 agent 从 daily 续接段恢复上下文后,继续 T2/T3 subagent 测试 + bump + push。

### 后后段做了什么

#### 1. T2 subagent 试跑 + commit (feat, dual-lane + checklist)

- 给 T2 task card 注入"⚠️ 仓库现状 vs Spec 冲突点"段(预决 3 个冲突:回填 lane / YAML frontmatter / templates 目录不存在)
- Subagent **严守 spec 没自创 migration**;显式报告 4 个非平凡决策并附 "if you wanted otherwise" 提示
- 产出:4 新文件(templates/epic.md + lib/{frontmatter,lane,checklist}.ts)+ 4 源文件改 + 7 epic backfill
- 狗食:task start → review → done,task 卡移到 04-completed
- Commit: `45714a3`

#### 2. T3 subagent 试跑 + commit (feat, post-commit hook)

- 给 T3 task card 注入"⚠️ 仓库现状 vs Spec 冲突点"段(预决 5 个冲突:post-commit 已存在 / hexo sync 顺序 / task done 严卡 review / 递归触发 / CLI cwd)
- Subagent 在 hook staging 范围上做了第 6 个良性决策(避免把 wiki 漂浮扫进 follow-up commit),显式报告
- **狗食高光**:T3 commit message body 含 `Closes: TASK-20260503010229362`,hook 触发后自动产出了 follow-up commit `266bbfa`,把 T3 task 卡从 backlog → completed — 这就是 ADR-A 描述的"commit-driven 不变量"自我实现
- Commit: `74a2584`(实现) + `266bbfa`(hook 自动 dogfood)

#### 3. 安全修复 + bump + push

- GitHub CodeQL 报警 4 处 execSync template-literal → 全部改为 execFileSync argv 模式
- Bump: 0.7.3 → 0.8.0(minor,3 个 feat)
- Push: main 和 skills branch 都同步到 origin

### 再续段下一步(用户指示)

- T4(pre-commit 软提醒)直接写,不拉 subagent(太简单)
- wiki 漂浮(4 个 untracked hermes doc)留给用户自己处理
- 本次 session 收尾后补 daily 最新段

### 再续段元教训

- **"冲突点预决"有效**:T2/T3 subagent 对比 T1 明显更听话;预决范围越细,subagent 自由发挥空间越小,结果越可控
- **hook dogfood 自我实现**:T3 的 commit 自己触发 hook 把自己 task 卡 close 了,这是最理想的验证方式(不是人工 test,是生产 commit 自己验证)
- **bump 时机**:feat 级别的 T1-T3 完成后 bump 是自然的,用户主动提议"整体bump" —— 说明 0.8.0 的叙事完整

---

## Session 三续(T4 完成)

### 后后后段做了什么

#### 1. T4 直接写(不拉 subagent,太简单)

- `.husky/pre-commit` 追加第 3 段:扫描 `cortex/tasks/02-in-progress/` 下 .md 文件数
- >0 → 打印 reminder + task ID 短列表 + trailer 语法示例
- =0 → 静默
- 不阻塞 commit(exit 0 保证)
- **Dogfood**:commit daily 更新时,T4 reminder 正确列出 6 张 in-progress task;同时 commit message 里的 `Closes: TASK-20260503010230554` 触发 T3 post-commit hook,自动产出 follow-up commit `8da1965`,把 T4 task 卡移到 completed

#### 2. 历史小瑕疵:cherry-pick 与 hook 双重 follow-up

T3 在 test branch 上 commit 时,hook 产了 follow-up `10c772a`。
Cherry-pick 到 main 时,main 的 hook 又产了 follow-up `fc3a3a5`。
然后 cherry-pick `10c772a` 变成 `266bbfa` —— 同一 task 被 close 两次。
 harmless(idempotent no-op),但历史里多一行。

**教训**:含 trailer 的 commit cherry-pick 时,目标分支的 hook 也会再跑,会产生重复 follow-up。以后 cherry-pick 含 `Closes:` 的 commit 前,先 `git cherry-pick --no-commit` 然后把 trailer 从 message 里删掉,再 commit。

### 当前状态(收尾前)

| 维度 | 状态 |
|------|------|
| T1(cortex verb) | ✅ completed |
| T2(epic dual-lane) | ✅ completed |
| T3(post-commit hook) | ✅ completed + hook 自 dogfood |
| T4(pre-commit reminder) | ✅ completed + hook 自 dogfood |
| T5(三层文档镜像) | backlog |
| T6(BDD 覆盖) | backlog |
| main vs origin | 领先 2 commit(`d78e11f` bump 已推,`d5f6c0e` daily + `8da1965` T4 follow-up 刚推) |
| 版本 | 0.8.0 |
| 6 个历史 in-progress task | 仍在 02-in-progress/,T4 reminder 每次 commit 都会提示 |

### 三续段下一步(用户决定)

- T5:三层文档镜像(AGENTS.md / CLAUDE.md / memory / skill README) — 纯文档,可直接写
- T6:BDD 覆盖 trailer + lane FSM — 最复杂,需要 subagent 跑 test runner
- Session 收尾:补完 daily 最新段,下个 agent 进场

**元观察**:T1-T4 全部通过 subagent + 狗食验证。控制回路假设成立。剩余 T5/T6 可以继续同模式推进。

---

## Session 终段(T5/T6 完成 + 收尾)

### 后后后后段做了什么

#### 1. T5 三层文档镜像 + skill README/SKILL.md 同步

- **`AGENTS.md`**: 新增 "Project Governance (Cortex)" 整段 — trailer 语法表、hook 职责分工、失败兜底(`cortex probe`)、Cortex Granularity(epic dual-track + checklist + layer separation)
- **`CLAUDE.md`**: 追加 compaction-safe 段(cortex trailer + lane discipline)
- **`memory/feedback_cortex_trailer_lane.md`**: 新增 feedback 规则 — commit message 含 trailer 时 hook 自动 dispatch,失败时 `cortex probe` 兜底
- **`packages/lythoskill-project-cortex/skill/SKILL.md`**: 同步所有新 verb(complete/accept/reject/supersede/done/suspend/resume/epic/probe)
- **`packages/lythoskill-project-cortex/README.md`**: 扩写 usage、trailer 示例、5-question checklist 说明
- Dogfood:T5 task 卡 commit 时带 `Closes: TASK-...31389`,hook 自动 follow-up 移到 completed
- Commit:`08b90bd`

#### 2. T6 BDD 覆盖 — trunk 实现(3/12+ scenarios)

- **`packages/lythoskill-project-cortex/test/runner.ts`**: 填充骨架 — markdown scenario 解析器(frontmatter + Given/When/Then 分块)、git/cortex fixture setup(fake ID 绕过 timestamp 生成)、runScenario 断言(exit code / stderr / filesystem / Status History)
- **Scenarios 通过**:
  1. `trailer-closes-task.md` — `complete TASK-TEST-001` 后文件移至 `04-completed/`,Status History 末行 `completed`
  2. `lane-main-rejected.md` — main lane 已有 1 active 时第二个 `--lane main` 被拒绝,stderr 含 "Cannot create another"
  3. `lane-override-accepted.md` — `--override "reason"` 绕过满 lane,frontmatter 含 `lane_override_reason`
- **Subagent 质量**:严守 "冲突点预决" 规则,3 个 scenario 文件 + runner.ts 全部自主实现,未自创 migration 或引入测试框架
- Dogfood:T6 commit 带 `Closes: TASK-...31988`,hook 自动 follow-up 移到 completed
- Commit:`8791cc8`(runner 实现) + `a2e9aeb`(hook 自动 dogfood,同时处理了 4 个历史 task 的 terminate + 新 in-progress 创建)

#### 3. 安全修复(CodeQL execSync → execFileSync)

GitHub CodeQL "Shell command built from environment values" 报警 4 处,全部改为 `execFileSync` argv 模式:
- `packages/lythoskill-creator/src/build.ts`
- `packages/lythoskill-deck/src/add.ts`
- `packages/lythoskill-deck/src/link.ts`

#### 4. Push

`main` 推送到 origin(ssh alias `calt13.github.com`),skills branch 由 pre-commit hook 自动同步。

### 当前状态(本 session 真正收尾)

| 维度 | 状态 |
|------|------|
| T1(cortex verb 扩展) | ✅ completed |
| T2(epic dual-lane + checklist) | ✅ completed |
| T3(post-commit hook + trailer) | ✅ completed + 自 dogfood |
| T4(pre-commit 软提醒) | ✅ completed + 自 dogfood |
| T5(三层文档镜像) | ✅ completed + 自 dogfood |
| T6(BDD 覆盖 trunk) | ✅ completed(3 scenarios) + 自 dogfood |
| Epic EPIC-20260503010218940 | ✅ done(全部 6 task 完成,已移入 `02-done/`) |
| main vs origin | 同步 |
| 版本 | `0.8.0` |
| 历史 in-progress task | 4 张旧 task 已 terminate(06-terminated/),新 ID 版本进入 02-in-progress |
| Working tree | 几乎干净 — 仅 `bun.lock` modified + 4 个 wiki 漂浮文件 untracked |

### T6 剩余工作(留给后续 session)

- [ ] 9+ 额外 scenarios(trailer 格式错、状态非法、多条 trailer 串行、checklist skip、checklist 全过、ADR accept/reject、Epic done/suspend 等)
- [ ] Subagent 批量跑至少 3 条以上 scenarios
- [ ] `cortex SKILL.md` 中引用至少 1 条 scenario 作为示例
- [ ] CI 集成(若有)

### 本 session 元教训(最终版)

1. **"冲突点预决"是 subagent 可控的核心杠杆**:T1 没预决 → subagent 自创 migration;T2/T3/T6 预决后 → 零意外行为。预决粒度 = 可控度。
2. **hook 自 dogfood 是最强验证**:T3/T4/T5/T6 的完成全部由 commit 自己的 trailer 触发 hook 自动完成,不是人工 test。这是 ADR-A "commit-driven 不变量" 的 4 次自举证明。
3. **长 session 中段必须追写 daily**:本 session 经历 3 次压缩,每次靠 daily 续接段恢复上下文。没有中段追写 = 失忆。

---

## 追加段:Debt Epic 清理完成

### 做了什么

在 T6 剩余工作之前,用户指示先处理 `EPIC-20260430011158241`(monorepo config debt)。经过 challenge 后,路线从"手动改 4 个 task"切换为"增强 `creator align` 的声明式治理能力(k8s 心智)":

1. **`align.ts` 新增两个 check 组**:
   - `checkRootRuntimeDeps`: 根 `package.json` 不得有 `dependencies`(runtime dep 必须下沉到叶子包)
   - `checkPnpmResiduals`: `pnpm-lock.yaml` + `pnpm-workspace.yaml` 不得存在(Bun-only 工具链)
   - `checkPackages` 修复: 只有含 `skill/` 目录的包才检查 `skills/*/SKILL.md` built output(test-utils 不再误报)

2. **`align --fix` 自动收敛**:
   - 根删 `@iarna/toml` + `zod`(deck 已声明)
   - 删 `pnpm-lock.yaml` + `pnpm-workspace.yaml`
   - `bun install` 重生成 `bun.lock`,deck CLI 验证正常
   - 47 passed, 0 failed

3. **`probe.ts` 修复**: `inProgress` → `in-progress` camelCase 匹配 bug

4. **AGENTS.md 更新**: Package Manager 从 pnpm → Bun

5. **Epic 治理**: playground-epic → archived, virtual-evaluator → suspended

### Task 状态

| Task | 结果 |
|------|------|
| TASK-20260503132523380(根 dep 下沉) | ✅ completed(hook dogfood) |
| TASK-20260503132524022(lockfile 统一) | ✅ completed(hook dogfood) |
| TASK-20260503132524651(tsconfig base) | ❌ terminated(用户明确中止:过度优化) |
| TASK-20260503132525248(package.json 模板) | ✅ completed(align 已覆盖核心字段) |
| EPIC-20260430011158241 | ✅ done |

### 元教训

- **Challenge 假设比执行更重要**:4 个 task 的前提假设经过 challenge 后,T3 被证伪(过度优化),T4 被重构(从手动改文件 → align 自动治理)。
- **K8s 心智 = 声明式治理**:align 定义期望状态,`--fix` 自动收敛。这比一次性清理更有价值,因为 debt 会反复产生(每个新 session 的 agent 都可能固化新选择)。
- **团队统一用 Bun**:人类 + agent swarm 的"团队"只需要 memory + AGENTS.md + README 三重确认,不需要复杂沟通流程。
- **execFileSync 替代 execSync 是 security 基线**:模板字面量注入风险真实存在,CodeQL 能抓到。所有未来 shell 调用默认走 argv 模式。
