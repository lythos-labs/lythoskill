# Daily — 2026-08-29

## Session Handoff — P1 推进：TASK-671（host-handoff）+ TASK-988（站点数字注入）双关闭

### 0. Verify Current State

**Git**: HEAD = 本会话最后一个提交（以 `git log -1` 为准），已推送
**Version**: v0.17.11（无 bump；671 是 minor-level 行为变更，发版窗口等用户拍板）
**Branch**: main，clean working tree
**Active Task**: 0 in-progress，0 in review
**Tests**: `bun --filter='*' run test` EXIT=0；probe 514 docs 0 issues
**Config（仓外）**: `~/.kimi-code/config.toml` — `max_steps_per_turn` 100→500，`max_retries_per_step`→`max_attempts_per_step`（deprecated 改名，值不变）；备份 `config.toml.20260828-235308.bak`

```bash
# Verify: if output diverges from the fields above, this handoff is stale
git status --short && git log --oneline -5
```

### Completed

1. **TASK-20260828141622671（arena host-agent handoff 模式）** — ADR-20260828004129143 Option B 落地。`packages/lythoskill-arena/src/host.ts`（`detectHost(env)` + `resolveSingleMode`，纯函数可注入）；cli.ts singleRun：mode 解析放最前（验收 3 要求 no-player 错误先于 --deck 错误）→ handoff 分支在 deck fetch + adapter 动态 import 之前（no-spawn 结构性成立）→ external 路径字节级不变。env marker 实况：kimi-code 无 `KIMI*` 变量，只有继承自 claude-code 的 `CLAUDE_CODE_SSE_PORT`；检测宿主无关，handoff 文案也因此宿主无关。12 tests（4-cell 矩阵 + env 钉死集成 + tmp cwd 无 `agent-output-*` 断言）。ZK skeptic PASS-WITH-NITS + user-sim yes-with-conditions，条件全在 pre-review 修掉：help 文案补双模式、handoff 指引 How 第 1 步补 `bunx @lythos/skill-deck link --deck <path>`、bogus 本地 deck fail-fast（URL 放行）、`--player ""` 响亮拒绝。R5 文档审计：player-setup.md 删掉 "Defaults to kimi" + fallback 链、site guide en+zh 改写、wiki player-abstraction 加指针。
2. **TASK-20260828141622988（站点数字构建时注入）** — `site/scripts/inject-stats.ts`（机械化计数：decks=`examples/decks/*.toml`、packages=`packages/*/` 有非 private package.json、skills=`skills/*/` 目录）→ `.vitepress/stats.json`（gitignored，镜像 version.json 模式）→ config.ts markdown-it core rule 替换 `{{DECK_COUNT}}/{{PACKAGE_COUNT}}/{{SKILL_COUNT}}`。EN+ZH 6 处占位符；CI grep 断言进 test.yml site-build；inject 步骤补齐 deploy-pages.yml + release.yml（缺了会在 stats.json import 炸 build）。盘点表在卡上：注入 6 处、intentionally-static 7 类（600+ tests 无廉价来源、philosophy 个人快照、示例算术等）。ZK skeptic PASS-WITH-NITS（P2 zh README parity + 3 P3 全修）；user-sim yes-with-conditions，fresh-clone 反向实验实证 **fail-loud**（缺 stats.json → vitepress build exit 1）。附带刷新：README 双语 661→1000+ 测试数（实际 1013/56 文件）、"18+ decks"→"24+"、conclusion-first ADR 数 90→nearly 100（实际 98）。
3. **kimi-code 配置调整（仓外）** — 见 Verify Current State；走 update-config 技能流程（copy→edit→doctor→timestamped backup→mv）。

### Pitfalls

- **esbuild 拒绝被 import 的非入口文件里的 shebang**——`inject-stats.ts` 被 config.ts import，`#!/usr/bin/env bun` 直接炸 build。教训：会被 import 的脚本不写 shebang；`import.meta.main` 同样触发 cjs 警告，用 `process.argv[1]?.endsWith(...)` 判直跑。
- **站点测试的落点**：`site/` 不是 workspace member（workspaces = `packages/*`），site 本地 test script 永远进不了 canonical gate。site 相关测试放 root `scripts/`（check-site-commands 先例），由 skill-creator 的 `bun test src/ ../../scripts/` 带跑。
- **CLI_TABLE drift tripwire 会抓 `=== 'enum-literal'`**——新枚举（SingleMode）按 curator 先例入 `ignoreDispatch` 并注释出处。守卫按预期工作，不是误报。
- **tsc 不在本项目工具链**——`bunx tsc` 解析到 4.3.5 远古版，全仓库存量文件都"编译不过"。本仓库的 "TS compiles" = Bun 原生跑绿，别再去验 tsc（用户当场纠正的一次过度确认）。

### Key Decisions

- **671 不 bump 版本**——minor-level 行为变更已写进 commit message，发版窗口由用户决定（用户原话：值得独立发布窗口）。
- **988 机制选 option (a)**（markdown transform + 生成 JSON，镜像 inject-version），不选 markdown-it plugin 包——触文件最少，复用现有心智模型。
- **README 测试数走 range 化**（"1000+"）而非精确值——README 无构建管线，精确值必腐化，range 腐化慢。
- **`{{SKILL_COUNT}}` 已计算但无页面使用**——user-sim 记录为轻微 over-engineer 气味，不追责；下次加占位符前先问"现在有页面用吗"。

### Next Steps

1. **TASK-20260828004417068（DSH 调研）**——P1 序列下一张，消费方明确（adapter 矩阵 planned 行）。注意：671 落地后本会话内 `arena single` 会触发 handoff（dogfood 新默认）——按 arena skill 的 agent-orchestrated 模式自己 spawn 研究 subagent。耗外部调研预算，启动前问用户。
2. **发布窗口决策**（用户）：671 + 988 都在未发布 commit 里；若发版 = minor bump（`bunx @lythos/skill-creator@0.17.11 bump` → push --follow-tags → 盯 release workflow）。
3. backlog 衍生卡：TASK-20260828212204402（kimi probe 加固 P3 包）、TASK-20260828220646204（curator add 硬编码 parsed）、TASK-20260828232221671（stale `players detect` 文档引用）。
4. …502（方案D）仍 parked：≥2 次 dreaming 运行或 …425 落地后 4 周。

### Temp Artifacts

- `/tmp/commit-671-*.txt`、`/tmp/commit-988-*.txt`、`/tmp/test-gate-*.log`——临时，无需保留。

## Addendum — submit 时 CI 抓到本地门禁的盲区（已修，教训入库）

- **事件**：push 后 CI 红（3 个 arena singleRun validation 测试），本地 gate 全绿。根因：671 的 mode 解析在 flag 校验之前，这 3 个存量测试因此依赖宿主 env——本地 kimi-code 会话有 `CLAUDE_CODE_SSE_PORT`（走 handoff，仍校验 flag），CI 裸环境走 no-player 错误先触发。修复：3 个测试显式传 `--player kimi` 钉死 external 路径（它们测的本就是 legacy 校验顺序）。
- **结构性教训**（已写入 AGENTS.md Critical Gotchas [TEST]）：env 敏感代码的测试必须显式钉 env；拿不准就跑 `env -u CLAUDE_CODE_SSE_PORT -u CLAUDECODE bun --filter='*' run test` 做 CI 模拟。**ZK skeptic 在同宿主 env 里跑测试，同样会被 masking**——以后 ZK 审查涉及 env 分支时，prompt 里要求裸 env 复跑。
- 修复后双 env profile 各跑一遍（host / 无 host）158/158 绿 + CI 模拟全量 EXIT=0；push 后 CI ✅。

## Addendum 2 — v0.18.0 发布：npm 传播延迟击穿 verify 重试（已修复管线）

- **用户拍板发 0.18.0**（minor：671 默认行为变更）。流程：bump minor → install → gate EXIT=0 → commit → tag v0.18.0 → push --follow-tags。
- **事件**：release workflow 的 13 个包全部发布成功，但 verify tripwire 在发布完成后 ~7s 开始查 `@lythos/skill-creator@0.18.0` 连续 5 次 404（重试预算仅 ~10s），fail-closed 退出 → GitHub Release 和 release 内 Pages job 被 skip。重跑失败 job 又撞 E403（publish 步骤非幂等）。
- **手动补完**：本地 tripwire `bun scripts/check-published-manifests.ts 0.18.0` ✅ 13/13 无 workspace: 泄漏 → `publish-github-release.sh` 建 Release ✅ → Pages 由 `workflow_dispatch` 补发 ✅（deploy-pages.yml 的 paths 过滤不含 package.json/skills，release commit 不触发它——site footer 版本号靠 release 管线更新，这次靠手动 dispatch）。
- **管线加固**（commit `8d75c4af`）：publish 步骤幂等（`npm view` 已在则跳过，重跑安全）；verify backoff 5×(1-4s)≈10s → 8×(5-35s)≈140s。
- **教训**：fail-closed tripwire 是对的（它挡住的不是泄漏而是"未验证"），但重试预算要按 npm 真实传播延迟定（分钟级，不是秒级）；发布管线每一步都要可重入——"重跑一次"必须是无害操作。
- deck refresh --exec + link 已跑，lock hash 更新随本 addendum 一起提交。
