# Daily — 2026-05-13

## Session Summary

User requested: compare TDD skill frontmatter/body with cortex current format, then evolve into a real feature implementation.

### What happened

1. **Confirmed `.claude/skills/` lag** — symlinked cold pool skills were at v0.9.29 while local source was v0.10.1. Root cause: cold pool had an uncommitted local modification (`skills/lythoskill-project-onboarding/SKILL.md`) blocking `git pull --rebase`.
   - Diagnosis: `git status --short` in cold pool → one modified file.
   - Verification: checked local `skills/` dir, confirmed the improvement was already in v0.10.1.
   - Fix: `git checkout --` to discard stale change, `git pull --rebase` fast-forwarded 0d81657→8b23d70 (310 files), `deck link` synced.
   - Result: all lythoskill skills now at v0.10.1.

2. **Baoyu-skills context** — user explained the baoyu-skills example decks were created for end-to-end testing of third-party skill integration + mirror fallback. Previous network issues prevented success.
   - Discovered `7dbe951` fix: `LYTHOSKILL_GH_MIRROR` was not wired into `buildFetchPlan`'s git clone URL — only HTTP fetches used mirror fallback.

3. **`probeConnectivity` feature** — user wanted UX improvement: probe network before attempting operations, fail fast instead of waiting 30s timeouts.
   - TDD approach: wrote 6 tests first (direct/mirror/all-fail/404/racing/timeout).
   - RED→GREEN: tests passed after implementing concurrent racing + `AbortSignal.timeout` + failure collection.
   - Research insight: web search revealed `Promise.any` + shared `AbortController` as standard pattern. Custom racing loop was correct for the specific requirement (collect all failures + return first success), but the search saved ~15min of unproductive debate.

4. **AGENTS.md update** — added "Plan must include research" section in the Intent/Plan/Execute pattern. Research is part of Plan, not a gate between Plan and Execute.

### Commits

- `8b097c5` feat(cold-pool): probeConnectivity with concurrent racing + error collection
- `bb66cbe` docs(AGENTS): Plan must include research

### Cortex artifacts

- Epic: `EPIC-20260513010237904` — Popular third-party skills end-to-end with network probe UX
- Task: `TASK-20260513010246527` — Implement probeConnectivity with TDD + racing + proper error collection (completed)

### Pitfalls for next agent

- `project-scribe` package is thin-skill only (no src/ CLI). Do not try `bun packages/lythoskill-project-scribe/src/cli.ts`.
- Cold pool git repos can have stale uncommitted changes that block `deck refresh`. Always `git status` in cold pool before diagnosing refresh failures.
- When mocking `fetch` with `AbortSignal.timeout`, the mock must listen to `signal.addEventListener('abort', ...)` or the test will hang.

### Next steps

- Integrate `probeConnectivity` into `deck add` / `deck refresh` / `arena` CLI for fail-fast UX
- Run baoyu-skills end-to-end test once network stabilizes
- Consider `Promise.any` + shared `AbortController` refactor if orphan fetch cleanup becomes a concern
