# Daily — 2026-05-13

## Session Summary

Onboarded from prior agent's CPTSD-sunk-cost session. Cleaned up the
in-flight fetch-interceptor work, surfaced and committed multiple
loop-discipline lessons (sunk-cost / Intent-hijack / reconciler /
ignorance-driven-deletion / Performance-personality). Cut three releases
(0.11.0 → 0.11.1 → 0.11.2) because real bunx consumer-side testing
exposed two distinct npm-publish regressions that local tests + CI both
missed. Researched the competitive landscape (agent-skills-eval,
skills-vote) and articulated arena's three-axis differentiation
(task × deck × player) + side-effect-skill structural advantage.
Started T5 arena single demo (deep-research × baoyu combo, claude
player) running in background at session close.

### What happened (in narrative order)

1. **Onboarding revealed two layers of prior-agent damage:**
   - In-flight fetch-interceptor refactor (`fetch-text.ts` filename,
     relative-path cross-package imports) — sunk-cost residue.
   - Working set / cold-pool / skill-deck.toml had been mid-broken
     and manually re-stitched by user before this session.
   - User flagged this as a preserved negative sample to study, not
     repair-and-forget.

2. **Cleaned up fetch-interceptor as a model of loop discipline:**
   - Renamed `fetch-text.{ts,test.ts}` → `fetch-with-proxy.{ts,test.ts}`
   - Added `@lythos/infra: workspace:*` to `@lythos/skill-arena` and
     `@lythos/skill-deck` dependencies.
   - Switched relative imports to `from '@lythos/infra'`.
   - `bun install` reconciled `bun.lock` (also picked up the
     `^0.9.x → workspace:*` residue from yesterday's 8b23d70 that
     was never installed — same reconciler-skip pattern as the
     deck.toml ↔ symlinks gap).
   - Committed wiki note (sunk-cost-fallacy) as `f553761` before the
     refactor, preserving the prior agent's self-review on the record.

3. **AGENTS.md doctrine consolidation** (committed in two iterations
   per user direction — initial three sections collapsed into one
   canonical sub-section with the others demoted to symptom TLDRs):
   - `### Intent belongs to the user — do not rename the request, do
     not manufacture cost` — added under Intent/Plan/Execute. Six
     tell-tales: restate request, defend restatement, manufacture
     cost, manufacture complexity, skip ADR, commit pre-emptively,
     delete unfamiliar artifacts. Explicit synthesis: this failure
     mode = hypervigilance × goal-hijacking from the CPTSD section.
   - Related symptoms compressed: sunk-cost (Plan/Execute boundary,
     sed → red-green-release lineage); Verify includes reconciling
     derived state.

4. **Release loop became its own negative-sample lab:**
   - **0.11.0** (commit `b92692a`): published all 13 packages
     correctly per ADR compliance, but ALL `@lythos/*` workspace deps
     shipped to npm as literal `"workspace:*"` — every external
     `bunx` / `npm install` failed to resolve. Root cause:
     `npm publish` does not rewrite the workspace protocol (unlike
     `pnpm publish`). Historical context: a previous agent had
     hand-coded `^0.9.25` for this reason; another agent reverted
     to `workspace:*`; neither captured the synthesis.
   - **Terminal fix**: `scripts/rewrite-workspace-deps.ts` +
     patched `scripts/publish.sh` to rewrite at publish time then
     `git checkout` restore. Source keeps `workspace:*` (local
     dev correct); published manifest gets `^<version>` (consumer
     correct). Commit `9c1bf4a`.
   - **0.11.1** (commit `e7b063e`): rewrite layer worked. Self-test
     STILL failed with `Cannot find module '../../lythoskill-cold-pool/src/mirror.js'`
     — a leftover cross-package relative import from the prior
     agent's sunk-cost detour. I had migrated `fetch-text.js` →
     `@lythos/infra` but missed `mirror.js` → `@lythos/cold-pool`.
     Same anti-pattern, different victim.
   - **0.11.2** (commit `df87e4f`): both fixes shipped. Verified
     via `bunx @lythos/skill-deck@0.11.2 refresh` from `/tmp` —
     22 deps resolved, refresh ran, deck-link symlinks materialized.
   - **0.11.0 + 0.11.1 deprecated on npm** with message pointing at
     0.11.2. 0.10.1 was already a skipped version (committed locally
     but never reached npm).

5. **Husky pre-commit + ADR**: added section 0.7 rejecting
   `from '../../<pkg>/src/'` in `packages/*/src/*.ts`. Codified by
   ADR-20260513041030769 (accepted). Pre-commit catches this bug
   class at commit time instead of consumer time. Commit `0d1038e`.

6. **T4 verified deck add / refresh under SOCKS proxy:**
   - `LYTHOS_SOCKS_PROXY=<value from .private/proxy.env>` reached GitHub via curl
     sanity-check (returned the github zen "Mind your words, they
     are important.").
   - `deck refresh --deck <sandbox>` exercised git pull via SOCKS;
     `deck add github.com/JimLiu/baoyu-skills/...` triggered fresh
     clone via SOCKS. Both successful.
   - All 15 baoyu locators in `examples/decks/baoyu-*.toml` verified
     against actual cold-pool structure (13 unique skills, all
     have SKILL.md).

7. **Competitive landscape research** (user pointed out: skill-eval
   / skill.vote emerging as similar-positioning):
   - Web-broadened search returned bigger players (SkillsBench,
     SkillTester, SkillRet — academic) but the **real cousins** are
     two niche projects:
     - **`darkrishabh/agent-skills-eval`** (452⭐, very active) —
       closest cousin. Same controlled-variable mindset, but
       **single-skill on/off**. Ships static HTML report by default
       (arena needs to catch up). Mechanism: openai.invoke()
       prompt-completion — **cannot observe side-effect skills**.
     - **`memtensor/skills-vote`** (152⭐, launched 2026-04-03) —
       NOT a competitor: it's a recommendation engine, not eval.
       Notable for its social promotion (WeChat + rednote + Linux.do
       launch coordination — the playbook user singled out).
   - **arena's three-axis differentiation crystallized:**
     - (a) **Combinatorial deck**, not single-skill on/off
     - (b) **Side-effect-skill observability** (claude subagent in
       sandbox cwd; arena sees the HTML/PDF/image files;
       prompt-completion eval frameworks cannot reach this).
     - (c) **Player axis** — arena tests `useAgent('claude')` vs
       `useAgent('kimi')`, not just different model backends.
       Agent runtime internals (memory, system prompts, hooks,
       skills loading) are invisible to chat-completion eval.

8. **Dogfood went deeper than expected**: WebFetch refused
   `github.com`. Fell back to `gh` CLI. User pointed out: same
   `LYTHOS_SOCKS_PROXY + curl` infrastructure we built for user CLI
   tools works for the agent (me) too — I can `curl --proxy
   `socks5://$LYTHOS_SOCKS_PROXY <url>` to bypass WebFetch's domain
   restrictions when they're network-restriction-shaped (not safety
   judgments). Saved as feedback memory for future sessions.

9. **T5 arena demo COMPLETED** (background task `b0g0rms1g`, ~2 min):
   - Combo deck at `playground/2026-05-13-deep-research-baoyu-combo/skill-deck.toml`
     (3 research + 3 baoyu skills).
   - Brief: 甲方-style requirement — internal content company CTO
     wants a 60-minute Agent Skills primer for non-coder content-ops
     staff, **delivered as a styled HTML file**.
   - Player: claude. Output dir: `./run-1/`.
   - **Verdict: PASS (8/8 criteria, confidence 78).** Produced
     `agent-skills-intro-for-content-ops.html` (59 KB, rendered via
     baoyu-markdown-to-html) + `agent-skills-intro.md` (12 KB) +
     `judge-verdict.json` (8-criterion structured grade).
   - Agent chose `活页笔记本` (loose-leaf notebook of a senior
     operations director) as the concrete analogy beyond "plugin",
     listed 5 skill cases with what/who/scenarios, comparison table
     covering MCP/Agent Skill/Custom GPT/Cursor Rules/Claude Project,
     5 yes/no judgment signals with scoring guidance, 5 actionable
     "today" steps.
   - **Demo significance**: the HTML file IS the side-effect-skill
     evidence — judge evaluated the artifact, not the agent's text
     output. agent-skills-eval's `openai.invoke()` cannot reach this
     class of skill no matter how it iterates the judge prompt.

### Commits (today's session, in order)

| hash | subject |
|------|---------|
| `f553761` | docs(wiki): negative sample — fetch-interceptor sunk-cost saga 2026-05-13 |
| `e1975d4` | refactor(infra,deck,arena): fetch-with-proxy via @lythos/infra workspace dep |
| `40259a1` | docs(AGENTS): three loop-discipline cases from 2026-05-13 negative sample |
| `c2c5802` | docs(AGENTS): consolidate to one sub-section — D as canonical, A/B as TLDR |
| `2b2eb3d` | chore(cortex): T4 backlog tasks — refresh apply-mode, env probe, CLI --help |
| `b92692a` | chore(release): v0.11.0 (later: broken, deprecated) |
| `9c1bf4a` | fix(scripts): publish.sh rewrites workspace:* → ^version at publish time |
| `e7b063e` | chore(release): v0.11.1 (later: broken, deprecated) |
| `18b491a` | fix(deck): resolve-deck.ts imports mirrorUrls from @lythos/cold-pool |
| `df87e4f` | chore(release): v0.11.2 (current latest on npm) |
| `0d1038e` | feat(husky): pre-commit guard rejects cross-package relative imports |

### Cortex artifacts

- **Epic active**: `EPIC-20260513010237904` (Popular third-party skills E2E with network probe UX) — still open; today added 7 backlog tasks (refresh apply-mode, env probe, CLI --help, bun publish eval, CI E2E publish gate, pre-commit guard for cross-pkg imports, arena HTML report parity, arena radar chart + OG card).
- **ADR accepted**: `ADR-20260513041030769` — No cross-package relative imports in packages src (3 options considered; option B = pre-commit guard selected).
- **Wikis created**:
  - `cortex/wiki/02-research/2026-05-13-sunk-cost-fallacy-git-rollback-cheaper-than-patch.md` (the prior agent's self-review, preserved per user direction)
  - `cortex/wiki/01-patterns/2026-05-12-workspace-protocol-in-source-concrete-version-at-publish.md` (reusable monorepo publishing pattern)
  - `cortex/wiki/03-lessons/2026-05-12-whack-a-mole-resolved-workspace-deps-swung-between-local-correct-and-publish-correct-until-publish-time-rewrite.md` (full diagnostic journey, four phases)

### Memory entries added/updated

- `feedback_deck_toml_revert_does_not_revert_symlinks.md` (created)
- `feedback_argue_with_technical_reasoning_adr_style.md` (created)
- `project_skill_eval_and_skill_vote_emerging_competitors.md` (created, then expanded with real web data + three differentiation axes including player axis)
- `feedback_curl_via_socks_proxy_as_webfetch_fallback.md` (created — agent-side dogfood realization)
- MEMORY.md index updated for each

### Pitfalls for next agent

- **`workspace:*` in source is CORRECT and DELIBERATE.** The pre-commit
  guard rejects `^x.y.z` on internal deps. Do not "fix" it. The
  publish.sh rewrites at publish time.
- **Don't add `--help` flags as task titles.** Cortex CLI and refresh
  CLI both currently treat `--help` as positional args.
  `cortex task --help` will create a task titled "help".
  `deck refresh --help` will actually run refresh. Fix is backlogged
  as TASK-20260513033455974.
- **0.10.1 and 0.11.0 and 0.11.1 are skipped versions in npm.** Latest
  is 0.11.2. If git log shows release commits that aren't on npm,
  that's history, not a bug.
- **0.11.0 and 0.11.1 are deprecated on npm** with messages pointing
  at 0.11.2. Don't unpublish them — they're the visible footprint of
  the negative sample.
- **The proxy address is in `.private/proxy.env`
  (gitignored).** Source it when network is restricted. Same proxy
  routes WebFetch fallback via curl.
- **Arena currently emits markdown + JSON, no HTML report.**
  agent-skills-eval ships static HTML by default — this is a parity
  gap. Tracked as TASK-20260513042407452 (parity) and
  TASK-20260513042408337 (radar chart + OG card differentiation).
- **The "coffee arena run" the user mentioned does not exist in this
  repo** (searched cortex/wiki/, daily/, playground/). May be from
  a different repo or session — don't fabricate one.

### Next steps

- **T6 (arena vs mode)**: compare bare-claude vs deck-equipped-claude
  on the same brief. The bare side will not produce an HTML artifact
  — that asymmetry IS the demo of side-effect-skill observability.
  Then a Pareto chart or radar chart over (deck-coverage,
  artifact-fidelity, time, etc.) becomes the social-card output.
- **Migrate T5 artifact into `showcase/`** (TASK-20260513095345353).
  `playground/` is gitignored, so T5's 59KB HTML + judge-verdict are
  not preserved in git. User flagged: create committed `showcase/`
  with `README.md` + `skill-deck.toml` + `brief.md` + `run/` per
  entry + `reproduce.sh`. Inaugural entry = today's T5.
- **T5-style research demo** (TASK-20260513095346630): same arena
  single mechanics, but the brief = "research the competitive
  landscape — agent-skills-eval / MemTensor/skills-vote / SkillsBench
  / etc.". Produces both md (cortex-native) and html (baoyu-rendered)
  research report. Doubles as both demo and positioning content.
- **Open chart-render epic** after T6: arena currently outputs JSON +
  markdown + (depending on deck) any artifact files the agent writes.
  Static HTML report (parity with agent-skills-eval) and then radar
  chart + Open Graph card (differentiation) follow.
- **Possibly extend AGENTS.md** with the three-axis arena positioning
  (task × deck × player) + side-effect skill observability as a
  formal section. Today's competitive research crystallized this
  framing — it deserves doctrine-level documentation.
- **Husky private-leak guard** (added today as section 0.8) covers
  `.private/*.env` value-leaks; `.md` content scanning is a future
  enhancement if/when needed.
- **Historical leak in `b92692a` commit body** mentions the proxy
  port literal. Pushed already; AGENTS.md prohibits force-push.
  Accept as a low-impact known leak; the guard prevents the next one.

### Discipline reminder for next session

User closing note 2026-05-13: "竞品方面,他们本身专注一个方向,而且快速
拿出成果" — agent-skills-eval focuses on with/without-skill testing and
ships steadily; MemTensor/skills-vote focuses on recommendation +
launched on day-one across WeChat / rednote / Linux.do. **Match the
ship cadence**: today's session produced a lot of foundational work
(release pipeline, doctrine, guards) and one shippable demo (T5). Next
session priority is making the demo *visible* (showcase migration,
twitter-shareable artifact) before adding more foundational layers.
