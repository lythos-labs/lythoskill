# Daily — 2026-05-29

## Ground Truth

**Git**: HEAD = `3045aac` (IO refactor) → T4 commits (uncommitted)
**Version**: v0.15.6
**Deck**: 15 skills
**Branch**: main, dirty working tree (T4 changes)
**Active Epic**: EPIC-20260529214429614 (Curator CLI IO injection + BDD coverage)
**Active Task**: TASK-20260529214622541 (T4: runRefreshPlan/Execute IO injection)

## Session Handoff — T4 completion, reproduce.sh BDD, ZK audit

### Completed

1. **T4: runRefreshPlan / runRefreshExecute IO 注入** (TASK-20260529214622541)
   - `runRefreshPlan(argv, io?)` 改为 `export async`，接受 `CuratorIO`，解构 `{log, error, exit}`
   - `runRefreshExecute(argv, io?)` 同上
   - `main` 入口改为 `await runRefreshPlan(...)` / `await runRefreshExecute(...)`
   - **Git two-dot 修复**: `HEAD...@{upstream}` → `HEAD..@{upstream}`（两处）
     - 三-dot 范围会计算本地领先上游的提交，导致 "behind" 计数虚高
     - 二-dot 范围只计算上游有但本地没有的提交，符合 "behind" 语义
   - 测试 R1-R5:
     - R1: empty pool → plan with 0 items
     - R2: pool with git repo → plan includes repo
     - R3: 源代码级验证 two-dot 语法（解析 safeGit 调用而非全文匹配）
     - R4: no plan file → error + exit(1)
     - R5: all up to date → success message
   - `bun test packages/lythoskill-curator/src/cli.test.ts`: 50 pass, 0 fail

2. **reproduce.sh Agent BDD 场景**
   - 位置: `showcase/2026-05-29-curator-io-injection-bdd/`
   - `reproduce.sh`: IoC handoff 模式，验证 11 个 CLI 函数的 IO 注入 + git range + 测试覆盖
   - `judge.md`: 7 条评分标准（6 条 weight=1 + 1 条 weight=0.5）
   - `README.md`: 场景说明

3. **ZK self-audit**
   - ✅ 零 `spyOn(console)` / `spyOn(process)` — 所有输出通过 `CuratorIO`
   - ✅ 所有 `CatalogDb` 实例都有 `.close()`（显式 try/finally 或 `useDb` 助手）
   - ⚠️ DRY 违规: `catchExit` ×4, `seedDb` ×4 重复定义（各 describe 块内）
   - ⚠️ `seedDb` 类型不统一: runAudit 版本有额外字段
   - ⚠️ `spyOn` 导入已清理（未使用）
   - 结论: 无阻塞问题，DRY 违规属于技术债务，不在 T4 范围内

### Key Decisions

- **T4 范围严格限制**: IO 注入 + git two-dot 修复 + R1-R5 测试。refresh 语义混淆（cold pool refresh vs catalog refresh）留待未来 ADR 实现。
- **R3 测试策略**: 不全文搜索（会匹配注释），而是解析 `safeGit([...])` 调用，验证 `rev-list` 参数包含 `"HEAD..@{upstream}"` 且不包含 `"HEAD...@{upstream}"`。

### Pitfalls

- **注释污染测试**: 第一次 R3 实现全文匹配 `HEAD...@{upstream}`，失败因为测试注释本身包含该字符串。→ 修复为只检查 `safeGit` 调用参数。
- **async 函数导出**: `runRefreshPlan`/`runRefreshExecute` 需要 `export async` 才能在测试和 main 中正确 await。

### 0. Verify Current State

```bash
bun test packages/lythoskill-curator/src/cli.test.ts    # Should be 50 pass, 0 fail
git diff --stat                                          # cli.ts + cli.test.ts + reproduce.sh + judge.md + README.md
```

### Next Steps

1. **Review 推进**: T1-T6 + T4 全部完成，执行 `cortex review` 推进到 review 状态
2. **合并后**: 关闭 EPIC-20260529214429614
3. **技术债务**: 提取 `catchExit`/`seedDb` 到测试 helpers（低优先级）

### Temp Artifacts

- 无
