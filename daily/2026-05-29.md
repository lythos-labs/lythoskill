# Daily — 2026-05-29

## Ground Truth

**Git**: HEAD = `7a8d6a6` (6 commits today)
**Version**: v0.15.6 (unchanged — no version bump today)
**Branch**: main, clean working tree
**Active Epic**: 0
**Active Task**: 2 backlog (TASK-20260529132734903, TASK-20260529132806774)

## Session Handoff — IO injection unification, reproduce.sh migration, external agent report

### Completed

1. **CLI IO 注入统一收尾**
   - ADR-20260529002942317 从 `02-accepted/` → `03-superseded/`（非删除）
   - conventions.md 删除 L1 Escape Hatch 段落
   - Internal Roundtable / Agent Evaluation Arena 文档更新为 RESOLVED
   - AGENTS.md 新增 **ADR Immutability Rule**

2. **Kimi Adapter 修复**
   - `detectKimiBinary()`: 优先 `kimi-cli` (v1.x) 回退 `kimi` (v0.5.0)
   - 解决 `--print` vs `--prompt` CLI 不兼容问题

3. **过期 .agent.md 清理 + reproduce.sh 迁移**
   | 删除的 .agent.md | 替代 reproduce.sh | 结果 |
   |-------------------|-------------------|------|
   | deck-add.agent.md | `showcase/deck-add-reproduce-sh/` | 12/12 PASS |
   | deck-refresh.agent.md | `showcase/deck-refresh-reproduce-sh/` | 12/12 PASS |
   | deck-to-symlink-to-snapshot.agent.md | `showcase/deck-symlink-snapshot-reproduce-sh/` | 13/13 PASS |
   | deck-remove.agent.md | 包含在 deck-add-reproduce-sh 中 | — |

4. **Refresh reproduce.sh 的关键发现**
   - 成功复现外部代理报告的两个已知问题：
     - behind count 高估（1 commit → "2 behind"）
     - monorepo 报告反直觉（1 Updated + 1 Up-to-date for same repo）
   - 两个 issue 已登记 TASK-20260529132734903

5. **Symlink/Snapshot reproduce.sh 的 "tar" 采样策略**
   - 从真实 repo（lythoskill）`git clone --depth=1` 到 TMPDIR
   - 隔离、可复现、可更新，不触碰 ~/.agents/skill-repos

6. **外部代理报告登记**
   - `playground/lythoskill-improvement-proposal.md` → TASK-20260529132734903
   - 子任务 TASK-20260529132806774: refresh reproduce.sh with mock-git isolation

7. **Cortex 清理**
   - TASK-20260519224912252 (bare name lookup) → completed（curator find 已实现）
   - `cortex probe` 无活跃异常

### Key Decisions

- **统一风格 > 豁免复杂度**: 用户决策确认
- **ADR 不可删除**: 被推翻的决策与通过的决策同等重要，是治理样本
- **reproduce.sh 替代 .agent.md**: ADR-20260518024500631 确立的 canonical 格式
- **cold_pool 配置即隔离**: reproduce.sh 不需要 mock git，只需要指向 TMPDIR 的 cold_pool
- **"tar" 采样**: 从真实 repo 采样比 mock git 更真实，保留可更新性

### Pitfalls

- **kimi vs kimi-cli 混淆**: v0.5.0 (kimi-code) 和 v1.45.0 (kimi-cli) 是不同的二进制文件，CLI 标志不兼容
- **.agent.md 的 localhost 格式过期**: `localhost/skill-x` 与当前 FQ 规则 `localhost/me/<skill>` 不兼容
- **refresh --exec 风险**: 会实际 git pull 用户的 cold pool，测试必须用隔离 cold_pool

### Next Steps

1. TASK-20260529132734903: 修复 behind count + monorepo 报告（设计决策 pending）
2. TASK-20260529132806774: refresh reproduce.sh 已完成，可关闭或合并到父任务
3. 考虑将 reproduce.sh 模式推广到其他包（curator, arena）

### Temp Artifacts

- 无
