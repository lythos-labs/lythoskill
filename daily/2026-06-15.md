# 2026-06-15 Daily Handoff

## Ground Truth

**Git**: HEAD = `8a975de` (docs(adr): add onboarding impact and AGENTS.md follow-up to ADR-20260616000939948)
**Version**: v0.17.0
**Deck**: 14 skills linked
**Branch**: main, dirty working tree (`skill-deck.lock` modified, 4 new task files untracked), ahead of origin by 9 commits
**Active Epic**: 1 (EPIC-20260615211529145)
**Active Task**: 0 in-progress, 3 in review, 8 in backlog (including 4 new onboarding friction tasks)

---

## Session Handoff (afternoon) — AGENTS.md v2.1 onboarding friction audit

### 0. Verify Current State

```bash
git diff 8a975de --stat
git status --short
git log --oneline -3
```

### Completed

1. **AGENTS.md v2.1** (commit `7cca90b`, `6f6037a`, `598567a`)
   - Merged §0.5 "Onboarding for New Agent" into Boot First as "After executing — now read"
   - Added probe drift guidance: "drift after commit is expected, use git log to judge"
   - Added "Plan must include research" as Hard rule #8
   - Rewrote CPTSD "When Internal Signals Fire" with TDD/Diagnose connection
   - Added Z4 §5 "Arena at a Glance" with 3 patterns + HATEOAS/dormancy notes
   - Renumbered Z4 sections 6-11
   - AGENTS.md: 695 → 728 lines (+33 net)

2. **ADR-20260616000939948 accepted** (commits `101b247` → `8a975de`)
   - Split `skill-deck.lock` (declarative, git-tracked) from `skill-deck.state` (operational, git-ignored)
   - `.lock`: relative paths only, idempotent (only changes when content hash changes)
   - `.state`: absolute paths, timestamps, runtime decisions
   - Onboarding impact documented: "Boot First produces clean git status"

3. **ZK Review A/B execution testing** (no code change, methodology validation)
   - Spawned zero-knowledge agents with bash access to execute Boot First commands
   - v2.1 agent scored 8/10 (vs 7/10 for v2.0)
   - Key improvement: agent did NOT auto-fix probe drift, used git log to investigate
   - Agent showed exploration spirit: "I would move the file back OR investigate when/why 'done' was recorded"

4. **4 onboarding friction tasks registered** (backlog)
   - TASK-20260616002855133: skill-deck.lock drift after deck link (code implementation)
   - TASK-20260616002926373: Bun prerequisite not stated (doc, 1-line fix)
   - TASK-20260616002953212: probe output format not shown (doc, example block)
   - TASK-20260616003015674: daily handoff format unknown (doc, structure guide)

### Key Decisions

- **Merge §0.5 into Boot First**: ZK execution test revealed agent executes §0 then never reads §0.5. "After executing — now read" keeps flow continuous.
- **Agent autonomy over hand-holding**: "Don't auto-fix drift without understanding why" + "Use git log to see what changed" — encourages exploration, not dependency.
- **ADR before implementation**: skill-deck.lock split is architecture-level, needs ADR before code changes. Accepted same day.
- **Relative paths in lock, absolute in state**: Cross-machine consistency for lockfile, local debugging for state file.

### Pitfalls

- **Pitfall: "ZK Review prompt told agent it had bash ability, but first prompt didn't"**
  - Wrong approach: First A/B test was pure reading test ("Read the file, then answer questions")
  - Symptom: Agent "read" but didn't "execute" — asked "what if bun not installed?" instead of running `which bun`
  - Fix: Second A/B test explicitly said "You have a bash shell. Execute the commands."
  - Root cause: Prompt design determines agent behavior more than doc content. "Read and explain" ≠ "Execute and report"
  - Time wasted: ~10 minutes of irrelevant reading-test results

- **Pitfall: "skill-deck.lock field归属反复摇摆"**
  - Wrong approach: Initially assumed working_set/cold_pool were runtime派生, put them in .state
  - Symptom: User corrected — they come from skill-deck.toml [deck] section
  - Fix: Re-classified as declarative (from TOML), but kept in .lock with relative paths
  - Root cause: Didn't read skill-deck.toml before classifying fields
  - Time wasted: ~5 minutes

- **Pitfall: "创建重复task"**
  - Wrong approach: Used `cortex task` twice for same skill-deck.lock drift topic
  - Symptom: Two identical task files in backlog
  - Fix: `rm` the duplicate
  - Root cause: Didn't check existing tasks before creating new one
  - Time wasted: ~2 minutes

### Next Steps

1. **P0: Implement skill-deck.lock / skill-deck.state split** (TASK-20260616002855133)
   - `packages/lythoskill-deck/src/link.ts`: write .state always, .lock only when content changes
   - Add `skill-deck.state` to `.gitignore`
   - After implementation: remove drift warning from AGENTS.md Boot First

2. **P1: AGENTS.md doc fixes** (3 tasks, can batch)
   - Bun prerequisite (TASK-20260616002926373)
   - Probe output example (TASK-20260616002953212)
   - Daily handoff structure (TASK-20260616003015674)

3. **P2: Push current commits** — 9 commits ahead of origin, includes AGENTS.md v2.1 + ADR

### Temp Artifacts

- `/tmp/agents-v2.0.md` — ZK test material, can delete
- `/tmp/agents-v2.1.md` — ZK test material, can delete

---

## Work Log

- 15:30 — AGENTS.md v2.1 implementation (onboarding flow, plan research, CPTSD rewrite, Arena)
- 16:00 — ZK execution test revealed §0.5 position problem
- 16:05 — Merged §0.5 into Boot First, added probe drift guidance
- 16:30 — ADR-20260616000939948 proposed, accepted, refined with field-level schema
- 17:00 — 4 onboarding friction tasks registered to EPIC-20260615211529145
- 17:30 — Session close, scribe daily
