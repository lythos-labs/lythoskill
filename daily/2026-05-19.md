# Daily — 2026-05-19

## Ground Truth

**Git**: HEAD = 22d628f (deck: add project-scribe-weekly skill)
**Version**: v0.14.6 (published to npm)
**Deck**: 12 skills, skill-deck.toml
**Tests**: 385+ pass, 0 fail across all changed packages
**Kanban**: 0 active epic, 4 backlog tasks, 2 completed today

## Session Handoff — Env Var Cleanup + Symlink Pollution修复 + Weekly重建验证

### Completed

1. **Symlink pollution cleanup** (`64d0d41`)
   - Fixed working_set back to `.claude/skills` (was agent-changed to `"skills"`)
   - Removed 17 committed symlinks: `.agents/skills/` × 15 + `skills/diagnose` + `skills/tdd`
   - Added husky pre-commit guard: blocks symlinks + extraneous entries in `skills/`
   - Added defensive gitignore for CLI working_set directories
   - Rebuilt all 12 skills

2. **Env var rename + cleanup** (`0ad8003`, `adf96a7`, `df5cc2f`)
   - `LYTHOSKILL_GH_MIRROR` → `LYTHOS_GH_MIRROR` (backward compat with deprecation warning)
   - Removed `LYTHOS_COLD_POOL` env var (superseded by deck.toml + `--pool`)
   - Documented `LYTHOS_GH_MIRROR` / `LYTHOS_SOCKS_PROXY` / `LYTHOS_GIT_PROTOCOL` in README.md + deck SKILL.md
   - Bumped v0.14.6, published to npm

3. **Cortex登记**
   - ADR-20260519144445916 accepted: working_set/build_output分离
   - ADR-20260517152850372 revised: also_link_to格式→TOML array
   - ADR-20260519144500000 accepted: LYTHOS_COLD_POOL移除
   - TASK-20260519144445916 completed: symlink清理
   - TASK-20260519144500000 completed: LYTHOS_COLD_POOL移除

4. **Added project-scribe-weekly to deck** (`22d628f`)

5. **W20 weekly重建验证** — 确认CQRS机制健壮性
   - `weekly/2026-W20.md` 缺失，通过 daily × 6 + git log × 29 + cortex date-grep 成功重建
   - 验证了 cortex timestamp ID 命名 (`ADR-yyyyMMdd...`) 支持 `ls | grep` 日期范围搜索
   - 将此机制写入 weekly scribe + onboarding skill 描述

### Pitfalls

- **working_set = "skills" 是agent疲劳期产物**：与thin pattern的build output目录冲突，导致deck link覆盖build output。已用husky guard防御。
- **.agents/skills/ 被提交到git**：15个符号链接指向cold pool绝对路径，对clone者无用。已清理并加gitignore。
- **LYTHOSKILL_GH_MIRROR前缀不统一**：3/4 env var用`LYTHOS_`，只有GH_MIRROR带`KILL`。已rename，legacy仍兼容。
- **我误将project-scribe-weekly当localhost skill**：用户纠正——项目自身技能应用GitHub locator。已修正。

### Next Steps

- also_link_to实现（Task #5，已规划，待启动）
- W20 weekly写入 `weekly/2026-W20.md`（draft已确认，待写入）

### Verify Current State

```bash
git status --short          # Should be clean
git log --oneline -3        # 22d628f, df5cc2f, 0ad8003
```
