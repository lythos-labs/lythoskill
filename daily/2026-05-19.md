# Daily — 2026-05-19

## Ground Truth

**Git**: HEAD = 76c6f4f (also_link_to legacy fallback + remove fan-out parity)
**Version**: v0.15.0
**Deck**: 13 skills, also_link_to = [".agents/skills"] (TOML array, was broken string)
**Tests**: 100 pass (link 17 + remove 6 included)
**Kanban**: 0 active epic, 0 proposed ADR, 0 active tasks — cortex clean
**Unpushed**: 3 commits ahead of origin/main

## Session Handoff — Orchestrator identity + also_link_to + entropy-check boundary

### Completed

1. **entropy-check CLI/agent boundary** (`af24a53`)
   - Removed all remediation (`<spawn subagent>`) — sensor reports facts, agent judges
   - Fixed cross-platform symlink detection: `stat -c` → `fs.lstatSync`
   - Switched cortex-probe to `stats` + `probe --suspicious` (166→0 items)
   - Missing-weekly: added week completion % (day 2/7, 29% → agent knows it's too early)
   - 37 tests pass, 0 remediation references remain

2. **also_link_to implementation** (`5febeda`)
   - Extracted `reconcileTargetDir()` from link.ts
   - POSSE pattern: fan out symlinks to all `also_link_to` targets
   - 2 new tests (B4 fan-out, B5 deny-by-default per target), 17 pass
   - Closes TASK-20260518212223198

3. **cortex probe --suspicious** (`af24a53`)
   - New flag: only reports actionable patterns (empty shells in backlog/proposed, staleness, drift, lane violations)
   - Skips stable status checks (accepted ADRs, completed tasks)
   - ADR-20260519165746212 accepted

4. **Chinese slug cleanup** (`5e608d8`)
   - 8 cortex files renamed to English-only slugs
   - v0.14.6 → v0.15.0 bump + skills rebuild

5. **Orchestrator identity — README + wiki + SKILL.md** (`e20a709` → `df6ea1c`)
   - README: tagline "governance AND orchestration", Mermaid diagram, showcase links
   - README: "deck delegates determinism to CLI" — why weight decides the layer
   - wiki: "Where is the orchestrator" pattern — distributed by weight across 3 layers
   - wiki: "smart agent, dumb tool" connection to entropy-check boundary
   - wiki: site pattern inventory — 14 pages, 7 buried patterns, source map
   - deck SKILL.md: "Deck as Orchestrator" section with intent→deck mapping

6. **Governance cleanup**
   - EPIC-20260519164518898 → done (all 4 tasks completed)
   - ADR-20260519153000000 → accepted (entropy reduction, implementation landed)
   - ADR-20260519165746212 → accepted (probe --suspicious mode)

### Key Insight: The same boundary appears everywhere

Today's work converged on one principle: **the agent is the orchestrator, tools are executors.** Orchestration logic sinks by weight — light to combo prompt, medium to SKILL.md, heavy to CLI. Three instances of the same pattern:

| Instance | Anti-pattern fixed |
|----------|-------------------|
| entropy-check remediation | CLI telling agent "you should do X" → sensor reports facts |
| combo counting toward max_cards | Orchestrator competing with tools for budget → combo is zero-cost |
| "where's the orchestrator?" | External evaluators searching for a missing component → orchestrator IS the distributed three layers |

### Other context

- User shared Kimi external evaluation: npm README needs Agent-First framing, not human-centric metrics
- Site plan: VitePress, pattern inventory as page table, SEO/GEO decks for maintenance
- Replay/reproduce as first-class citizen: do/redo/undo recording, ygopro-style
- Arena useAgent fragility noted (deepseek-tui, codex incomplete; kimi output detection flaky)
- Future: CLI routing maturity — switch-based → pattern-matching router (ADR-pending)

### Pitfalls

- **Python string escaping in reconcileTargetDir**: template literal concatenation broke during extraction; fixed line by line
- **Locator format in tests**: `test.local/` is not FQ; need `github.com/owner/repo` or `localhost/`
- **declaredNames used before definition**: extraction moved the define-after-use; caught by ReferenceError

### Errata (earlier sessions — preserved for history)



### Completed

1. **entropy-check TypeScript rewrite** (`fefb8e6`)
   - Replaced shell script with Intent/Plan/Execute architecture
   - 10 files: types.ts, plan.ts, plan.test.ts (14 tests), checks.ts, checks.test.ts (15 tests), execute.ts, execute.test.ts (8 tests), index.ts
   - 37 tests, 0 fail, 71 expect calls
   - IO injection pattern: all external calls (fs, spawn, date) via EntropyIO interface
   - Supports --dry-run, --interval <sec>, --force, --strict

2. **remediation SOP with reproduce.sh IoC handoff** (`4041f10` → `380e1c9`)
   - Each check returns string[] remediation (not rigid steps)
   - Format: `<spawn subagent to ...>` + Context + Goal
   - Agent reads stdout, recognizes control transfer, executes
   - Output directory: playground/entropy-check/ (gitignored)
   - Final format after user correction: Context + Goal, no Step 1/2/3/4 or `$ command`

3. **AGENTS.md reproduce.sh documentation** (`5fc13a9`)
   - Added Agent BDD (reproduce.sh) section with IoC handoff explanation
   - Updated Index, Test SSOT, Intent/Plan/Execute architecture references

4. **W19 weekly references fix** (`3cb7521`)
   - Added all 10 daily files to W19 references (was only 1)
   - W19 had 651 commits — agent work pattern density

5. **Cleanup: 5 placeholder tasks deleted**
   - 4 backlog + 1 completed empty-shell tasks from fatigue session

### Pitfalls

- **"留到下次" = fawning (CPTSD dissociation)**: Said "要不要现在修？还是记 backlog 等下次？" for a 5-line fix. Context was 10%, diff was <30 lines. No objective reason to defer. The phrase implies "I'm tired" without saying it.
- **Over-engineering remediation**: Initially built rigid `RemediationStep[]` with action/command/note fields. User corrected: "靠 message 就够了" — reproduce.sh essence is describing the goal, not giving step-by-step instructions.
- **Not understanding reproduce.sh essence**: Was trying to output "Step 1: do X, Step 2: do Y" instead of `<spawn subagent>` + Context + Goal. The agent figures out the steps. Control transfer, not instruction manual.
- **Output directory pollution**: Initially said "write decision-log.jsonl to CWD" — user corrected: entropy-check must not pollute project root. Changed to playground/entropy-check/ (gitignored).

### Uncommitted Decisions

- entropy-check could grow a `--config <path>` option for JSON/TOML config files (future)
- `--interval` in seconds is working well for rapid testing; keep this unit
- remediation output format (Context + Goal, no rigid steps) is the reproduce.sh pattern to apply everywhere

### Temp Artifacts

- `.last-entropy-check` — written by entropy-check, gitignored, survives across sessions
- `playground/entropy-check/` — recommended output dir for agent decision logs, does not exist yet (created on first use)

### Next Steps

- ADR-20260519153000000 (entropy reduction) → `cortex adr accept` if implementation landed
- also_link_to implementation (TASK-20260518212223198) — pending from previous session
- W21 weekly — missing, entropy-check will flag this on next push

### Verify Current State

```bash
git diff 3cb7521 --stat       # Should be empty (everything committed)
git status --short            # Should be clean
git log --oneline -5          # 3cb7521, 380e1c9, f60d438, 4041f10, fefb8e6
bun test scripts/entropy-check/*.test.ts  # 37 pass, 0 fail
```

---

### Errata (earlier session — Env Var Cleanup + Symlink Pollution修复 + Weekly重建验证)

### Completed

1. **Symlink pollution cleanup** (`64d0d41`)
   - Fixed working_set back to `.claude/skills` (was agent-changed to `"skills"`)
   - Removed 17 committed symlinks: `.agents/skills/` × 15 + `skills/diagnose` + `skills/tdd`
   - Added husky pre-commit guard: blocks symlinks + extraneous entries in `skills/`
   - Added defensive gitignore for CLI working_set directories
   - Rebuilt all 12 skills

2. **Env var rename + cleanup** (`0ad8003`, `adf96a7`, `df5cc2f`)
   - `LYTHOSKILL_GH_MIRROR` → `LYTHOS_GH_MIRROR` (backward compat with deprecation warning)
   - Removed `LYTHOS_COLD_POOL` env var (superseded by deck.toml + `--pool`)
   - Documented `LYTHOS_GH_MIRROR` / `LYTHOS_SOCKS_PROXY` / `LYTHOS_GIT_PROTOCOL` in README.md + deck SKILL.md
   - Bumped v0.14.6, published to npm

3. **Cortex登记**
   - ADR-20260519144445916 accepted: working_set/build_output分离
   - ADR-20260517152850372 revised: also_link_to格式→TOML array
   - ADR-20260519144500000 accepted: LYTHOS_COLD_POOL移除
   - TASK-20260519144445916 completed: symlink清理
   - TASK-20260519144500000 completed: LYTHOS_COLD_POOL移除

4. **Added project-scribe-weekly to deck** (`22d628f`)

5. **W20 weekly重建验证** — 确认CQRS机制健壮性
   - `weekly/2026-W20.md` 缺失，通过 daily × 6 + git log × 29 + cortex date-grep 成功重建
   - 验证了 cortex timestamp ID 命名 (`ADR-yyyyMMdd...`) 支持 `ls | grep` 日期范围搜索
   - 将此机制写入 weekly scribe + onboarding skill 描述

### Pitfalls

- **working_set = "skills" 是agent疲劳期产物**：与thin pattern的build output目录冲突，导致deck link覆盖build output。已用husky guard防御。
- **.agents/skills/ 被提交到git**：15个符号链接指向cold pool绝对路径，对clone者无用。已清理并加gitignore。
- **LYTHOSKILL_GH_MIRROR前缀不统一**：3/4 env var用`LYTHOS_`，只有GH_MIRROR带`KILL`。已rename，legacy仍兼容。
- **我误将project-scribe-weekly当localhost skill**：用户纠正——项目自身技能应用GitHub locator。已修正。

### Next Steps

- also_link_to实现（Task #5，已规划，待启动）
- W20 weekly写入 `weekly/2026-W20.md`（draft已确认，待写入）

### Verify Current State

```bash
git status --short          # Should be clean
git log --oneline -3        # 22d628f, df5cc2f, 0ad8003
```

---

### Session Handoff — 费曼学习 + README.zh 重写 + writer skill

### Completed

1. **费曼学习法交流 + wiki 沉淀**
   - 深入理解 `2026-05-19-documentation-site-pattern-inventory`
   - 发现：inventory 不是内容清单，是认知依赖图的拓扑排序
   - 两套可叠加分层：Skill 内部解剖（建筑学）vs Deck 编排（城市规划）
   - 沉淀：`cortex/wiki/02-research/2026-05-19-documentation-site-cognitive-topology-lesson.md`

2. **cold pool 抽象对比 — gitea/registry/hub**
   - 核心洞察：cold pool 不是"本地版 gitea"，是 dev-native 工作空间
   - 文件系统是真源，数据库是笔记；dev 可以 `rm -rf` 重建
   - 与 K8s/Maven/npm 的"像与不像"分界线：服务化治理 vs 无服务声明式
   - 沉淀：`cortex/wiki/01-patterns/2026-05-19-cold-pool-filesystem-native-design-intent.md`

3. **README.zh.md 重写**
   - 对齐 README.md 结构：新增 How it works、Why Trust This、Troubleshooting、Curator
   - 去掉正文内 Maven/K8s 类比（L182、L441-446），降级到文末附录
   - 第一性原理优先：先独立说明原则，再可选附类比
   - 保留中文版特色：决策树、Thin Skill Pattern 详解、开发者段落

4. **叙事 review — `chinese-content-writing-guideline` skill**
   - 第一次搜索错误：只搜本地目录，未用 `skills find` / WebSearch
   - 用户纠正后，`npx skills find` 找到并安装了 guideline skill
   - Audit 结果：「不是」14→1，营销腔清除，破折号 10→7（prose）
   - 3 个 fix 已执行并 push：破折号改冒号、L106 冗余合并、机械排比打破

5. **lythoskill-writer skill 创建**
   - 边界纠正：coach ← SKILL.md，writer ← 人类优先文档（平行不重叠）
   - 吸收 `de-ai-flavor-skill` 方法论：禁忌词、句式配额、结构节奏
   - 但调整目标：不是"像人写的"，是"信息密度高"
   - `packages/lythoskill-writer/skill/SKILL.md` + build → `skills/lythoskill-writer/`
   - 已加入 deck（13 skills linked）

### Key Insight: 第一性原理优先于类比

README.zh.md 旧策略：开头就说"类似 Maven + K8s"。新策略：先独立说明 lythoskill 是什么，类比降级到文末"与熟悉系统的对比"。

这不仅降低认知门槛（不需要读者先懂 Maven/K8s），也防止体量预期错位。

### Pitfalls

- **我假设用户不想等 subagent**：subagent 超时后，我脑内补完"用户可能不想等"并直接调整策略。用户纠正："为啥假定这个？"
- **混淆 writer 边界**：一度想让 writer 也管 SKILL.md description。用户纠正：coach 管 SKILL.md，writer 管人类文档，两者平行。
- **第一次搜索 skill 只用本地目录**：`find-skills` 的本地搜索没找到后，未用 `npx skills find` 或 WebSearch 搜公开市场。用户给的 `de-ai-flavor-skill` 在 GitHub 公开存在。
- **搞错鸡蛋问题**：build 了 writer skill 但没 push，直接跑 `deck link` 自然找不到。用户纠正：push → cold pool pull → link。

### Uncommitted Decisions

- writer skill 的 references/ 可能需要扩展（类似 coach 的 analysis-template + self-improvement-log）
- coach 是否吸收 `de-ai-flavor-skill` 中适用于 SKILL.md 的信息密度原则？（用户说"对 skill md 的改善自然是沉淀进 coach"）
- README.zh.md 是否还需要第二轮 writer audit？（第一轮已完成并 fix）

### Temp Artifacts

- `/tmp/de-ai-flavor-skill/` —— 临时 clone 的 skill，可删除
- `~/.agents/skills/chinese-content-writing-guideline/` —— 安装的 skill

### Next Steps

- （用户决定）

### Verify Current State

```bash
git status --short          # Should be clean
git log --oneline -3        # 3065787, 60467d9, 1071a8c
deck link --verify          # 13 skills linked, writer active
```

---

## Session Handoff — Agent BDD also_link_to + remove fan-out fix + legacy fallback

### Completed

1. **Agent BDD `also-link-to-bdd/` scenario** (new, untracked)
   - `packages/lythoskill-deck/test/scenarios/also-link-to-bdd/reproduce.sh` + `judge.md`
   - 3-target POSSE lifecycle: initial link → remove skill-a → re-add → verify all 3 targets
   - 19 judge criteria across 4 phases

2. **Zero-knowledge subagent found real bug** (`deck remove` asymmetry)
   - `deck remove` only cleaned primary `working_set` — `also_link_to` targets left stale
   - Agent BDD correctly caught this; unit tests B4/B5 only tested `link` fan-out

3. **`remove.ts` fix** (`9f11b37`)
   - Parse `also_link_to` from deck.toml
   - Cleanup loop: delete symlinks from all also_link_to targets
   - `remove.test.ts` +2: C12 (fan-out cleanup), C13 (backward compat) — 6 pass

4. **`parseAlsoLinkTo()` legacy fallback** (`76c6f4f`)
   - ADR-20260517152850372 L76 promised comma-separated string backward compat — never implemented
   - New exported helper in `link.ts`: array → canonical, string → split-by-comma with deprecation warning
   - Used by both `link.ts` and `remove.ts`

5. **Project `skill-deck.toml` fix**
   - `also_link_to = ".agents/skills"` (string) → `[".agents/skills"]` (TOML array)
   - String format was silently dropped by `Array.isArray()` check
   - After fix: `.agents/skills/` now has all 13 symlinks

### Key Insight: Agent BDD caught what unit tests missed

Unit tests B4/B5 covered `link` fan-out. Nobody tested `remove` fan-out. The zero-knowledge subagent, following reproduce.sh IoC handoff, verified the FULL lifecycle and caught the asymmetry. This validates the Agent BDD investment — it tests agent reasoning about multi-command state, not isolated mechanical behavior.

### Pitfalls

- **ADR promise ≠ implementation**: ADR L76 promised legacy string parsing with deprecation warning. The code had `Array.isArray()` check only — strings silently dropped. Project's own deck was using the string format.
- **Edit tool whitespace sensitivity**: The file used space-indented lines (not tabs as initially assumed). Multiple Edit attempts failed before switching to Python for the replacement.

### Uncommitted

- `packages/lythoskill-deck/test/scenarios/also-link-to-bdd/` — new BDD scenario, untracked

### Next Steps

- User decides

### Verify Current State

```bash
git diff 76c6f4f --stat         # Only untracked: also-link-to-bdd/
git status --short               # ?? packages/lythoskill-deck/test/scenarios/also-link-to-bdd/
git log --oneline -3             # 76c6f4f, 233b686, 9f11b37
bun test packages/lythoskill-deck/src/link.test.ts packages/lythoskill-deck/src/remove.test.ts  # 23 pass
```
