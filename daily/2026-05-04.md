# 2026-05-04

## Session Handoff ⭐(状态快照 @ 2026-05-04 后段 commit `59e3241` 后 — 最新)

> **⚠️ 顶部 = 当下真相。下方多个历史快照段按时间从早到晚,有冲突以本段为准。**
> **如果验证命令的输出与本段不一致 → daily 过期,立即覆盖此段(不要只追写末尾)。**

```bash
# 验证(输出应与下方 Ground Truth 一致)
git status --short                          # 应为空(本段 commit 已落)
git log --oneline -3                        # 顶端见下表"最近 commit"
ls cortex/tasks/01-backlog/ | wc -l         # 应为 7(T3-T6 + T8-T9 + 1 张 refresh bug 卡)
ls cortex/tasks/02-in-progress/ | wc -l     # 应为 2(T2 + T7)
ls cortex/tasks/03-review/                  # 应为空
ls cortex/tasks/04-completed/ | grep -c TASK-20260503235008935  # 应为 1(T1 已落)
```

### 项目身份

- **项目**: lythoskill — thin-skill monorepo scaffolding + cortex governance OS
- **技术栈**: Bun + TypeScript + Bun workspaces + ESM-only
- **当前分支**: `main`(领先 `origin/main` **2 个 commit**:`59e3241` + `8350f7f`;`48fec23` + `1616563` 已被那边 push 到 origin/main,push 时机由用户定)
- **最近 commit**:
  - `8350f7f docs(daily): post-commit hygiene patch — sync top to 59e3241 + clean tree`(本段)
  - `59e3241 chore(cortex): pull T2/T7 to in-progress + refresh daily handoff`(本段)
  - `48fec23 chore(deck): migrate skill-deck.toml to alias-as-key dict schema`(用户旁路,**已 push**)
  - `1616563 docs(cortex): register deck refresh monorepo git-root bug (TASK-20260504012457126)`(用户旁路,**已 push**)
  - `1dfa1de chore(cortex): complete TASK-20260503235008935`(T1 trailer auto-flow,已 push)
- **当前 monorepo 版本**: `0.9.0`

### Ground Truth State(@ 后段 onboarding 复盘 + pull T2/T7 后)

| 维度 | 状态 |
|------|------|
| working tree | **clean**(本段 commit 已落,旧 .bak 已 rm) |
| `main` vs `origin/main` | **领先 2 个 commit**(`59e3241` + `8350f7f` 待 push;`48fec23` + `1616563` 已被那边 push)— 用户决定 push 时机 |
| `skills` branch | pre-commit hook 自动同步 |
| ADR 合规检查 | 提交时再跑 |
| in-progress tasks | **2**(T2 reconciler core A + T7 runClaudeAgent helper) |
| review tasks | **0** |
| backlog tasks | **7**(T3-T6 + T8-T9 + 1 张 refresh bug 卡) |
| 本 session 完成 | **T1**(`TASK-...235008935`) — 9 pass, 10 expect, test:all 21 BDD 仍绿;`Closes:` trailer 触发自动从 review → completed |
| epic lanes (active) | main: 1(deck verification,双侧 + bug 卡反向引用), emergency: 0 |
| 版本 | `0.9.0` |

### Kanban 状态栏(CFD)

| Column | Count | WIP Limit | Status |
|--------|-------|-----------|--------|
| backlog | 7 | ∞ | - |
| in-progress | 2 | 5 | ✅ 还有容量 3 |
| review | 0 | 3 | ✅ 有容量 |
| completed | 36+ | - | T1 本段落地 |

### Backlog(7 张待 pull)

**White-box(单测,无 LLM 依赖)**

| Task ID | 主题 | 依赖 |
|---------|------|------|
| `TASK-...235011219` | T3 reconciler core B(linkDeck deny-by-default + alias collision) | T2 |
| `TASK-...235012454` | T4 command layer A(validateDeck + addSkill tests) | T2 |
| `TASK-...235013705` | T5 command layer B(removeSkill/refreshDeck/pruneDeck tests) | T4 |
| `TASK-...235014489` | T6 coverage sweep,backfill 边缘 case 到 80% | T1-T5 |

**Black-box(Agent BDD,LLM in-loop,不进 CI)**

| Task ID | 主题 | 依赖 |
|---------|------|------|
| `TASK-20260504004954526` | T8 第一个 `*.agent.md`(`skills-introspection`),tracer bullet | T7 |
| `TASK-20260504005000534` | T9 add/refresh/remove/prune scenarios(4 个) | T7+T8 |

**Bug fix(用户旁路登记,可顺便在 BDD 里被发现)**

| Task ID | 主题 | 依赖 |
|---------|------|------|
| `TASK-20260504012457126` | fix `deck refresh`:traverse up to git root for monorepo skills | 无 |

### In-progress(2 张,本段已 pull)

| Task ID | 主题 | 起跑 status |
|---------|------|-------------|
| `TASK-20260503235009959` | **T2** reconciler core A(linkDeck empty + symlink creation) | 刚 pull,T1 已解锁 |
| `TASK-20260504004947351` | **T7** `runClaudeAgent` helper + checkpoint JSONL schema | 刚 pull,无依赖,可与 T2 并行 |

### 本 Session 后段累计完成

1. **后段 onboarding 识别 daily 漂移**:顶部 ground truth 停在 `c491a59` + backlog=9 + in-progress=0,实际 HEAD `1dfa1de`(3 commits ahead),T1 已经穿过 03-review 直接进 04-completed(`Closes:` trailer + post-commit dispatcher 自动推完整流程)
2. **修 daily typo**(line 208):`@lythos/skill-cortex` → `@lythos/project-cortex`(包名)。用户提示"这种 typo 在 daily 交接文档影响很大";写 memory `feedback_handoff_typos_must_be_fixed.md` —— factual error 即使在历史段也修,不当 history 留
3. **Pull T2(white-box)+ T7(black-box)进 in-progress**:WIP=2,合规;两条线无依赖可并行
4. **本 daily 顶部覆盖 + 历史段 typo 修复**:反映 T1 完成 + T2/T7 已 pull + 新增 refresh bug 卡
5. **`59e3241` 单 commit 落地**:INDEX + 2 task move + daily 顶部覆盖,顺手 rm 隔夜 .bak;ADR 合规检查全绿
6. **Post-commit daily 补丁(本步)**:用户准则"压缩和失忆随时可能发生" — 每次 commit 后必须把 daily 顶部刷成 post-commit 真相,不要让 daily 滞后于 git;memory 更新 `feedback_daily_top_overwrite_not_append.md`

### 上一日(2026-05-03)关键结转(无变化)

- 5 ADR accept、`flow` 子命令落地、leetcode bash harness reject + revert、deck TDD epic + 6 task 入 backlog,详见早段。

### CLI surface 当前状态

| Command | 状态 | 备注 |
|---------|------|------|
| `link` | ✅ 稳定 | reconciler, deny-by-default, max_cards, backup |
| `validate` | ✅ 稳定 | read-only validation |
| `add` | ✅ 稳定 | FQ path + `--as` alias |
| `refresh` | ⚠️ 已知 bug | TASK-20260504012457126:monorepo skill 路径下 traverse 没到 git root,需修;BDD 跑 refresh scenario 应能复现 |
| `remove` | ✅ 稳定 | alias/path match |
| `prune` | ✅ 稳定 | interactive confirm + `--yes` |
| `update` | ⚠️ deprecated | shim → `refresh` |
| `migrate-schema` | ✅ 稳定 | `--dry-run` + timestamped backup |
| `flow` | ✅ 稳定 | kanban CFD |

### 下一步(动作清单)

| 优先级 | 动作 | 备注 |
|--------|------|------|
| P1 | **跑 T2**(白盒):`linkDeck(emptyDeck) + symlink creation` —— 续 T1 的 tracer pattern,新增 ~50-80 行 test | 先看 T2 卡的验收标准 |
| P1(可并) | **跑 T7**(黑盒):`runClaudeAgent` helper + checkpoint JSONL schema 落进 `bdd-runner.ts` —— 不进 CI | T8/T9 解锁前置 |
| P2 | T2 完成后 → pull T3;T7 完成后 → pull T8 | 严守 WIP 限 5 |
| P3 | refresh bug(`TASK-20260504012457126`)等 BDD 跑起来后顺便复现修;不优先单独搞 | 用户原话"应该能顺便在这次 BDD 中发现" |
| P3 | Push 2 个 commit 到 origin/main(`59e3241` + `8350f7f`;旁路那 2 个已被那边 push) | 用户决定时机 |

### 接手自检(本段)

- [ ] `git status --short` → **空**(本段已 commit + 补 daily 后再 commit)
- [ ] `git log --oneline -1` 应为本段 daily 补丁 commit hash 或 `59e3241`(若未 commit daily 补丁)
- [ ] `git rev-list --count origin/main..HEAD` → **2 或 3**(看本补丁是否已 commit;self-reference 无法消)
- [ ] `ls cortex/tasks/01-backlog/ | wc -l` → **7**
- [ ] `ls cortex/tasks/02-in-progress/ | wc -l` → **2**
- [ ] `ls cortex/tasks/04-completed/ | grep TASK-20260503235008935` → 1 行
- [ ] `bun run test:all` → 全绿(T1 落地时跑过 21 BDD;本段 pull 不动 src,理论保持)
- [ ] **任何不一致 → daily 过期,立即覆盖此段**

### 为下个 session 留的 reference 句

- T1 已经穿过 03-review 进 04-completed:`Closes:` trailer + post-commit dispatcher 自动推完整流程,**别误以为还在 review**
- 写 daily/CLI 命令时核包名:`@lythos/project-cortex`(不是 `skill-cortex`),memory 已存 typo 防御
- T2 和 T7 是双轨:T2 续 T1 的 tracer pattern,T7 是黑盒 BDD 入口;两边谁先跑都行
- `refresh` 已知 bug → BDD 跑到 refresh scenario(T9 一部分)就会触发,顺便修
- bump → `AGENTS.md → Release & Auth Workflow → Lock-step versioning`
- git remote / .ssh → **不要动**,问用户
- daily 大段更新 → **覆盖顶部,不只追写末尾**(本段已遵守);**factual typo 即使在历史段也修**(本段第一次执行);**每次 commit 后必须 post-commit 补 daily**——压缩和失忆随时可能发生,daily 滞后 = 下个 agent 直接读到错的 ground truth
- 板子 pull 模式 → 拉式,严守 WIP 限

---

> **以下为历史续接段(早到晚),记录 session 过程中的详细决策。**
> **如顶部和下方矛盾,以顶部为准(顶部是当下真相,下方是历史)。**

---

## Session Onboarding(2026-05-04 早,跨日)

### 进入时状态

- 上一日 daily(`daily/2026-05-03.md`)顶部 HEAD=`138fe51`、backlog=0,已过期
- 实际 HEAD=`3da4e7d`,backlog=6,工作树有 1 个未提交改动(ADR rejection 详情)
- 用户:"嗯。这个顺序非常正确。先1和2吧。然后其实已经跨日了我觉得"
  - 1 = commit ADR rejection
  - 2 = 写新 daily(跨日 → 新建,不覆盖昨天)

### 本段做了什么

1. **`b286a1a` docs(adr): document leetcode harness rejection rationale + replacement plan**
   - 32 行 insertion 到 ADR-20260503230522270(已在 03-rejected/)
   - "Why Rejected" 段引用用户原话: "明明可以用ts,学之前的东西吧" / "东西竟然沉淀进不在git管理的playground"
   - "Replacement" 段定下走既有约定的方案(SCENARIOS.md + bdd-runner.ts + `*.agent.md`)
   - pre-commit ADR 合规检查全绿

2. **`daily/2026-05-04.md` 创建**(本文件)
   - 跨日,不覆盖 2026-05-03 的顶部
   - Ground Truth 反映当前真相:HEAD=`b286a1a`,工作树干净,backlog=6

### 教训(候选,待沉淀进 wiki/lessons)

- "压缩 + 没好好 onboarding = 平行世界事故" —— leetcode harness 的 process failure 是真实例:某个 session 没读 `SCENARIOS.md` 既有约定,自己起了 bash 平行世界,沉淀进了 `playground/`(不在 git 管理的目录),用户压缩后续接发现并 reject
- ADR-B(epic-granularity)的 "task = subagent bootloader" 原则要求 task 卡 self-contained;同理,**SCENARIOS.md / 既有 runner 是 session-survivable 的真理来源**,memory 是辅助
- 本 session 的对照组:onboarding 第一步就读了 daily 顶部 + git 实测 + 识别漂移,避免了重蹈覆辙

---

## Session 续接(早段后,概念澄清 + epic 扩展)

> **元观察**:本段是上面 onboarding 段的续写。我在 onboarding 时把"leetcode-style 工作成果检查"这个 pattern **也连带否定了**——这是错读。用户连续几条消息纠正,产出三层模型 + epic 双侧扩展 + wiki/lessons。

### 用户校正轨迹(逐条)

1. "我们的确要做 agent 各种 deck 操作场景。为了能拉起 agent 做,我的确建议从 shell 去避免 subagent 的各种限制(比如 skill 初始化)" —— shell 是必要的(为解锁 subagent 限制),但
2. "我一直就是在说用 bun shell" —— Bun Shell(`Bun.$`),不是 bash 脚本
3. "增强 test-utils" —— 扩 `bdd-runner.ts`,不平行世界
4. "因为 LLM 自己能推理能做判断——所以我才说" —— LLM judge,不脚本判官
5. "(那有点想 leetcode 交接工作成果检查(其实已经是 arena 的做法了)" —— pattern 本身是对的,arena 已经在做
6. "我们一直在讨论怎么检查 agent 要要求做对了,所以联想到 leetcode 也挺自然的?" —— 验证 metaphor 合理
7. "为了检查方便,还是可以要求 arena 那样输出必要产物作为 json/jsonl as metadata 方便看的" —— substrate 是结构化产物
8. "其实就是 checkpoint(我做完了,位置在,你看)" —— substrate 的口语化定义:不是冷数据,是自陈+定位+邀请验证
9. "因为行动输出 report 增强可视化和可 review 的确好" —— 价值确认

### 浮现的三层模型

| 层 | LeetCode | Agent BDD | 能否照抄 |
|---|---|---|---|
| Pattern | submit code → judge → AC/WA | submit agent run → judge → pass/fail | ✅ |
| Substrate | hidden test cases | **checkpoint JSONL + fs state** | ❌ 换衬底 |
| Judge | 脚本 | **LLM** | ❌ 换判官 |

原 ADR 抄对 pattern,**但中间一格选错衬底**(把 substrate 抄成"hidden tests" → judge 也跟着退化成 bash 脚本)。

### 本段做了什么

1. **Edit `EPIC-20260503234346583`**: 标题改 "Verification coverage(TDD unit + Agent BDD with leetcode-shape + LLM judge)";背景故事拆双侧;新增主题 D(Agent BDD,T7-T9 placeholder);技术决策表加 ADR-20260503230522270 反例引用;归档条件双侧化
2. **Write `cortex/wiki/03-lessons/2026-05-04-agent-verification-leetcode-shape-llm-judge.md`**: 三层模型 + 反例 + 5 条 checklist
3. **Update 本 daily**: Ground Truth 反映 epic 扩展;下一步加 T7-T9 创建动作;append 本续接段
4. **(待办)cortex CLI 创建 T7-T9 task 卡**:不在本段做,等 commit 完之后或下一 session

### 续接段下一步

1. 单 commit 把"epic 扩展 + wiki/lessons + 本 daily 更新"一起入库,commit message 用 `feat(cortex): expand deck verification epic to two-sided scope (TDD unit + Agent BDD)`
2. 后续 session(或本 session 继续):用 `bunx @lythos/project-cortex task` 把 T7-T9 落到 backlog,然后从 T1 或 T7 开 pull
3. push 时机由用户定

### 续接段元教训

- **Pattern 抄对一半最危险**:错的不是 metaphor,是没识别 metaphor 中"哪一格不能搬"。下次设计验证机制时,wiki/lessons 那张 5 条 checklist 是入门题
- **用户连续 6-9 条消息逐步澄清** = 这是核心概念(verification 三层模型),值得一次性沉淀进 wiki,不能只散落在 daily 历史段
- **Checkpoint 这个词比 metadata 准确**:metadata 是冷数据观感,checkpoint 是 agent 主动留的"我做完了/位置在/你看",含语用色彩,能解释为什么 review 一眼看 5 行 JSONL 就够
- **Arena 早就在做这件事**:agent-autonomous-arena.md 的 "Judge = Agent, Agent = Judge" + test-play-model.md 的 deck duel,本来就是 leetcode-shape with LLM judge。本 session 只是把 arena 已经在做的事识别出来、落进 SCENARIOS.md 工具链 + 写进 wiki

---

## Session 续接(下半场:失忆诊断 → loop 调度 → T7-T9 落卡)

> **元观察**:上半场结尾,用户问"这个你也记得,对吗"(指 /tdd skill);我承认 leetcode harness ADR 是失忆产物。本段把"失忆"从单点错误升成可持续防御机制 + 把 epic 留位的 T7-T9 落实成卡。

### 失忆诊断 + memory 沉淀

1. **用户提出"裸着 = 失忆"自检准则**:"如果你自己是裸着的,连 skill 都不记得,说明你可能已经失忆"——记忆失败的唯一信号是 agent 自己的 state,不是外部通知
2. **写 memory `feedback_post_compaction_skill_amnesia_taxonomy.md`**:
   - 把"压缩失忆"升为项目 #1 enemy
   - 列举 leetcode harness 事故里同时掉的 5+ anchors(tdd / SCENARIOS.md / ADR-B / ADR-180000000 c4 / ADR-24812645 / AGENTS.md)
   - 沉淀 trust hierarchy:**working-tree-self-describing files > daily > memory > deck-install > AGENTS.md 内容回忆**
   - 标记最坏变种 = silent / in-loop / scheduled(`<<autonomous-loop>>` 类)
3. **MEMORY.md 加 1 行索引**

### Loop-based 周期复活机制(候选 → 已开试)

- 用户提议:`/loop 30m /lythoskill-project-onboarding ...`,周期重新唤起 onboarding,把 silent amnesia 变成"周期检测+自愈"
- 本 session 实际跑了:cron `855945b5`(`*/30 * * * *`),prompt = `/lythoskill-project-onboarding 重点核对 skill-deck.toml 与 .claude/skills/ 软链状态;若发现自己想不起项目核心约定 = 已失忆,立刻 cat 相关文件再继续`
- /loop skill 立即触发跑了一遍验证:9 个 declared skill 全在 `.claude/skills/` 软链中,git 干净,自检"我没裸着"——loop 链路 OK
- **未来 session 注意**:`/loop` 是 session-only(关 session 即失);若需跨 session,转 `/schedule`(cloud)

### T7-T9 task 卡落地(per "task = subagent bootloader" 原则)

| Task ID | 主题 | 依赖 | Bootloader 字段(本卡 frontmatter) |
|---|---|---|---|
| `TASK-20260504004947351` | T7 `runClaudeAgent` helper + checkpoint JSONL schema | 无 | epic/反例 ADR/wiki-lessons/SCENARIOS.md/arena 范本/ADR-B 全引用 |
| `TASK-20260504004954526` | T8 `skills-introspection.agent.md`(read-only tracer) | T7 | 同上 + 反向引用 T7 |
| `TASK-20260504005000534` | T9 add/refresh/remove/prune 4 个 scenario | T7+T8 | 同上 + 反向引用 T7/T8 |

每张卡写法严守 ADR-20260503003315478 "task = pointers + action,不是 tutorial":
- frontmatter 表格:zero-context subagent boot 用的反向引用
- 需求详情:可勾选的行动列表(D1.a / D1.b / ...)
- 验收标准:具体到字段名 / 行数 / 状态判断
- 备注:失败诊断顺序

### Epic ID 替换

`EPIC-20260503234346583` 表里 `T7 (TBD)` / `T8 (TBD)` / `T9 (TBD)` → 三个真 ID;状态 `not-yet-created` → `backlog`;移除"待用 cortex CLI 创建"备注;Status History 加一行 2026-05-04 落卡。

### Daily 顶部覆盖

per `feedback_daily_top_overwrite_not_append`:本段顶部 Ground Truth / Kanban / Backlog / Next Steps / 自检表全部覆盖,不只追写。下方历史段保留(早 → 晚),如有冲突顶部为准。

### 续接段下一步

1. **单 commit** 把"3 个 T7-T9 task 卡 + epic ID 替换 + 本 daily 覆盖"一起入库;message 用 `feat(cortex): land T7-T9 Agent BDD task cards (TDD-Theme-D bootloaders)`
2. **Pull T7 + T1**(WIP=2,合规)进 in-progress;T7 是黑盒 tracer,T1 是白盒 tracer,无依赖可并行
3. push 时机由用户定;`/loop` 30 分钟一次的复活机制本 session 内观察是否有效

### 续接段元教训

- **失忆是常态,onboarding 是激活动作**:不是 nice-to-have refresh,是每次会话开始都必须跑的"上电序列"
- **Memory 比 ADR/wiki 慢,但比代码快**:像本段把"失忆 + /loop 防御"沉到 memory 而不只是 wiki/lessons,是因为它跨项目可移植;wiki 留三层模型(项目特定),memory 留失忆模式(普适)
- **Bootloader 写法验证**:T7-T9 三张卡每张能让"零上下文 subagent"读完就 boot 到执行就绪——下次 pull 进 in-progress 时验证;如果还要回头读 epic / 反例 ADR,说明卡不够 self-contained,要回流改进
