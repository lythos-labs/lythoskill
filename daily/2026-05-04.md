---
type: handoff
created_at: 2026-05-04T22:45:00
updated_at: 2026-05-04T22:45:00
session_rounds: ~45
git_branch: main
git_commit: fa6ac67
---

# Handoff: Agent BDD evidence path split + CI tag fix. T8-ready.

## 0. 验证当前状态

```bash
git diff HEAD --stat
git status --short
git log --oneline -3
```

**Expected**: clean tree, HEAD = `fa6ac67`, daily handoff modified but not yet committed.

## 1. Commit Summary

`fa6ac67 chore(bdd,ci): split agent-bdd evidence path, fix coverage action tag`

- deck: 替换 `lythoskill-creator` → `bdd-lythos` (localhost fork, BDD methodology + bun:test)
- BDD infra: Agent BDD evidence 单走 `runs/agent-bdd/<stamp>/<scenario>/` (tracked, 纯文本); CLI BDD sandbox 留在 `playground/test-runs/` (ignored, 可含嵌套 git/cold-pool)
- CI: coverage action tag `v1` → `v1.0.3` (修复 v0.9.1 以来的 failure 根因)
- SCENARIOS.md: 更新 Agent BDD convention, 说明 sandbox/report 分离架构
- 删除: `playground/agent-bdd/` (命名误导,实际是 Tier 2 CLI 集成测试,非真 Agent BDD)

## 2. 本次 Session 做了什么

| 主题 | 动作 |
|------|------|
| deck 适配性 | 确认 `bdd-lythos` SKILL.md 与当前 Agent BDD 进度 95%+ 对齐; 唯一差异:timeout 模板 60s vs 项目约定 300s |
| 清理误导目录 | 删除 `playground/agent-bdd/` (fixture 用 spawnSync, 无 LLM, 名实不符) |
| 分流架构 | runner.ts 引入 `cliSandboxDir` + `agentReportDir`, 从源头分离 sandbox/evidence |
| CI 故障排查 | `gh run list` 定位到 v0.9.1 分界线; `gh api` 查 action tags 确认 `v1` 不存在; 修复 test.yml |
| 验证 | `bun packages/lythoskill-deck/test/runner.ts` → 21 CLI pass + 1 Agent evidence 正确落盘到 `runs/agent-bdd/` |

## 3. 踩过的坑与修正

### 坑 1: `playground/agent-bdd/` 命名误导
- **错误**:目录名带 "agent-bdd" 但实际用 `spawnSync` spawn CLI, 无 LLM 在 loop
- **正确**:Agent BDD = Tier 3, 只有 LLM + checkpoint JSONL; CLI BDD = Tier 2, 用 fixture spawn
- **根因**:早期 harness 把 "agent = 测试自动化 actor" 和 "agent = AI agent" 混为一谈

### 坑 2: test-runs 32M 是累积,不是单次
- **错误**:第一眼以为 Agent BDD 一次跑 32M
- **正确**:55 次 stamp 累积,平均 ~580K/次; 95% 是 CLI scenario 的 cold-pool/workdir 副本
- **根因**:runner.ts 把 CLI + Agent 产物混在同一个 stamp 下

### 坑 3: CI failure 误判为 BDD 测试崩
- **错误**:v0.9.1 以来 CI 全 failure, 直觉怀疑 BDD 测试
- **正确**:test job 47 passed; coverage job `70-10/bun-coverage-report-action@v1` tag 不存在
- **根因**:action 作者只打了 `v1.0.x` 没打 floating `v1` tag; daily 决策记录"wiki 已有 pattern"但没验证 tag 存在性

## 4. 真实状态

| 文件 | 状态 | 说明 |
|------|------|------|
| `packages/lythoskill-deck/test/runner.ts` | committed | fa6ac67, 分流架构 |
| `.github/workflows/test.yml` | committed | fa6ac67, action tag fix |
| `packages/lythoskill-test-utils/SCENARIOS.md` | committed | fa6ac67, convention update |
| `skill-deck.toml` | committed | fa6ac67, bdd-lythos |
| `runs/agent-bdd/` | committed | 首次 Agent BDD evidence baseline (T7 tracer bullet) |
| `daily/2026-05-04.md` | modified | 本 handoff, 待 commit |

## 5. 下一步

- **T8**: `TASK-20260504005000534` — 写 4 个 `.agent.md`(refresh / remove / prune + 扩展)
- **CI 风险**:test.yml 第 20 行仍引用 `packages/lythoskill-creator/src/cli.ts align`, skill-deck.toml 已移除该 skill; 若 lythoskill-creator 包被删, CI 会断
- **daily handoff commit**: 本文件覆盖顶部 Ground Truth 后 commit + push

## 6. 接手自检

- [ ] `git status` clean tree
- [ ] `git log --oneline -3` → `fa6ac67` on main
- [ ] `bun test packages/lythoskill-deck/src/*.test.ts` → 49 pass
- [ ] `bun packages/lythoskill-deck/test/runner.ts` → Agent BDD evidence 落 `runs/agent-bdd/`

---

## Session Handoff (from b9ded00 → fa6ac67)

### 0. Verify Current State
```bash
git diff b9ded00 --stat          # Construct "from T0 to now"
git status --short                # Real-time working tree
git log --oneline -3              # Confirm: fa6ac67 chore(bdd,ci): split agent-bdd evidence path...
```

**Expected**: working tree has ONBOARDING.md (untracked), ADR-20260504134942164 (untracked); HEAD = `fa6ac67`.

### 1. Commit Summary

`b9ded00 docs(adr): propose ADR-20260504135256566 — cortex init ships jira-simulation hooks`

- 冻结 cortex 在 husky-mixin 中的最小自治行为集(三块):
  1. T3 trailer-driven task/ADR/epic 自动流转(post-commit)
  2. Epic→ADR 自动接受(pre-commit;**触发条件是 staged active EPIC 文件**,不是 commit message 里的 `Epic:` trailer)
  3. Lane 守护双轨道 max-1-active(已 CLI 层强制;commit-time guard 待 mixin 实现)
- 显式排除:hexo-style skills 分支同步、skill-builder 重建、mixin 协议本身
- husky-mixin 打包协议 → 留给后续 ADR

### 2. Uncommitted Changes & Untracked

| 文件 | 状态 | 说明 |
|------|------|------|
| `ONBOARDING.md` | untracked | 团队 onboarding guide;**已修正 recycle tip 顺序** |
| `cortex/adr/01-proposed/ADR-20260504134942164-…` | untracked | 前 session 产出,本次只当风格参考,尚未 commit |
| `skill-deck.toml` | modified, unstaged | 来源未确认,本次 session 未主动编辑 |
| `feedback_proactive_recycle_at_breakpoints.md` | updated | memory 路径(`.claude/projects/…/memory/`),不归 git |

### 3. Pitfalls & Lessons

- **Trailer ≠ trigger**:pre-commit 的 Epic→ADR auto-accept 看的是 **staged active EPIC 文件**,不是 commit message 里的 `Epic:` trailer。本次初期搞混,已修正。
- **Recycle 顺序必须拆 commit**:scribe 写 `daily/YYYY-MM-DD.md` 到磁盘,这个文件需要先 commit 再 push,不能折叠进 push 一步。用户纠正了 ONBOARDING.md 和 memory 中的错误描述。
- **ADR scope 被 user mid-flight 收窄**:user 在 ADR 已创建后追加了两条约束:"lane 守护也纳入" + "只清理 lythoskill 自主部分,skill-builder/hexo 排除"。已纳入正文。

### 4. Next Steps for Reader

- 决定 `ONBOARDING.md` 和 ADR-20260504134942164 是否 commit / push
- 处理 `skill-deck.toml` 的 modified 状态(来源未确认)
- 如需继续推进:起草 husky-mixin ADR(分发协议/与其他 mixin 共存/选择性激活)
- 当前 context ~40%,安全;如需回收可按修正后的顺序(commit→scribe→commit daily→push→clear→onboard)执行

---

## Handoff: T5/T6/T7 completed — deck unit tests 82% + first Agent BDD tracer bullet GREEN. Ready for T8.

### 0. 验证当前状态

```bash
git diff HEAD --stat
git status --short
git log --oneline -3
```

**Expected**: clean tree, T7 files committed, HEAD = latest.

### 1. 项目身份

- **项目名称**: lythoskill
- **类型**: thin-skill monorepo (Starter + Skill + Output)
- **技术栈**: Bun 1.3.11, TypeScript 5.0+, ESM-only
- **当前分支**: `main`
- **最近 commit**: TBD — T7 tracer bullet
- **前序 commit**: `7e4d8ee` — T5/T6 coverage sweep, `201ca11` — command layer B tests

### 0. 验证当前状态

```bash
git diff 7e4d8ee --stat
git status --short
git log --oneline -3
bun packages/lythoskill-project-cortex/src/cli.ts probe 2>/dev/null || echo "cortex probe 未安装"
```

**Expected**: clean tree, HEAD = `7e4d8ee`, no in-progress tasks.

### 1. 项目身份

- **项目名称**: lythoskill
- **类型**: thin-skill monorepo (Starter + Skill + Output)
- **技术栈**: Bun 1.3.11, TypeScript 5.0+, ESM-only
- **当前分支**: `main`
- **最近 commit**: `7e4d8ee` — chore(cortex): complete TASK-20260503235014489 (post-commit auto-generated)
- **前序 commit**: `201ca11` — test(deck): coverage sweep, `cf2f3f3` — test(deck): command layer B

### 2. 本次 Session 做了什么

#### T5/T6 已完成的修改

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `packages/lythoskill-deck/src/remove.test.ts` | add 4 it | C9–C11 + legacy string-array removal |
| `packages/lythoskill-deck/src/refresh.test.ts` | add 6 it | C12–C14 + localhost skip, not-git, target not found |
| `packages/lythoskill-deck/src/prune.test.ts` | add 2 it | C15–C16 + empty cold pool |
| `packages/lythoskill-deck/src/parse-deck.test.ts` | **new** | 5 it: dict, missing path, invalid schema, legacy, empty |
| `packages/lythoskill-deck/src/validate.test.ts` | add 3 it | toml parse error, deprecated warning, invalid transient expires |
| `packages/lythoskill-deck/src/add.test.ts` | add 1 it | invalid skill type rejection |
| `bunfig.toml` | **new** | auto coverage + lines threshold 0.8 |
| `packages/lythoskill-deck/README.md` | modify | coverage badge (82% brightgreen) |
| `.github/workflows/test.yml` | modify | job rename test, --frozen-lockfile, coverage job + lcov PR comment |

#### T7 已完成的修改

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `packages/lythoskill-deck/test/scenarios/skills-introspection.agent.md` | **new** | First Agent BDD scenario: agent reads deck → writes checkpoint |
| `packages/lythoskill-deck/test/runner.ts` | modify | Agent BDD extensions: parseAgentMd, runAgentScenario, runClaudeAgent import |
| `packages/lythoskill-test-utils/SCENARIOS.md` | modify | Agent BDD count 0→1, added scenario table + convention docs |

#### Coverage 变化

| 指标 | Before | After |
|------|--------|-------|
| deck tests | 35 pass | **49 pass** |
| deck lines | 74.54% | **82.35%** |
| deck funcs | 67.39% | **73.81%** |
| project-wide lines | — | **73.06%** (curator 14%, test-utils 51% 拉低) |

### 3. 关键决策

| 决策 | 选项 | 选择 | 理由 |
|------|------|------|------|
| Coverage threshold 双指标 | lines + funcs | 仅 lines 0.8 | funcs 73.81% 未达标，双 threshold 会导致 CI 红 |
| prune confirm() 分支 | mock readline | **不覆盖** | 用户认可"逻辑清楚但难触发的分支不追" |
| CI coverage action | codecov / shields | **bun-coverage-report-action** | wiki 已有 pattern，无需第三方注册 |
| README badge 位置 | 根 / deck | **deck README** | 82% 仅代表 deck 包，根 README 保持诚实 |

### 4. 踩过的坑与修正

#### 坑 1 (T7): Agent BDD 产物可观测性至关重要
- **教训**: agent stdout/stderr + checkpoint JSONL 是 debug 的唯一来源;agent 行为有语义自由度,失败产物本身就是推理样本
- **对策**: `runAgentScenario` 持久化 `agent-stdout.txt` + `agent-stderr.txt` + `_checkpoints/*.jsonl`

#### 坑 2 (T7): Given parser 把多行 bullet 子项当成了技能名
- **错误**: `tool skills?:\s*([^\n]+)` 捕获了缩进子项 `- skill-a at github...`
- **正确**: Given bullet 改为单行 `tool skills: skill-a, skill-b`
- **根因**: `.agent.md` 解析器没有做嵌套 bullet 处理

#### 坑 3: refresh localhost skip 断言写错
- **错误**: `logs.some(l => l.includes('local') && l.includes('skipped'))` 要求同一行
- **正确**: 分开断言 `includes('local')` + `includes('Skipped: 1')`
- **根因**: icon 行和 summary 行是分开打印的

#### 坑 2: parseDeck invalid schema 测试用错字段
- **错误**: `max_cards = "not-a-number"` 想触发 zod 失败
- **正确**: `path = ""` 触发 `z.string().min(1)` 失败
- **根因**: `SkillEntrySchema.passthrough()` 允许额外字段

#### 坑 3: C17 refresh target not found 用空 deck
- **错误**: 空 deck 导致 `declared.length === 0` 直接 exit(0)
- **正确**: 构造非空 deck 再传不存在的 target
- **根因**: 没看 refresh.ts 的 early return

### 5. 真实状态

| 文件 | 状态 | 说明 |
|------|------|------|
| 所有 src/*.test.ts | committed | T5 + T6 已提交 |
| bunfig.toml | committed | coverage auto-on |
| CI workflow | committed | coverage job |
| deck README badge | committed | 82% |
| T7 agent-md + runner | untracked | skills-introspection.agent.md, runner.ts Agent BDD ext, SCENARIOS.md |
| daily/2026-05-04.md | untracked | 本 handoff，待提交 |

### 6. 下一步

T7 完成。进入 **T8** `TASK-20260504005000534` — Add refresh/remove/prune agent-bdd scenarios (4 `.agent.md` files)。

Agent BDD 基础设施已就绪：
- `runClaudeAgent` + checkpoint schema in `bdd-runner.ts`
- `parseAgentMd` + `runAgentScenario` in deck `runner.ts`
- 第一个 tracer bullet 验证 pipeline 能跑通

**具体动作**:
1. 拉 T8 到 in-progress
2. 写 4 个 `.agent.md` 场景（refresh/remove/prune + 一个扩展）
3. 验证本地跑通（不进 CI）

### 7. 接手自检

- [ ] `git status` clean tree
- [ ] `bun test packages/lythoskill-deck/src/*.test.ts` → 49 pass
- [ ] `bun run test:all` → 21/21 CLI passed (Agent BDD skipped in CI)
- [ ] `bun packages/lythoskill-deck/test/runner.ts` → 22/22 passed (含 Agent BDD, 本地)

### 7. 接手自检

- [ ] `git status` clean tree
- [ ] `git log --oneline -3` 匹配 `7e4d8ee` / `201ca11` / `cf2f3f3`
- [ ] `bun test packages/lythoskill-deck/src/*.test.ts` → 49 pass
- [ ] `bun run test:all` → 21/21 passed
