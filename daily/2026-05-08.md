---
type: handoff
created_at: 2026-05-08T23:00:00
git_branch: main
git_commit: db755a8
---

# Handoff: 2 epics closed + v0.9.33 published + DeepSeek serve adapter + architecture refactor

> 本段: reconcile epic → URL epic → backlog 16→0 → claude-sdk verify → README slim → deck fact-check → AGENTS.md bootloader → DeepSeek serve adapter → actor wiki → agent-adapter architecture refactor → v0.9.33 publish

## 0. Verify Current State

```bash
git log --oneline -5
# 1370c4f feat(examples): add deep-research deck + update Codex decks for snapshot mode
# 9d5ee2a docs(readme): add working_set cross-agent config table in workspace section
# aa331ff docs(readme): add "Set Up a Workspace" section
# 6c0742e docs(readme): add sync/freeze/reconcile commands + snapshot mode to deck README
# aecd8fe chore(release): v0.9.32

git status --short    # clean
```

Tests:
- @lythos/cold-pool: 131 pass / 0 fail (10 files)
- @lythos/deck: 84 pass / 0 fail (10 files)
- reconcile BDD agent: PASS (独立 subagent 验证)
- All 10 example decks: `deck validate --remote` PASS

npm: v0.9.32 published (all 8 packages)
npm view @lythos/skill-deck version  # 0.9.32

## 1. EPIC-20260507191713917 交付清单

| Task | 产出 | 测试 |
|------|------|------|
| T1: deck sync/freeze | `sync-freeze.ts` — per-skill snapshot↔symlink 切换 | 4 pass |
| T2: buildReconcilePlan | `reconcile-plan.ts` — k8s-style desired vs actual diff | 15 pass |
| T3: deck reconcile CLI | `reconcile.ts` — plan-first drift report | 手动 BDD PASS |
| T4: snapshot storage + GC | working-set 同位置, mode 在 lock 中追踪 | 现有 link/remove 覆盖 |

## 2. 新增 CLI 命令

```
deck sync <alias>         # snapshot → symlink (实时模式)
deck freeze <alias>       # symlink → snapshot (pin 当前版本)
deck reconcile [--apply]  # lock vs cold pool 漂移检测
```

## 3. 关键设计决策

- **Mode 状态定义**: `LinkedSkill.mode: "symlink" | "snapshot"` 写入 lock 文件 (schema.ts)
  - `deck link --mode=snapshot` 设全局默认; lock 中已有 per-skill mode 则覆盖全局
  - `deck sync` / `deck freeze` 更新 lock 的 mode 字段
- **Snapshot 存储位置**: `<working-set>/<alias>/` — 与 symlink 同位置, 简化 GC
- **Reconcile plan**: 纯函数, plan-first (只报告), `--apply` 尚未实现收敛 (提示手动命令)
  - Missing → `deck add`, Behind → `deck refresh`, Extra → `cold-pool prune`

## 4. Schema 变更

- `LinkedSkillSchema` 新增 `mode: z.enum(["symlink", "snapshot"]).default("symlink")`
- `link.ts`: linkedSkills.push 包含 `mode: MODE` 字段
- 向后兼容: 旧 lock 文件无 mode 字段 → Zod default "symlink"

## 5. Agent BDD 场景

- `test/scenarios/deck-reconcile.agent.md` — cold-pool drift 检测 (missing/behind/extra)
- `test/scenarios/deck-sync-freeze.agent.md` — snapshot↔symlink 模式切换 + lock mode 验证
- Reconcile BDD 已由独立 subagent 执行通过

## 6. Example Deck 全量 Audit (deck validate --remote)

10 个预组 deck 全部通过远程验证 — 每个 locator 都解析到真实 GitHub repo 的 SKILL.md：

| Deck | Skills | 验证 |
|------|--------|------|
| design-studio.toml | 4 (anthropics: frontend-design, theme-factory, brand-guidelines) | ✅ user + remote |
| documents.toml | 3 (anthropics: pdf, docx) | ✅ user + remote |
| engineering.toml | 4 (mattpocock: tdd, to-prd + SpillwaveSolutions: mermaid) | ✅ remote |
| full-stack.toml | 6 (vercel-labs: react, composition + mattpocock + anthropics) | ✅ remote |
| governance.toml | 4 (lythos-labs: deck, cortex, scribe, onboarding) | ✅ remote |
| recipe-report.toml | 4 (anthropics: docx + Weizhena: research ×2) | ✅ remote |
| research-documents.toml | 6 (Weizhena: research ×3 + anthropics: pdf, docx) | ✅ remote |
| scout.toml | 1 (lythoskill-deck — minimal baseline) | ✅ remote |
| visual-explainer.toml | 3 (SpillwaveSolutions: mermaid + anthropics: theme-factory) | ✅ remote |
| deep-research.toml | 4 (Weizhena: research, research-deep, research-report) | ✅ NEW + remote |

Codex 变体 (codex/documents.toml, codex/engineering.toml) 更新：`cp -rL` 临时方案 → `link --mode snapshot`（0.9.32 已落地）。

`deck validate --remote` 管线（EPIC-20260507020846020 T8）就是为这个场景设计的 — 10 个 deck、45 个 locator、全部通过 GitHub API 验证。

## 7. README 三段路径

Quick Start 现在覆盖三个入口：

1. **30 秒体验** — `curl | bash` 零安装试跑
2. **准备工作空间** — `mkdir → cd → deck link --deck <url>`（新增）
3. **同任务不同卡组对比** — arena 三路并行

新增的 "Set Up a Workspace" 填补了"curl 一次"和"arena 对比"之间的空白。
包含 `working_set` 跨 agent 配置表（Claude Code / Cursor / Codex / Copilot / Windsurf）。

## 8. EPIC-20260508082810062 — Everything-from-URL 启动

填充了 epic demand tree（6 themes）并完成 T1-T2：

| Task | 状态 |
|------|------|
| T1: resolveDeckPath 提取 | ✅ done — `resolve-deck.ts` (sync split for test compat) |
| T2: validate URL 支持 | ✅ done — `deck validate --deck <url> --remote` |
| T3: validate-as-discovery | ✅ 自然实现（T2 已打通） |
| T4: Task from URL | 待做 |
| T5: AGENTS.md bootloader | 模式已验证（playground），待文档化 |
| T6: deck INDEX.md | 待做 |

关键设计：`resolveDeckPathSync` (sync) + `fetchDeckUrl` (async) 分离，避免 async 污染 local path 调用链（link.ts 测试兼容）。

Architecture-explainer 预组 deck 加入 `examples/decks/`（11 个 deck 全验证通过）。

## 9. Backlog 清理 — 16 → 0

全部 backlog task 审计：

| 动作 | 数量 | 说明 |
|------|------|------|
| completed | 10 | 今天实际完成或早已完成但未移走 |
| terminated | 6 | 空壳/愿景/被吸收/blocked |

终止明细：
- MCP adapter — 愿景类，除非找到值得做的 CLI
- DeepSeek TUI adapter — blocked on serve mode
- skill-type runtime — 大部分已实现（combo killed, lock type field exists, transient warnings done）
- curator-add error path — 被 cold-pool 吸收
- curator discover BDD — 太模糊，discover 只是 feed 浏览

## 10. Claude Player — claude-sdk 默认化

- `registerAgent('claude-sdk')` → `registerAgent('claude')`，与 BUILTIN_PLAYERS 对齐
- 跑通两个 arena agent-run 测试：
  - skill introspection (122ms, PASS)
  - cookie recipe .docx + radar chart (302ms, agent 产出正确, judge parser 已知问题)
- claude-cli adapter 标注 deprecated，默认路径不可达

## 11. Innate Priority Convention

在 lythoskill-deck SKILL.md 补充：context compaction 或 skill reload 后，innate skill 优先读全文（eager/hungry）。tool 是 lazy。agent 端约定，lock type 字段是信号。不是 CLI 机制。

## 12. Architecture-Explainer Playground

`playground/architecture-explainer/` — 完整 PoC：
- 混合 deck (visual-explainer + design-studio + docx + pdf)
- AGENTS.md bootloader (agent 读它 → 导航 parent 项目 → 产出)
- 产出: architecture.md (644行, 8 Mermaid 图), architecture.docx (332KB), architecture.pdf (353KB, 21页)
- 模式提炼为 `examples/decks/architecture-explainer.toml` + wiki pattern

## 13. DeepSeek Serve Adapter — Actor Model

研究了 `deepseek serve --http` 的完整 API surface，确认与 wiki 调研一致：
- Thread API: POST /v1/threads → POST .../turns → SSE events (工具执行确认)
- One-shot `deepseek "prompt"` + `deepseek exec` → text-only（不执行工具）
- `--yolo` flag 在 0.8.14 移除，改用 `auto_approve:true` in thread config

实现了 actor-model daemon adapter：
- PID lock file at `~/.agents/lythoskill/deepseek-serve.json`
- 自动启动/复用/健康检查
- session→thread 映射（跨 session 可恢复）
- 27 个 FSM unit tests (lock file lifecycle, PID detection, version parsing, session IDs, API paths)

## 14. Agent Adapter 架构修正 — 独立包

发现架构错误：deepseek-serve 错放在基座包。修正：
- `@lythos/agent-adapter` — interface + registry + lightweight CLI adapters only
- `@lythos/agent-adapter-claude-sdk` — heavy: Anthropic SDK
- `@lythos/agent-adapter-deepseek-serve` — heavy: daemon + SSE + PID (new)
- 删除旧 `deepseek.ts` one-shot adapter (text-only, 已过时)
- 删除 `deepseek.test.ts` (过期导致 CI fail)
- 基座 README + AGENTS.md 写明架构契约："heavy = new package"
- publish.sh +3 agent-adapter 包

## 15. CI & Publish Gotchas 表

AGENTS.md 新增 8 项常见故障：
- frozen-lockfile → bun install + commit bun.lock
- 新包不在 publish script → 加入 PACKAGES 数组
- require() in .ts → ESM import
- 删除 adapter 时忘记删 test 文件
- 等（全部是今天踩过的坑）

## 16. DeepSeek 优化预组 Deck + Players.toml

- `deepseek-research.toml` — 子代理并行调研
- `deepseek-codebase.toml` — explore→plan→implementer→verifier 链
- `players.toml` — 诚实记录 tested/theoretical/blocked 状态

## 17. 其他

- README 739→650 行，Architecture+Quick Reference 深潜→wiki link
- localhost uniform FQ: `localhost/me/<name>`
- deck add 优先，裸 git clone → advanced
- CodeQL 3 alerts 全部修复
- Judge markdown parser 修复（`**Verdict: PASS**` 提取）
- Innate priority convention in SKILL.md
- Architecture-explainer playground: .docx + .pdf verified
- 15 README skill paths validated via GitHub API
- bun.lock 漏提导致 CI frozen-lockfile 失败（已修）

## 18. 待办 (下次 session)

- v0.9.33 发布：deepseek-serve 新包首次 npm publish（按 publish.sh 新列表）
- `deck reconcile --apply` 收敛实现
- `@lythos/infra` 落地（SqliteDb 提取）
- curator catalog DB 继承 SqliteDb
- arena agent-run 跑通 deepseek serve 完整 e2e（当前 serve 启动正常，SSE 收集需 eventsource 库优化）

- `deck reconcile --apply` 收敛实现
- `@lythos/infra` 落地（SqliteDb 提取, ADR accepted 但代码未迁）
- curator catalog DB 继承 SqliteDb
- judge parser 修复（markdown `**` → JSON parse error）
- npm publish v0.9.33（accumulated changes: claude-sdk default, URL dispatch, deck INDEX, innate priority）

- `deck reconcile --apply` 收敛实现 (当前 plan-first, 提示手动)
- `@lythos/infra` 落地 (SqliteDb 提取, ADR-20260508075913360 accepted 但代码未迁)
- curator catalog DB 继承 SqliteDb
- 如果 v0.9.32 publish 未在本次完成: bump + publish

---

# Earlier today (snapshot mode, URL deck, SqliteDb extraction, ADR sweep) — preserved below

> 本次 session segment: deck snapshot mode 落地 → URL deck fetch → working_set 解析修正 → SqliteDb 提取 → example deck 规范化 → ADR 审计

## 0. Verify Current State

```bash
git log --oneline -5
# eb11a04 docs(adr): proposed — @lythos/infra runtime base package
# cd6d3e4 fix(deck): replace spawnSync('curl') with native fetch() for URL deck
# e408144 feat(deck): link --deck accepts http/https URL (fetch + save + link)
# a580353 feat(deck): link --deck in subdirectory defaults working_set to cwd + abs path logging
# bdbc76d refactor(cold-pool): extract SqliteDb base class from MetadataDB

git status --short    # 待提交: daily/2026-05-08.md
```

## 1. Snapshot Mode — deck link --mode=snapshot

**触发**: Codex 用户需要真实目录（非 symlink），Codex #11314 已知 symlink bug。

**实现**:
- `linkDeck(cliDeckPath, cliWorkdir, { mode: 'snapshot' })` → `cpSync` 替代 `symlinkSync`
- `deck add --mode=snapshot` 传递到 link
- 测试: 15 pass（含 snapshot 专项测试）

## 2. Deck Add 模板规范化

`deck add` 新建 toml 时写入显式配置 + 全平台注释：

```toml
#   working_set = ".claude/skills"   # Claude Code (also read by Cursor, Copilot)
#   working_set = ".agents/skills"   # Codex CLI, OpenClaw
#   working_set = ".cursor/skills"   # Cursor-native
#   working_set = ".github/skills"   # GitHub Copilot
#   working_set = ".windsurf/skills" # Windsurf

[deck]
cold_pool = "~/.agents/skill-repos"
working_set = ".claude/skills"     ← 改这一行切平台
```

## 3. working_set 解析修正 (ADR-20260508074057834)

**问题**: `--deck ./decks/my.toml` 时 working_set 解析到 `./decks/.claude/skills/`而非 cwd。

**方案 C**: `--deck` 显式时 PROJECT_DIR = cwd。同时 link 日志输出绝对路径：

```
📋 deck:      /absolute/path/to/skill-deck.toml
📁 working_set: /absolute/path/to/.claude/skills
🗄️  cold_pool:   /Users/.../.agents/skill-repos
```

agent 看到路径就知道技能装哪了——内文档化。

## 4. URL Deck Fetch (ADR-20260508075301691)

**发现**: quick-agent.sh 已支持 URL deck，但 CLI 用户还需两步（curl + link）。

**实现**: `deck link --deck https://.../deck.toml` → `fetch()` + 保存 + link。一行完成。

**技术**: `spawnSync('curl')` → 原生 `fetch()`（AbortSignal.timeout，HTTP_PROXY 自动生效）。

## 5. SqliteDb 提取 (ADR-20260508075913360)

**问题**: `SqliteDb`（lazy-open, migrations, exec/queryOne/queryAll）寄宿在 `@lythos/cold-pool`。curator 想用就得依赖整个 cold-pool。

**ADR**: 新建 `@lythos/infra` — 前端 `@company/shared` 模式。零依赖运行时基座。

## 6. Example Deck 规范化

9 个 deck 加 usage 注释 + `innate lythoskill-deck`。Codex 子目录（`.agents/skills`）。Validator 改为递归扫描。

## 7. ADR 状态审计

| 动作 | ADR |
|------|-----|
| proposed → accepted | FQ-only, localhost, prune, refresh, validate-companion, metadata-layer, snapshot-vs-sync |
| 新建 proposed | working_set 解析, URL deck fetch, @lythos/infra |

## 8. Bare-name 审计

| 位置 | 迁移状态 |
|------|----------|
| 单元测试 fixture | ✅ 已迁 FQ（80/80 pass） |
| Agent BDD `.agent.md` | 故意保留 bare name — `.md` 天然隔离 test runner，agent 读 Markdown 直接执行 |

`.agent.md` 格式确认正确：不需要 `parseAgentMd` 这种 parser，agent 自然读 Given/When/Then 就能执行。

## 9. EPIC-20260507191713917 收尾计划

### 已完成的前置工作

| 前置 | 状态 |
|------|------|
| ADR-20260507190157540 (snapshot vs sync) | accepted |
| ADR-20260507021957847 (cold-pool resource holder) | accepted |
| ADR-20260507143241493 (metadata layer) | accepted + implemented |
| `ColdPool` manager + metadata DB | done |
| `buildListPlan` + cold-pool primitives | done |
| `SqliteDb` base class | done |
| `deck link --mode=snapshot` | done |
| `deck add` template (explicit cold_pool/working_set) | done |
| Graduation exam E2E | 手动跑通 |

### 待完成的 T1-T4

```
T1: deck sync / deck freeze CLI       ← snapshot↔symlink 意图切换
T2: buildReconcilePlan + execute      ← 核心收敛逻辑（cold-pool 层）
T3: deck reconcile CLI                ← 用户面命令（依赖 T2）
T4: snapshot 存储 + GC                ← cp 位置 + prune 清理
```

### 依赖链 + 推荐顺序

```
Step 1: T2 (reconcile plan) — 纯函数，已有 metadata DB + cold-pool primitives
        输入: lock file + cold pool fs state → 输出: { missing, behind, extra }
        可独立开发，不需要 CLI

Step 2: T1 (deck sync/freeze) — 已有 --mode=snapshot 的 cp/ln 分支
        sync  = snapshot → symlink（切到实时模式）
        freeze = symlink → snapshot（pin 当前 HEAD）
        可并行于 T2

Step 3: T3 (deck reconcile) — 消费 T2 的 plan，加 CLI + --apply
        和 T1 集成: reconcile 发现 drift → 提示 sync/freeze

Step 4: T4 (snapshot storage + GC) — 决定 cp 位置，prune 清理孤儿快照
```

信心来源：E2E 毕业考跑通，`link --mode=snapshot` 正常工作，metadata DB 记录 head_ref 已验证。

## 10. 待办 (下次 session)

- 启动 T2 (buildReconcilePlan) — 核心纯函数，风险最低，解锁后续
- T1 并行（deck sync/freeze — 轻量 CLI 糖）
- @lythos/infra 落地
- curator catalog DB 继承 SqliteDb
- v0.9.31 published — npm 已更新
