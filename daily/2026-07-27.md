# Daily — 2026-07-27

## Session Handoff — goal-driven: T610 + T556 双卡进 review（含 user-sim 门首跑）

### 0. Verify Current State

**Git**: HEAD = `725e540f` (docs(deck,cortex): user-sim gate follow-ups) — pushed to origin
**Version**: v0.17.2
**Deck**: 14 skills linked
**Branch**: main, clean working tree
**Active Epic**: 1 — EPIC-20260717161516583 k3-era-harness-molting（主题 A 加固 + 主题 B shed 判决均落地，待用户定 close/re-scope）
**Active Task**: 0 in-progress, **2 in review** (TASK-…610, TASK-…556 — 等用户标记 done)
**CI**: STILL UNVERIFIED — `.github-token` 过期 (gh 401)，自 07-20 挂起至今

```bash
# Verify: if output diverges from the fields above, this handoff is stale
git status --short
git log --oneline -3
ls cortex/tasks/03-review/
```

### Completed

1. **TASK-20260719015727610 (probe hardening) → 03-review** — 续 07-20 中断（API 额度耗尽，非计划内停止）的未提交工作。发现并修复 draft 里的真 bug：`link.ts:811` 未 await 异步化的 `checkColdPoolHealth` → boot 健康告警**整体静默失效**（TypeError 被 catch 吞掉）;`skipFetch` 无调用方。R1 保留 shallow fetch（设计注记）,R2 跨仓并行 + 嵌套 `skipHealthFetch`（实测 link 4.78s),R3 自愈补 untracked-conflict 类 + `reset --hard` 修 staged-only,R4 `LYTHOS_DEBUG` 耳语，R5 fixture + 守卫式跳过。ZK 2 轮 APPROVE(round 1 抓出 P1：告警文案 `git clean -fd` 缺 `-C`，用户复制粘贴会清掉自己仓库）。
2. **TASK-20260719015727556 (A/B 干净重跑) → 03-review** — 30 盲受试（5 场景 × 2 臂 × N=3,20 explore + 10 coder)，臂文件 staging 中性路径去臂名提示。结果：**0/30 退化细胞**（新增 S5 螺旋探针两臂均无焦虑螺旋）;**canary 双向交叉引用 0/15**；回插两章节被 B 臂在目标场景真实引用（DHE 5/15、CPTSD 表 1/15）但 A 臂无章节行为等价 → **shed 成立（词汇冗余 ≠ 行为必需）,v1 撤回正式结案**。报告：`cortex/wiki/03-lessons/2026-07-27-agents-md-shed-sections-ab-rerun-vocabulary-not-necessity.md`;ZK 2 轮 APPROVE（修 2 处数字错：diff 54→55、DHE 4/15→5/15)。
3. **user-sim 门（审查梯第 3 层）两卡首跑** — 均 yes-with-conditions，条件已闭环：T556 落 sha256 manifest(37 文件，tracked:`cortex/wiki/03-lessons/2026-07-27-ab-rerun-evidence-manifest.sha256`,37/37 OK);T610 的 `LYTHOS_DEBUG` 写入 deck SKILL.md（已 build + link)。
4. **全量测试门绿**(final HEAD 复跑 EXIT=0, 11 suites 0 fail),probe 一致，全部推送。

### Key Decisions

- **停在 Review 而非 Closes**：目标完成判据里我写了 Closes trailer，但项目制度是 "03-review = STOP，用户标记 done"——以制度为准，Closes 留给用户。用户说"标记完成"即发 Closes trailer。
- **quirk 不定案为环境问题，测试做守卫式降级**:`bun test <path>`（仓库根直跑）时子进程 piped stdout 读空（exit 0、副作用正常、stdout=""),Bun 1.3.11/macOS；同一文件 /tmp 下通过，canonical 全量门（per-package scripts）下 fixture 实绿。R5 fixture 以 spawn-sanity 探针守卫：quirk 环境 `describe.skip` + 响亮 ⚠️，正常环境真跑。未做根因深挖（env 级，收益低）。
- **raw 证据 local-only 但加完整性锚**:playground/ 按 .gitignore 惯例不入库（v1 同样），但支撑删节判决的证据不该比判决短命 → sha256 manifest 入 tracked wiki 目录。
- **矩阵升级到 N=3/cell**（卡要求 N≥2):+1 coder-type per cell 覆盖 subject-type 方差（v2 缺陷 3)，总 30 受试 8 批。

### Pitfalls

- **API 403 会伪装成"会话结束"**：两次中断（07-20、今天各一）都是额度 403，工作留在未提交树里。教训：长批次实验每批落盘（raw 逐批 Write),handoff 之外也要有可恢复锚点。
- **自己 diff 的行数会数错**:report 里 54 vs 55 的错来自我凭过滤管道数行；ZK 用原始 `grep -c "^>"` 复核抓出。机械数字必须原样引用命令输出，不过滤。
- **修复 wait bug 后告警立即抓到真 drift**:mattpocock/skills ≥1 behind origin —— 这不是测试，是 guard 上线第一分钟的真实产出（又一次 day-one 自证）。
- **user-sim 的 "chat optimization" 大小写**:S5B-e1 引的是小写 "performance over work"，大小写敏感 grep 会漏 → engagement 统计用 `-i`。

### Next Steps

1. **用户标记 done**:`Closes: TASK-20260719015727610` + `Closes: TASK-20260719015727556`（用户过目后；v1 卡 TASK-20260717161516693 也在 03-review,556 关时一并处置——user-sim P3)。
2. **Epic 收尾**:EPIC-20260717161516583 主题 A/B/C 全落地，close 或 re-scope（用户定）。
3. **CI 验证（挂起 7 天）**：更新 `.github-token` 后 `gh run list`（重点 validate-example-decks)。
4. **冷池 drift 处理**:boot 告警报 mattpocock/skills ≥1 behind → `deck refresh --exec && deck link`。
5. **user-sim 校准待办**(`.private/decision-profile.md`):sim 自报 4 个 guesses——raw local-only 定 P2 是否过轻、LYTHOS_DEBUG 文档 P2 力度、R2 不要并发断言测试是否过忍、yes-with-conditions 而非 no 是否符合用户心思。用户下次评价 review 贴近度时校准。
6. (P2 legacy) 2 个 07-10 stale backlog 任务仍待 work-or-suspend。

### Temp Artifacts

- `playground/2026-07-17-molting-experiment/rerun/` — 30 raws + arms + scenarios + staging(local-only;sha256 manifest 在 tracked wiki 目录）
- `/tmp/full-gate*.log`、`/tmp/dbg-health.test.ts`、`/tmp/real-health.test.ts` — quirk 调查临时文件（/tmp，自动清理）
- `.private/user-sim-reviews/` — 本轮 user-sim 输出在会话 agent-32（未落盘；如需复看从本 daily 的 Next Steps 5 重建）

---

## Work Log

- 07-27 — Onboarding 发现 07-20 后有一整个未提交 session(556/610 已 start、610 代码半成品）;probe 一致；建 goal 推进双卡
- 07-27 — T610：修复 await bug + skipFetch 接线 + R3/R4/R5 全部落地；deck 155+1skip / gate 156 绿；ZK 2 轮 APPROVE;4 commits 推送 → 03-review
- 07-27 — T556:hygiene 验证 → 30 受试 8 批（中途 API 403 一次，逐批落盘无损失）;canary 扫描 + 章节引用扫描；报告 + 卡 + epic 更新；ZK 2 轮 APPROVE;2 commits 推送 → 03-review
- 07-27 — user-sim 门两卡首跑：2×P2(manifest、LYTHOS_DEBUG 文档）+ 4×P3 全部闭环；commit 推送；最终 gate 绿；daily
