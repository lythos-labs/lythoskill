---
type: handoff
created_at: 2026-05-05T00:00:00
updated_at: 2026-05-05T03:15:00
git_branch: main
git_commit: 75f7878
session_rounds: ~300
---

# Handoff: Day 2 — fractal coverage sweep + arena battle-test + growth hacking research

## 0. Ground Truth

```bash
git log --oneline -5   # expected HEAD = 75f7878
# 75f7878 feat(arena): deck link in setupWorkdir + comparative judge fallback
# 30bd8e3 chore(release): v0.9.12 — arena one-shot verification fixes
# 2e822ca fix(arena): one-shot pipeline verified — path resolution + Zod-spec prompts
# 480a4c0 docs(daily): 2026-05-05 handoff final — +curator EPIC, feed source model
# db67722 chore(cortex): 2 trailer dispatches
git status --short      # expected: R daily/README.md -> daily/_README.md (sort fix), 2 untracked runs/ dirs
bun run test:all        # expected: 78 + 13 + 21 = 112 pass, EXIT 0
cortex probe            # expected: All clear. 0 active epics, 0 active tasks.
```

## 1. Day 1 产出 (early session)
### EPIC-20260504230503067: Arena TOML declarative config (k8s-style) — DONE
- T1-T5: arena.toml schema/parser, player resolution, stats, reconciler, CLI

### EPIC-20260504231931835: Deck refresh/prune intent/plan/execution extraction — DONE
- T1-T5: buildRefreshPlan, buildPrunePlan, IO injection, executePlan, --dry-run

### Other
- probe fix: TASK-20260423124059736 Status History drift
- runs/ 28 deck wrappers de-absolute-pathed

---

## 2. Day 2 产出 (late session)

### EPIC-20260504235551635: test-utils fractal coverage extraction — DONE
| File | Before | After |
|------|--------|-------|
| `bdd-runner.ts` | 45% | 90% |
| `agents/claude.ts` | 42% | 89% |
| `sanitize.ts` | 90% | 100% |
| **Overall** | 85% | **94%** |

Key extractions:
- `buildClaudeCommand(opts)` → pure `SpawnCommand` DSL, 7 pattern-match tests
- `extractJson(raw)` → pure JSON extraction from markdown fence
- `buildToolPrompt` → exported + tested
- `slugifyWorkdirName`, `assertOutput` → standalone unit tests
- `runCli` injectable `SpawnFn` (default `spawnSync`, test: mock)
- `sanitize.restore()` round-trip tests

### Permission hardening
- Replaced `--dangerously-skip-permissions` with `--permission-mode bypassPermissions --allowedTools "...,WebSearch,WebFetch" --disallowedTools "Bash(rm *),Bash(sudo *)..."`
- AgentAdapter.spawn accepts `allowedTools` / `disallowedTools`

### EPIC-20260505015029961: Curator intent/plan/execute + feed source — DONE
- T1: curator-core.ts — parseFrontmatter, buildSkillMeta, formatMarkdownTable (15 tests)
- T2+T3: scanSkill uses curator-core + curator add command (clone to cold pool, no install)
- T4: SourceAdapter interface + ColdPoolSource + LobeHub/GitHub placeholder
- Architecture: Feed (discover) → Cold pool (add) → Working set (deck add), three layers

### Arena battle-testing (real Claude runs)
- Created playground/arena-growth-hack experiment (bare vs deep-research, growth hacking)
- Fixed multiple arena runner bugs found in real execution:
  - `parseArgs` missing `--config` `--out` `--players` flags
  - Relative paths break with `bunx` cwd → auto-resolve from configDir
  - Per-cell verdict persistence (both try and catch blocks)
  - comparative-judge 3x retry + Zod fallback (was crashing on LLM non-JSON)
  - Task file format must be `.agent.md` (needs `## When` section)
- Findings: `claude -p` with old flags produces empty stdout for web tasks

### Wiki + Docs
- Intent/Plan/Execute pattern wiki + AGENTS.md section
- READMEs: test-utils (new), deck/arena (coverage badges, architecture sections)
- Keywords added to test-utils package.json (11 keywords)

### Growth hacking research
- Curator empty (0 indexed skills) — needs scanning
- Discovered ecosystem: SkillsMP (800K), agentskill.sh (110K), skills.sh (91K), LobeHub (284K, agent-first)
- LobeHub already indexed lythoskill v0.6.x — needs update
- Key insight: desc = agent SEO battlefield, arena = anti-cheating mechanism
- TCG analogy complete: Player → DeckBox → Arena → Report → Curator → Coach

### EPIC-20260504230503067: Arena TOML declarative config (k8s-style) — DONE
- T1: arena.toml Zod schema + parser + buildExecutionPlan (pure, 14 tests)
- T2: player resolution + side mapping (pure, 9 tests)
- T3: runs_per_side statistical aggregation (pure, 9 tests)
- T4: declarative reconciler (runArenaFromToml, --dry-run)
- T5: CLI --config arena.toml + --dry-run
- 架构: intent/plan/execution 分离，dry-run 自然浮现

### EPIC-20260504231931835: Deck refresh/prune intent/plan/execution extraction — DONE
- T1: buildRefreshPlan (pure target collection + type classification)
- T2: buildPrunePlan (pure unreferenced detection)
- T3+T4: workdir/coldPool/deckPath injection + edge case tests
- T5: executeRefreshPlan / executePrunePlan with injectable IO
- IO 注入模式: { gitPull, delete, log } 可替换 → 测试时 capture buffer / no-op

### 其他
- probe 修复: TASK-20260423124059736 Status History 漂移（pre-hook legacy）
- runs/ 批量脱敏: 28 个 deck wrapper 绝对路径 → 相对路径

## 3. 关键决策

| 决策 | 内容 |
|------|------|
| Intent/plan/execute | 所有 EPIC 采用此模式；分形：package → command → test |
| IO 注入 | 函数式组合，不搞 DI；默认值兜底，参数覆盖 |
| Command DSL | `buildClaudeCommand` → `SpawnCommand`；剩余未覆盖 = Bun.spawn 胶水，TS 类型守护 |
| desc = agent SEO | SKILL.md desc 是流量入口，arena 是反作弊；growth hacking = 可验证的 SEO |
| Player | useAgent 承载，不新建抽象层；OpenClaw/Hermes 对接后就是 TCG deck box |
| Permission model | `--permission-mode bypassPermissions --allowedTools "...,WebSearch"` 替代 `--dangerously-skip-permissions` |
| LobeHub | 284K skills, agent-first；lythoskill v0.6.x 已被索引，需更新 |
| Arena 实战发现 | `claude -p` 子进程做 web research 时 stdout 为空 — 需要 WebSearch tool 权限 |

## 4. 测试矩阵

| 层级 | 测试数 | CI | 覆盖 |
|------|--------|-----|------|
| test-utils 单元 | 78 | ✅ | 94% lines |
| cortex CLI BDD | 13 | ✅ | — |
| deck 单元 | 71 | ✅ | 82% |
| deck CLI BDD | 21 | ✅ | — |
| arena 单元 | 41 | 待加入 CI | — |
| curator 单元 | 15 | ✅ | curator-core.ts |
| Agent BDD | 6 (本地) | ❌ skipIf | — |

## 5. 下一步

- `curator add` 用新 permission flags 验证 `claude -p` web search
- LobeHub 更新 lythoskill → v0.9.x
- Curator: 实现 LobeHub/agentskill.sh remote adapters (SourceAdapter)
- Growth hacking: 准备 LobeHub/agentskill.sh/skills.sh 提交材料
- OpenClaw/Hermes player adapter
- Arena 可视化 web app（TCG 卡组展示 + battle report + 分享）
- Deck `refresh`/`prune` 委托 curator

---

## 6. Arena 实战验证 (late session)

### 目标
验证 arena 用真实 Claude 调用跑通 one-shot pipeline。创建简单 smoke test（无 web search，避免已知的空 stdout 问题）。

### Bugs 发现 & 修复

| Bug | 位置 | 原因 | 修复 |
|-----|------|------|------|
| task 文件找不到 (ENOENT) | `runner.ts:106` | `resolve(taskPath)` CWD-relative 而非 configDir-relative | → `taskAbs` |
| deck 路径二次 resolve | `runner.ts:110` | `resolve(cell.deck)` 重复 resolve 已绝对化的路径 | → `cell.deck` |
| Per-cell judge criteria 格式 mismatch | `judge.ts` | LLM 输出 `criteria: {k: "PASS"}` (对象) 而非 `criteria: [{name, passed}]` (数组) | 添加 normalizeVerdictJson fallback |
| Comparative judge pivot-table 格式 | `comparative-judge.ts` | LLM 输出 `{bare: {correctness: 5}}` 而非 `{score_matrix: [...]}` | 添加 pivot→flat conversion |

### 架构决策

**Prompt 策略：Zod schema 字面声明 > 自然语言格式描述**

Conversation 中确认的关键洞察：
- Zod 语法在 LLM 训练数据里的密度远高于任意自然语言格式描述
- Agent 时代的 IoC 心态：把 spec（Zod schema）直接给 agent，而非 post-hoc normalize
- 保留 normalization 为 fallback（防御性），但主导策略是 prompt-level spec

两个 judge prompt 都改为直接嵌入 Zod schema literal：
- `comparative-judge.ts` → `z.object({ score_matrix: z.array(z.object({...})), ... })`
- `judge.ts` → `z.object({ verdict: z.enum([...]), reason: z.string(), criteria: z.array(z.object({...})) })`

### 验证结果
- Task: 写 greet.ts + greet.test.ts + bun test
- `playground/arena-one-shot/` — arena.toml + TASK-arena.agent.md + 2 deck configs
- 3 次 Claude 调用全链路通过：agent ×2 + comparative judge
- 全链路：agent execute → per-cell judge → stats → comparative judge → report with Score Matrix + Findings + Recommendations
- 测试套件：112 pass, EXIT 0

### 修改文件
- `packages/lythoskill-arena/src/runner.ts` — path resolution fix
- `packages/lythoskill-arena/src/comparative-judge.ts` — Zod-literal prompt + pivot flatten + normalization
- `packages/lythoskill-test-utils/src/judge.ts` — Zod-literal prompt + criteria normalization
- `playground/arena-one-shot/` — smoke test 整套（新增）

---

## Session Handoff — Curator 三层架构 + Feed/ColdPool 分离 (session 2)

### 0. Verify Current State

```bash
git log --oneline -3   # expected HEAD = 75f7878
git status --short      # curator-core.ts, cli.ts, curator-core.test.ts, README.md, SKILL.md modified; architecture.md new; daily/_README.md rename
bun test                # expected: 26 pass, 4 pre-existing fail (scanSkill YAML parse, not our regression)
```

### 1. 产出

#### curator-core 类型重构
- `SkillSource` → `Feed`（去 `cold-pool` type — 冷池是目的地不是源）
- `SourceAdapter` → `FeedAdapter`（包装异构上游：LobeHub/GitHub/agentskill.sh）
- `createColdPoolSource` → `scanColdPool`（同步本地扫描）
- `CuratorPlan { coldPool, feeds?, skillDirs }` / `AddPlan.feed`
- 新增 `SkillAddition` 类型 + `buildAdditionRecord` 纯函数

#### additions.jsonl 决策持久化
- `curator add --pool <dir>` 强制显式（无默认）
- `--reason <text>` / `--forked-from <locator>` flag
- clone 后写 `{pool}/.lythoskill-curator/additions.jsonl`（JSONL append-only）
- 状态机：added → evaluated → forked → activated

#### 文档
- `skill/references/architecture.md` — ASCII 架构图 + 三层信任 + 决策生命周期
- `README.md` — 命令列表更新，--reason/--forked-from 示例
- `skill/SKILL.md` — "推荐/排名 → Arena" 显式声明
- `daily/README.md` → `daily/_README.md`（排序修复）

### 2. 架构共识

| 决策 | 内容 |
|------|------|
| Feed / ColdPool 分离 | 冷池 = ~/.m2/repository，Feed = Maven Central/Nexus；Agent 优势 = 包装异构源 |
| 三层信任 | L1 卖家秀（SKILL.md）→ L2 Big V（Feed 排名）→ L3 买家秀（Arena 实测）；L3 是最终激活权威 |
| additions.jsonl | 冷池不只是缓存 — 是决策历史库（为什么买单、试了如何、是不是 fork） |
| 推荐/排名归 Arena | Curator 提供数据，Arena 提供评测。SKILL.md 显式声明 |
| bdd-lythos = fork 闭环实例 | 发现 → 试试 → Arena 评测 → fork → 再测 → deck link |

### 3. 本次 session 额外完成

- [x] 删除 `deck add --via skills.sh`（ADR-20260502012643444 accepted → 代码+文档清理）
- [x] `--as` → `--alias` 重命名（避免 JS/TS 保留字）
- [x] Deck SKILL.md: `--type combo` → `--type tool`（combo 是代理层，不应随意示例）
- [x] `daily/README.md` → `daily/_README.md`（排序修复）

### 4. 下一步

- [ ] `curator add` 用新 permission flags 验证 `claude -p` web search
- [ ] arena 写回 `additions.jsonl` 的 `arenaResult` 字段
- [ ] LobeHub/agentskill.sh 的 FeedAdapter 实现
- [ ] 修复 `scanSkill` 4 个 pre-existing YAML parse 测试
- [ ] 项目定位语 "declarative package manager (Maven) + runtime governance (K8s RBAC)" 推 keywords/repo desc
- [ ] 研究 Rosie (github.com/matthewp/rosie) — lockfile 共识 + 多 agent 同步

### 5. 关键洞察

- **combo = 代理层**：解决"同 niche 多实现 → agent 不知道选哪个"。它是 BFF 路由，不是业务实现。`fork` 比 `combo` 更常见。
- **deck add vs curator add**：两条独立路径到同一个冷池。deck add = 零摩擦快速安装（"我要这个"）。curator add = 决策追踪（agent 自动填 --reason）。不互相委托。
- **--reason 不要变成用户表单**：agent 从发现上下文自动填充。用户不应该被问"你为啥想加这个"。
- **工具保持单职责**：agent + memory 知道用户偏好（"给我看备选" vs "直接上"），工具不需要知道，agent 做路由。
