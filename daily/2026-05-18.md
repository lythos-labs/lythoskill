# Daily — 2026-05-18

## Ground Truth

**Git**: HEAD = bc44858 (docs(daily): session handoff)
**Version**: v0.14.5
**Deck**: 10 skills, vanilla.toml
**Tests**: 661 pass, 0 fail across 44 files
**Kanban**: 1 active epic (deepseek hardening), 2 new backlog tasks

## Session Handoff — Epics 2 & 3 (afternoon session)

### Completed

1. **Curator MVP Epic** (EPIC-20260518125955940) — closed
   - ADR-20260518123403810: curator = 策展者/买家秀 = 查卡器 + 备注 + 组卡审美
   - Niche: `deck_niche` frontmatter extraction → `curator tag` agent-enriched metadata
   - Audit: empty niche violation → legacy pattern detection
   - SKILL.md: rewritten with discovery SOP + fact-check + confidence evaluation
   - Write-through cache: `curator add` auto-indexes new skills
   - Reproduce.sh: 13/13 PASS (scan → tag → query → audit → legacy)
   - Showcase: curator-mvp/reproduce.sh, architect-deck/

2. **Emergency adapter fixes Epic** (EPIC-20260518145235543) — closed
   - `resolvePlayer('claude')` → `'claude-sdk'` (not deprecated CLI spawn)
   - deepseek + codex → arena optionalDependencies
   - codex import in runner.ts
   - localhost FQ cleanup: `localhost/<name>` → `localhost/me/<skill>` (8 locations)
   - Published v0.14.4

3. **Deepseek daemon discovery fix** — published v0.14.5
   - Root cause: `process.kill(pid, 0)` blocked in bunx sandbox
   - Fix: health-check discovery → probes cached port + lock port + base range
   - Sentinel: `pid: 0` when daemon discovered (not spawned)
   - **Still fragile**: deepseek agent mode ~40s for echo hello, >3min for real tasks
   - Arena default 120s timeout too tight — need longer timeout or warm daemon

4. **Journalist skill** (lythoskill-journalist) — created
   - 7 core rules, arena = multi-agent infra
   - Pure skill (no CLI, no npm publish) — like scribe/coach
   - Cold pool: `localhost/me/journalist`
   - Single tests: agent-orchestrated ✅, kimi ✅, claude ✅ (18 claims + WebSearch)
   - Deepseek: ❌ timeout (daemon fragility, not skill issue)

5. **ADR-20260518155038335**: reproduce.sh + decision-log + logical framework — accepted
   - Structured decision-log schema (premise + relation + conclusion)
   - CLI mechanical checks: confidence propagation, contradiction detection, leverage analysis
   - Conjunction fallacy detection: `confidence(C) > min(confidence(P_i))` when AND → flag
   - Bayesian nuance: prior vs evidence separation — NOT blindly flagging AND
   - Detective reasoning: informative likelihood can justify high posterior under Bayes
   - Coinflip analogy: 10 heads → doubt the coin, not the probability

6. **Creator skill updated**: Skill+CLI vs Pure Skill distinction, coach integration at Step 2

### Key ADRs accepted (this session)
- ADR-20260518123403810: Curator role re-derivation
- ADR-20260518155038335: Reproduce.sh + decision-log + logical framework

### Key cognitive insight
Classical vs Bayesian probability **language-game** in verification:
- Classical: P(A∧B) ≤ min(P(A), P(B)) — iron law
- Bayesian: P(A∧B | evidence) can genuinely be > P(A | evidence) when evidence is informative
- CLI should NOT blindly flag AND with confidence > min(premises) as fallacy
- CLI SHOULD flag AND where premises lack independent evidence (circular, author-only)
- Detective reasoning (rare condition → high suspicion) is legitimate Bayes, not fallacy
- Coinflip 10 heads: classical says (1/2)^10, Bayesian says coin is rigged

### Active
- EPIC-20260518153034640: deepseek adapter hardening (health-check discovery done, timeout remains)

### Pitfalls
- **localhost FQ 残留**: `localhost/<name>` (2-segment) 反复被 agent 复活，误导了多个 session。FQ-only 后统一为 `localhost/me/<skill>` (3-segment, host/owner/repo 对齐)。8 处清理。
- **bunx sandbox + process.kill(pid,0)**: daemon 发现不能依赖 PID——bunx 沙箱里 signal 0 被拦截。必须用 health-check。
- **Pure skill 不需要 npm publish**: journalist 包误加了 package.json + publish.sh 条目，后移除。creator skill 已更新说明。

### Verify Current State
```bash
git log --oneline -5   # Should show: 053e794 v0.14.5, dba8aac deepseek fix, ...
git status --short      # Should be clean (or minor INDEX.md drift from cortex commands)
```


## Session Handoff — Journalist→Sober Evolution + README Rewrite (evening session)

### Completed

1. **Journalist → Sober rename** (`packages/lythoskill-journalist/` → `packages/lythoskill-sober/`)
   - Pure skill (no CLI, no npm publish)
   - Validated SKILL.md with combo pipeline hints, PAUSE directive, confidence gate
   - 4 references: example-evaluation.md, reproduce-scenarios.md, meta-cognitive-scenarios.md, combo-patterns.md
   - Cold pool: `localhost/me/sober`

2. **Blind arena testing** (6 scenarios, 2 players)
   - Claude: 0/6 explicit trigger, 6/6 SOP followed (subliminal activation — innate ideal)
   - Kimi: 4/6 explicit trigger, 6/6 SOP followed (explicit methodology)
   - B3 (RFC data) most discriminating scenario
   - Results archived: `showcase/sober-journalist-evolution/blind-tests/`

3. **A/B comparison** (journalist vs sober, innate placement, same B3 scenario)
   - Sober found critical blocker (`workspace:*` publish issue) that journalist missed
   - Sober: 16 decisions, 4-level risk grading. Journalist: 14 decisions.
   - Results: `showcase/sober-journalist-evolution/ab-comparison/`

4. **Meta-cognitive pipeline test** (M1 — Technical Due Diligence)
   - Sober activated explicitly (cited Practice 1-6 in decision-log)
   - Curator NOT triggered — correct: agent could execute with own capabilities
   - 447-line due diligence report with k6 load test script, Go/No-Go framework

5. **README rewrite** — 4 rounds of sober zero-knowledge review
   - v1: 7 major issues (npm/k8s overreach, analogy overload, mixed audience)
   - v2: Mostly addressed (analogies moved out, content architecture fixed)
   - v3: 7/8 YES on intent challenge (PARTIALLY: try-first flow weakened by gatekeeper)
   - v4: HIGH confidence. Two remaining fixes: typo "lythosill" + lockfile angle
   - Final structure: magic moment → pain point → decision table → how it works → ecosystem
   - Key changes: npm/k8s analogies out, "built by AI agents" removed, Vercel comparison added, magic moment via quick-init.sh

6. **quick-init.sh** — one-command governance setup
   - `curl | bash` pattern, installs Bun, creates deck, links, self-checks
   - Self-check = sober pattern: verify what you just did

7. **references/** created
   - `comparisons.md`: npm/Maven/k8s/Go/Cargo analogies with "Not shared" disclaimers
   - `landscape.md`: full ecosystem landscape (61,776 skills, 30+ platforms, pain points, security data)

8. **.gitignore**: skill-deck.lock removed — lockfile now committed like package-lock.json

### New Cortex Tasks
- TASK-20260518172921265: arena CLI single output leaks to CWD
- TASK-20260518212223198: implement `also_link_to` multi-platform fan-out

### Key Cognitive Insights

**Sober as innate**: Subliminal activation (Claude 0/6 explicit, 6/6 SOP) is the ideal for innate skills — shapes cognition without the agent needing to "invoke" a tool.

**Cross-player Pareto**: Claude (analytical, gap-finding) and Kimi (compliant, methodology-following) are non-dominated — both valid activation paths for different use cases.

**Skill name as signal**: "journalist" → "sober" changes the cognitive posture from "investigate this" to "stay clear-headed." The name visible in the skill list IS the first hint.

**Convergence validation**: Sober review must converge, not swing. v1 found 7 issues, v4 found HIGH confidence. If every review finds new issues regardless of changes, the review process is broken.

**Meta-cognitive pipeline**: Sober → curator → deck assembly needs a stronger trigger for the curator step. Agent defaults to "I can do this myself" unless clearly outmatched.

### Pitfalls
- **Kimi output leakage**: CLI `single` without `--out` writes to `./agent-output-<timestamp>/` in CWD. Registered as TASK-20260518172921265. Workaround: always pass `--out`.
- **`also_link_to` claimed but not implemented**: Found by sober fact-check. README now says "configure working_set per platform" instead of "fans out to 6 platforms."
- **Test count was stale**: README said 719 tests — actual count is 661.
- **"physically absent" inaccurate**: Skills ARE on disk (in cold pool), just not in working set. Changed to "absent from the working set."

### Files to Commit
- `README.md` — full rewrite
- `packages/lythoskill-sober/` — new (from renamed journalist)
- `packages/lythoskill-journalist/` — deleted
- `references/comparisons.md` — new
- `references/landscape.md` — new
- `examples/quick-init.sh` — new
- `showcase/sober-journalist-evolution/` — new
- `playground/arena-blind-20260518/` — archive data
- `playground/arena-ab-sober-vs-journalist/` — archive data
- `cortex/tasks/01-backlog/TASK-20260518172921265-*` — new
- `cortex/tasks/01-backlog/TASK-20260518212223198-*` — new
- `.gitignore` — skill-deck.lock removed
- `daily/2026-05-18.md` — updated

### Next Steps
- storyteller skill: narrative polishing for README (different audiences see different layers)
- `also_link_to` implementation (TASK-20260518212223198)
- arena output default path fix (TASK-20260518172921265)
- Deploy Kimi showcase page link in README/landscape
- Consider: make sober an official published package

## Session Handoff — README Polish Round (late evening)

### Completed

1. **Four-pillar litmus test moved** — from opening blockquote to "How it works" section. The "If any of these four are false, it's not lythoskill" language is internal design conviction, not an effective public opener.

2. **For Agents repositioned** — now sits right before "Quick Start" (collapsed). Agents scanning the README encounter their entry point before anything else. Humans see a fold.

3. **Quick Start inverted** — "Give this to your agent" is now the primary path. "Or run it yourself" is secondary. The agent-era quick start: tell your agent, not your terminal.

4. **Badges restored** — DeepWiki, TypeScript, ESM badges accidentally removed during rewrite. All recovered.

5. **"Remote" → "Sources"** — cold pool diagram mislabeled as "Remote" when it includes localhost. Fixed.

6. **`working_set` "(default)" removed** — troubleshooting table no longer implies `.claude/skills/` is the default.

7. **deepseek-tui topic restored** — fact-check confirmed DeepSeek-TUI supports skills (`.agents/skills/` primary, also reads `.claude/skills/`).

8. **GitHub About refreshed** — description uses positive framing only, no "Not a X" defensive language. Topics cleaned from 15 to 11.

9. **Storyteller review** — applied Before-After-Bridge framework. Key insight: agent-first quick start is the differentiator; dogfooding and "give this to your agent" deserve elevation.

### Verify Current State
```
git diff HEAD --stat    # Only README.md changed (33 insertions, 35 deletions)
git status --short      # M README.md only
git log --oneline -3    # 369d3f7 feat(sober): rename + README rewrite
```

### Next Steps
- storyteller skill: still needs cold pool discovery (network blocked on gh add)
- kimi external sober fact-check: ran but produced empty output — may need simpler prompt
- `also_link_to` implementation: TASK-20260518212223198 in backlog
