# Daily — 2026-05-18

## Ground Truth

**Git**: HEAD after bump v0.14.2
**Versions**: v0.14.2 (bump pending commit)
**Deck**: 10 skills, vanilla.toml as first-class citizen in examples/decks/
**Active**: cortex empty shell fix validated, AGENTS.md minimal template proven

### What changed

1. **Cortex Step 1/2/3 control transfer** (task/epic/adr/wiki):
   - All create commands output: `✅ Step 1/3: CLI created → 🔄 Step 2/3: YOUR TURN → ⏳ Step 3/3: probe verify`
   - Positive framing: "YOUR TURN — edit the file, fill these sections" replaces punitive "STOP HERE"
   - Template placeholders use `⚠️ REQUIRED` markers for probe grep detection
   - wiki.ts aligned with same pattern (was old-style `✅ Created:`)
   - Zero-knowledge subagent PASS: all 4 creates triggered fill with substantive content

2. **Control Transfer Protocol validation**:
   - Vanilla seed bootstrap: 79-line AGENTS.md + vanilla deck → zero-knowledge subagent correctly understands project, self-expands 1→5 skills, self-heals network failure
   - Cortex ZK test: subagent sees "YOUR TURN" output → fills all sections with production-quality content
   - Both tests run in `/tmp`, zero workspace pollution

3. **Archaeology wiki**: `2026-05-17-arena-cli-archaeology-and-agent-os-design-principles.md`
   - Git stratigraphy methodology: 10 strata of arena CLI evolution
   - Five discoveries: IO stripping as accident sediment, commits as SOP log output, guard system as cognitive blind spot, Git as causal chain, Cortex as Agent OS
   - 6502 bank switching metaphor formalized

4. **ADR-20260517224131119 accepted**: Multi-layer context persistence (4 options → selected C+D)
   - AGENTS.md + daily/ + memory/ + cortex/wiki/ formalized as four-layer architecture
   - Mnilax 12 rules mapped to lythoskill practices
   - Subdirectory AGENTS.md confirmed viable for monorepo packages

5. **Vanilla deck**: `examples/decks/vanilla.toml` as first-class seed bootstrap deck (scout deprecated)
   - INDEX.md updated

6. **Coach references**: Mnilax methodology (200-line ceiling, close-specific-failure, testable imperatives) added as coach review framework

### Uncommitted changes
```
M  packages/*/package.json (bump 0.14.2)
M  skills/*/SKILL.md (rebuild)
M  package.json, bun.lock
```

### Pitfalls
- Pre-commit hook auto-committed other agent's changes as separate commits — commit history shows 3 interleaved agents (0098d5d, ce0fc15, 8119fc6)
- `deck link --workdir` with wrong --deck flag first linked parent project's deck, had to retry with explicit --deck

### Remaining next steps
- [ ] Publish v0.14.2 to npm
- [ ] Deck refresh
- [ ] Probe to be enhanced to grep for `⚠️ REQUIRED` / `PLACEHOLDER_` markers (template ready, probe logic not yet)
- [ ] AGENTS.md actual distillation to ~150 lines (template validated at 79 lines, not yet deployed as live AGENTS.md)
