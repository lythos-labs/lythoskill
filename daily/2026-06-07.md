# Daily — 2026-06-07

## Ground Truth

**Git**: HEAD = `84efd6f` (chore(cortex): complete TASK-20260607000945113)  
**Version**: v0.16.0  
**Deck**: 14 skills linked  
**Branch**: main, clean working tree, up to date with origin  
**Active Epic**: 0  
**Active Task**: 0  

## Session Handoff — AGENTS.md v2 Smoke Test Validation + CI Fix

### Completed

1. **AGENTS.md v2 收尾**
   - `ef33581` Boot phase / Start of Session 使用完整可执行命令（`bun packages/...`），替代 `deck link` / `cortex probe` 缩写
   - 用户反馈：缩写会在无 alias 环境失败

2. **Smoke Test 全链路验证**
   - Branch: `smoke/agents-md-v2-20260606`（已废弃并删除）
   - 注入 3 个异常：stale in-progress task、README typo、bare-name deck locator
   - Subagent 1（zero-knowledge discovery）：完整执行 Boot First，发现 3 注入 + 3 自然债务
   - Subagent 2（fix + governance）：用 `cortex review` + `Closes:` trailer 关闭 task，删除 fixture，清理 temp artifacts
   - 结论：AGENTS.md v2 可被 zero-knowledge agent 自举执行，discovery / closing / governance 全通过
   - Wiki lesson: `cortex/wiki/03-lessons/2026-06-06-agents-md-v2-smoke-test-validation-lesson.md`

3. **CI 修复**
   - Push 后 CI 失败：`packages/lythoskill-agent-adapter/src/adapters/kimi.test.ts` 在 CI 无 `kimi`/`kimi-cli` PATH 时抛错
   - 注册 `TASK-20260607000945113`
   - 修复：`buildKimiCommand` 增加可注入 `binary` 参数，测试不再依赖 PATH；新增 dormancy test
   - CI run `27067276270` 全部绿灯（test + 3 coverage jobs）

### Key Decisions

- **命令必须完整可执行**：AGENTS.md 中所有直接让 agent 跑的命令，不能用 shorthand
- **Smoke branch 是耗材**：注入异常 → 验证 → 写报告 → 删除 branch，不合并 fixture commits
- **Pre-existing CI failure 也要先 task 再修**：虽然是别人代码，但按 "see a bug, fix a bug" 走完整 governance

### Pitfalls

- **Subagent 对 shorthand 敏感**：Smoke test Round 1 证实 `deck link` / `cortex probe` 在 zero-knowledge 环境会失败；庆幸用户及时纠正
- **CI 失败 ≠ 本次改动导致**：要学会先看历史 run 列表，避免 panic
- **Monitor 工具不适合短轮询**：本次用 `gh run view` 直接查询更高效

### Next Steps

1. **Node.js 20 deprecation**：CI warning 来自 `actions/cache@v4` / `actions/upload-artifact@v4`，未来需要升级
2. **Daily 2026-06-06 历史 ground truth 已 stale**：不追写旧 daily，本日文件为新 SSOT
3. **可考虑将 smoke-test fixture 模式化**：写入 `showcase/` 或做成 deck，方便下次 BIOS 层改动复用

