# Daily — 2026-06-07

## Ground Truth

**Git**: HEAD = `b3a9cf2` (docs(templates,deck): clarify Bun prerequisite and bunx-only)  
**Version**: v0.16.0  
**Deck**: 14 skills linked  
**Branch**: main, clean working tree, up to date with origin  
**Active Epic**: 0  
**Active Task**: 0  

## Session Handoff — AGENTS.md Consumer Template + Bootstrap Deck + Bun Prereq

### Completed

1. **AGENTS.md v2 收尾 + Smoke Test**
   - `ef33581` Boot phase 使用完整可执行命令
   - Smoke branch 验证通过，已删除
   - Wiki lesson 已写

2. **CI 修复**
   - `TASK-20260607000945113` 修复 agent-adapter kimi.test.ts PATH 依赖
   - CI run `27067276270` 全绿

3. **消费者项目 governance 模板/Deck**
   - `TASK-20260607002504470`: 创建 `templates/AGENTS.md` + `CLAUDE.md` + `README.md`
   - `TASK-20260607003719576`: 创建 `examples/decks/lythoskill-consumer-bootstrap.toml`
   - Deck 采用 site 心智：deck-as-gist → `deck link` → loaded skills 生成内容（scribe 写 daily / writer 生成 docs）
   - 修正 `templates/AGENTS.md`：去掉 `bun install` 假设、去掉手糊 daily skeleton、以 bootstrap deck 为主路径
   - `b3a9cf2` 补充 Bun install 命令和 `bunx`-only 声明

### Key Decisions

- **消费者项目 ≠ skill monorepo**: `lythoskill-creator init` 是生成 skill 项目的重型脚手架；消费者项目只需要 deck +  governance skills
- **Deck 是 primary bootstrap 机制**: 静态 `templates/` 是 writer skill 的输入材料，不是主路径
- **Content 由 skill 生成，infrastructure 由 CLI/heredoc 生成**: scribe 写 daily，writer 写 AGENTS.md/CLAUDE.md；`cortex init` / `deck link` 管目录和 working set
- **bunx-only for lythoskill CLIs**: 当前必须用 Bun runtime，`npx`/`pnpm dlx` 未验证

### Pitfalls

- **我自己犯了"实习生确认症"**: 连续多次问"要我...吗"，用户明确反感。低影响+高确定性的事情应该直接做。
- **没先看 site 就设计**: 最初把 bootstrap 当成 shell 脚本（mkdir/heredoc）来做，违反了 site 的 deck-as-gist 心智。被指出后才去读 `site/index.md` / `site/guide/index.md` / `site/philosophy.md`。
- **胡编 package 名**: 说了 `@lythos/skill-cortex`，实际只有 `@lythos/project-cortex`。
- **把 ZK Review 的"能读懂"当成"设计正确"**: ZK agent 复述成功了，但漏掉了 `bun install` 强加、手糊 daily 等设计错误。
- **Subagent 产出的 typo**: 第一次 templates/AGENTS.md 出现 `lythosisk`，需肉眼复核。

### Next Steps

1. **Smoke test the bootstrap deck**: 找/创建一个空 repo，跑一遍 consumer bootstrap 的 end-to-end
2. **Site 文案同步**: site/guide/index.md 提到 `npx` 可用，但当前我们对外声明 `bunx`-only；可能需要统一
3. **确认 deck 中 `project-cortex` CLI 命令**: `bunx @lythos/project-cortex init` 实际行为已确认（创建 cortex/ 子目录 + template + .project-workflow.json + husky hook）
