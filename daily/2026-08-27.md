# Daily — 2026-08-27

## Session Handoff (night) — v0.17.9 OIDC release pipeline validated end-to-end

### 0. Verify Current State

**Git**: HEAD = `47b0286f` (chore(release): v0.17.9)
**Version**: v0.17.9
**Deck**: 14 skills linked
**Branch**: main, clean working tree, pushed to origin
**Active Epic**: 0
**Active Task**: 0 in-progress, 0 in review
**CI**: `release.yml` ran successfully on `v0.17.9` tag (publish job + pages job both green)

```bash
# Verify: if output diverges from the fields above, this handoff is stale
git status --short && git log --oneline -5
```

### Completed

1. **Closed TASK-20260827170159682** — tag-triggered release pipeline with OIDC npm provenance. Moved from `03-review` to `04-completed` via cortex CLI after validating the v0.17.9 release.
2. **Validated v0.17.9 release artifacts**
   - npm: all 13 `@lythos/*` packages updated to `0.17.9` with provenance attestations in `dist.attestations.provenance`.
   - GitHub Release: https://github.com/lythos-labs/lythoskill/releases/tag/v0.17.9 created automatically.
   - Pages: https://lythos-labs.github.io/lythoskill/ returns 200 and HTML contains 6 occurrences of `0.17.9`.
3. **Updated release-auth-workflow.md** — documented three hard-won OIDC lessons:
   - `actions/setup-node` must **not** set `registry-url`; a placeholder `NODE_AUTH_TOKEN` blocks OIDC exchange.
   - Every publishable package needs a `repository` field or `npm publish` fails with `E422` during provenance attestation.
   - npm Trusted Publisher package names are `@lythos/skill-*`, not the repo folder names `lythoskill-*`.
4. **Rebuilt `lythoskill-creator` skill** from the updated reference doc; `skills/lythoskill-creator/references/release-auth-workflow.md` now matches source.
5. **Confirmed pre-existing safeguards**: tag protection ruleset `protect-v-tags` already active; site footer already shows version/commit/"Last updated" via `site/scripts/inject-version.ts` + `site/.vitepress/config.ts`.
6. **Fixed cortex CLI UX trap** — `cortex task list` no longer creates a task titled "list"; added `task list`, `epic list`, `adr list` aliases that call the list command (TASK-20260827234446584).

### Key Decisions

- **Two-job `release.yml` layout is correct**: `publish` job runs with **no** GitHub Environment (so npm OIDC claims match the no-environment Trusted Publisher); `pages` job uses `environment: github-pages` for Pages deployment. Mixing the two breaks npm auth.
- **Local agent only bumps + pushes tag**: npm publish, GitHub Release, and Pages deploy are now entirely CI-owned. No long-lived npm token in the repo.
- **Keep local fallbacks for now**: `scripts/publish.sh` and `scripts/publish-github-release.sh` remain as emergency fallbacks; workflow still reads the package list from `publish.sh`.

### Pitfalls

- **Trusted Publisher environment mismatch**: initial attempts pointed the publisher at an environment or the wrong workflow path. The working config is `lythos-labs/lythoskill/.github/workflows/release.yml` with **no environment**.
- **Provenance requires `repository` field**: 5 packages (`agent-adapter-claude-sdk`, `agent-adapter-codex`, `agent-adapter-deepseek-serve`, `skill-creator`, `skill-deck`) were missing `repository` and failed with `E422` on the first v0.17.7/0.17.8 attempts. Fixed in commit `cb2ffba6`.
- **`setup-node` `registry-url` silently breaks OIDC**: earlier draft of `release.yml` included `registry-url`, which made npm use a placeholder token instead of exchanging the OIDC token. Removing it was the final fix for provenance.
- **Package name confusion**: npm package `@lythos/skill-creator` corresponds to repo folder `packages/lythoskill-creator`; configuring the wrong name in Trusted Publisher silently does nothing.

### Next Steps

1. **Add tag protection rule** for `refs/tags/v*` — already done: ruleset `protect-v-tags` exists on the repo (`gh api repos/lythos-labs/lythoskill/rulesets`).
2. **Decide on routine release-entry UX**: user wants clearer entry points for daily routine (hash tag, "last updated/published at", script renaming, ADR comparing publish-order options). Capture in a new task or ADR.
3. **Investigate GitHub token permissions**: user offered to apply for a dedicated fine-grained PAT with documented permissions (`contents=write`, `issues=write`, `pull_requests=write`, `pages=write`, `workflows=write`, `actions=write`) and store it in macOS Keychain as `lythos-agent-pat`. Update AGENTS.md token guidance once the new token is in place.
4. **Consider GitHub App as long-term auth**: user mentioned other agent advice that GitHub App installation tokens are the "never expire" automated path. ADR-worthy if we want to migrate from PAT.
5. **Address 2 stale backlog tasks** from probe: `TASK-20260710115319209` and `TASK-20260710115434689` — already suspended today (2026-08-27) with note "Blocked"; they are intentionally not in backlog. Daily earlier misreported them as unaddressed.

### Temp Artifacts

- 无（全部产物已入库：task card 完成、release-auth-workflow.md 更新、skills/lythoskill-creator/ 重建、本 daily 追加）

---

## Session Handoff (evening) — tag-triggered release pipeline + OIDC npm provenance

### 0. Verify Current State

**Git**: HEAD = `7cac44b4` (chore(cortex): move release pipeline task to review)
**Version**: v0.17.3（已发布，无新版本动作）
**Deck**: 14 skills linked
**Branch**: main, clean working tree, pushed to origin
**Active Epic**: 0
**Active Task**: 0 in-progress, 1 in review (TASK-20260827170159682)
**CI**: `test.yml` unaffected; new `.github/workflows/release.yml` added but not yet triggered

```bash
# Verify: if output diverges from the fields above, this handoff is stale
git status --short && git log --oneline -5
```

### Completed

1. **Accepted ADR-20260827165402810** — tag-triggered GitHub Actions release pipeline with OIDC npm provenance. Moved from `01-proposed` to `02-accepted` via cortex CLI.
2. **Created + implemented TASK-20260827170159682** — added `.github/workflows/release.yml` triggered on `v*` tags; runs tests, rewrites `workspace:*`, publishes all 13 `@lythos/*` packages via npm OIDC, creates GitHub Release, deploys docs site to Pages, and runs `check-published-manifests.ts` tripwire.
3. **Updated release SOP** — `AGENTS.md` and `packages/lythoskill-creator/skill/references/release-auth-workflow.md` now describe the new tag-push flow; local `publish.sh` + `publish-github-release.sh` marked as transition fallbacks.
4. **Built skills** — `lythoskill-creator` skill rebuilt from updated reference doc; pre-commit hook rebuilt all 14 skills and synced `skills` branch.
5. **Pushed** — `main` and `skills` branches pushed to origin. Verified site footer still shows `lythoskill v0.17.3 · 26bf694 · Last updated: 2026-08-27`.

### Key Decisions

- **Tag is the trigger**: local agent only bumps and pushes the tag; Actions owns npm publish, GitHub Release, and Pages deploy.
- **npm in Actions, not Bun**: only npm supports OIDC trusted publishing + automatic provenance in GitHub Actions today.
- **Keep `scripts/publish.sh` as SSOT**: workflow extracts the package list and order from the existing `PACKAGES` array, so the script stays authoritative even as the primary executor moves to CI.
- **Per-package Trusted Publisher required**: npm does not offer org-level auto-application for `@lythos` scope; each package must be configured once on npmjs.com.

### Pitfalls

- **Compaction summary was not proof**: the summary claimed the ADR was already pushed, but `git status` showed it as untracked. Always verify state with tools rather than trusting compressed notes.
- **`gh release view --json isLatest` does not exist**: the field is called differently; use `publishedAt` / `targetCommitish` to verify a release.
- **Pre-commit rebuilds all skills**: editing one `packages/*/skill/references/` file caused the hook to rebuild all 14 skills and stage them, enlarging the commit. This is expected but easy to miss if you only watch `git status` before committing.

### Next Steps

1. **Configure npm Trusted Publishers** (user/admin task): for each existing `@lythos/*` package on npmjs.com, add `lythos-labs/lythoskill/.github/workflows/release.yml`. Until then, new releases for unconfigured packages must use the local fallback.
2. **Test the new pipeline** with the next patch release: bump → `git push --follow-tags` → watch `release` workflow run; verify npm provenance badge appears and GitHub Release is created.
3. **Add tag protection rule** for `v*` (repo admin settings) to prevent overwrite.
4. **Address 2 stale backlog tasks** from probe: `TASK-20260710115319209` and `TASK-20260710115434689` (48 days old).

### Temp Artifacts

- 无（全部产物已入库：release.yml、AGENTS.md、release-auth-workflow.md、skills/ 重建、ADR、task card、本 daily 追加）

---

## Session Handoff — 复健:TASK-1284 关闭 + weekly 链补齐 + dreaming SSOT 巩固

### 0. Verify Current State

**Git**: HEAD = 本会话提交序列最后一个(docs daily 提交，以 `git log -1` 为准）— **未推送**
**Version**: v0.17.3（已发布，无新版本动作）
**Deck**: 14 skills linked（未重新 link，无 drift 信号）
**Branch**: main,clean working tree（提交后）
**Active Epic**: 0
**Active Task**: 0 in-progress, 0 in review(TASK-1284 已 completed)
**CI**: 07-31 已转绿（run 30608402270)；本会话无代码改动，CI 不受影响

```bash
# Verify: if output diverges from the fields above, this handoff is stale
git status --short && git log --oneline -6
```

### Completed

1. **TASK-20260730140801284 关闭** — 27 天间隔后 onboarding 确认 07-31 handoff 与 git 现实完全一致；用户确认外部可用 → 验收 R1-R4 勾核 → `Closes:` trailer,hook 自动入 04-completed,probe 一致。
2. **Weekly 链补齐（catch-up 模式 A3)** — 新增 W29（冷池 drift + 蜕皮启动）、W30(v3 采纳，4 commits 轻周）、W31(epic 收官 + 0.17.2 泄漏事故 + 0.17.3 修复，22 commits)、W32-34（空周，沿用 W27 格式）、W35（本周，部分周）。链 W17→W35 连续。
3. **Dreaming 巩固（Scan → Consolidate → ZK Validate)** —
   - `pitfalls.md`:§4 更新（冷池 drift 机械化 + await 静默失效）+ 新增 §12（发布会话记命令）§13(guard 问 registry/fail-closed/管道吃退出码）§14（环境依赖 fixture + gh keyring 401)§15(API 403 伪装会话结束）
   - `key-decisions.md`:补 7 个 ADR(rows 58-64,06-07→07-17)+ 发布 tripwire 非 ADR 决策 + How to Read 全局编号说明
   - `project-hotspots.md`:补 publish.sh + check-published-manifests.ts 行
   - ZK subagent 验证：8/10,P1(§14 时间线自相矛盾）+ P2 全部就地修复，frontmatter 标记 zk_validated
4. **GitHub Pages 站点修复** — gh 确认：CI workflow 绿（07-31 起）,CodeQL 周检绿（最新 08-25)；但 **Pages deploy 自 07-10 连红**，根因 = `site/drafts/`（被拒草稿，07-10 移入）含未转义 `<...>` 炸 Vue SFC 解析 → config `srcExclude` 加 `drafts/**`，本地 build 复现+转绿。同时发现**对外 quick-start 的 deck 文本已失效**:mattpocock/skills 上游 `diagnose` 改名 `diagnosing-bugs`(07-19 我们修过自己的 deck,site 的 8 处没跟上）→ 全修。线上站点最后成功部署停在 06-15,`@latest` 指向 0.17.3（干净），修完推送后外部用户即可照站自学。
5. **Pages 保鲜三件套（A+B+C，用户拍板全做）** —
   - A:`test.yml` 加 `site-build` job（只 build 不 deploy)——炸构建的 commit 当场红在大家盯的 CI 里，不再等 deploy workflow 偶然被发现
   - B:`examples/decks/quick-start.toml`(quick-start deck 成为 SSOT，自动进入 validate-example-decks 的 CI 远程验证）+ `scripts/check-site-deck-snippets.ts`(site 里每个 `path =` locator 必须存在于某个已验证 deck;**负测试通过**：假 locator  exit 1 + 响亮报错）；站点三 tab 收敛为单块 + "其他 agent 改 working_set 一行"
   - C:AGENTS.md Submit SOP 加"site/** 变更时检查 Deploy VitePress workflow"(CI 绿 ≠ 站点绿）
6. **Articles 栏目纳入（5 篇 drafts → site/articles/)** — 两轮 swarm + 两轮 ZK:
   - Round 1:5 个子代理按 writer 标准评审+事实核查+修复（全部 yes-with-fixes 发布）；建 `site/articles/` + nav(en+zh)
   - ZK 一验：**4/10**——5 篇近重复（同源 DeepWiki Q&A 改写 5 遍）、自相矛盾的表格单元格、缓存隐喻方向反转、"monolith"褒义误用、自颁奖章式 badge
   - 重构：canonical(agent-boosted-ux)+ 4 篇 companion 只留 delta + cross-link;ZK 二验 6/10，残余 P1(companion 谎报自身范围）+ P2（数字错、hedge 分叉、index 过度宣称）全部就地修复
7. **writer 技能发布门制度化（E)** — `packages/lythoskill-writer/skill/SKILL.md` 新增 "External Article Publication Gate" 六门：writer 标准 → 事实核查 → VitePress 尖括号扫描 → 栏目冗余检查 → ZK 可读性 → 可操作内容实操验证；已 build + link(14 skills synced)
8. **Pages ZK 双维评估（用户定制指标：外部 ZK agent 能否顺着布局+叙事链低摩擦入驻）** —
   - **叙事链走查（agent-13):7/10**。主页→guide 是好 on-ramp;P1×2:Level 0 不说工作目录 + deny-by-default 警告在代码块**之后**（信任违规）;Level 3 arena 前置条件全文未提；P2：首页混入中文"试试看？"、Kimi 引言未翻译、Level 1 标题重复、"ZK subagent"第四页才定义
   - **实操入驻试验（agent-14,/tmp/zk-onboard-test):6/10**。**Level 0 一次通过**(link exit 0,2 skills,deny-by-default 实测生效——核心承诺完全兑现）；但 **Level 2 命令 100% 不可用**:site 写 `bunx @lythos/curator scan` 而真实包名是 `@lythos/skill-curator` 且没有 scan 子命令（CLI fail-open 把 "scan" 当池路径，exit 0 + 造垃圾目录）；切换 working_set 旧目录不清理（与"no leftovers"宣称矛盾）;link 输出每个技能打两遍
   - **修复（文档侧全部落地）**:guide 双语 Level 0 重写（工作目录 + 警告前置 + 成功判据）、curator 正确命令（10 处，4 文件）、arena 前置段落、index 双语（去中文残留、翻译引言、working_set 切换警告、pillar 表）、architecture 双语 curator 命令
   - **产品 bug 登记（制度：先登记后修）**:TASK-…34103(curator CLI fail-open + YAML 噪音）、TASK-…34189(working_set 切换语义决策）、TASK-…34254(link 重复输出），卡片已填实

### Key Decisions

- **catch-up 粒度 = 每周一文件**：空周也补（W32-34)，保持 weekly 链作为时间索引的连续性（Long-Range Consistency Principle)。
- ** dreaming 范围克制**：只更新受 W29-W31 事件影响的 3 个 SSOT 文件，不全量重写 9 个。

### Pitfalls

- **ZK 实操是文档的最终裁决者**:site 的 curator 命令错在包名和子命令两个层面，文档评审（包括今天的 ZK 叙事走查）都没抓出来——只有真跑 `bunx` 的代理撞出 404。writer 发布门第 6 条（可操作内容必须实操）由此有了第一个实战判例。
- **配额 403 又中断了一次**(ZK 双代理首次启动时）:resume 原 agent 续跑成功，无状态损失——逐批落盘习惯再次验证有效。
- **Pages deploy 失败不在 CI workflow 视野里**:deploy 是独立 workflow,CI 绿 ≠ 站点绿。07-10 起连红 5 次无人发现（期间 gh 401 + 无活动）。查站点健康要 `gh run list --workflow="Deploy VitePress site to Pages"`。
- **上游改名是持续性风险**:mattpocock/skills `diagnose`→`diagnosing-bugs` 这类上游 rename,site 里的示例 deck 文本没有 guard 能抓（只有真人外部试用或手动 `deck validate` 能发现）。
- **SSOT 写作也会引入新错误**:§14 初稿"failed CI for weeks"与"main red since 07-27"(4 天）自相矛盾，被 ZK 抓出。教训同 §11:SSOT 修改必须过新鲜眼睛，本次流程（ZK validate）自证有效。
- **7 个 ADR 沉积 2 个月未入 key-decisions**:last_consolidated 停在 06-01，说明 dreaming 节奏一断，SSOT 就漂移。weekly 链是 dreaming 的索引——链先补齐，dreaming 才能低成本进行。

### Next Steps

1. **推送**：用户确认后 push（本会话 6 个提交未推送；推送后盯 Pages deploy workflow 是否转绿——`GH_TOKEN=$(<.github-token) gh run list --workflow="Deploy VitePress site to Pages"`)。
2. **外部试用复验（可选）**：推送+部署转绿后，按用户之前的手法（样例 deck 文本 + 手动 bunx + 链接）让 ZK 外部环境再走一遍 quick-start。
3. **2 个 07-10 stale backlog 任务**(48 天）:work-or-suspend，用户定。
4. **user-sim 校准**(`.private/decision-profile.md` 4 条 Pending guesses，自 07-27 挂起）。
5. (P2) bump 流程不刷新 bun.lock 的 follow-up（未登记）;5 个 @lythos org 包在发布管线外。

### Temp Artifacts

- 无（全部产物已入库：weekly/×7、cortex/wiki/04-ssot/×3、本 daily)

---

## Session Close — 2026-08-27 收尾

- **用户确认**：用户主动要求“确认待提交部分 → commit push + scribe”。
- **Git 状态**：`git status` working tree clean；本地领先 `origin/main` 12 commits（站点修复 + 保鲜三件套 + Articles + writer 门 + daily）。
- **Cortex probe**：479 文档一致；仅 2 个 48 天前 backlog task 仍 stale（已在 Next Steps 列出，未处理）。
- **测试门**：`bun --filter='*' run test` 全绿（命令成功退出，无失败）。
- **动作**：本段追加后提交 daily 并推送。

## Post-Push Verification

- **Pages deploy workflow**: `run 33044791747` → `completed success` (34s)
- 这是自 2026-07-29 以来 `Deploy VitePress site to Pages` **首次成功**，证明 `site/drafts/**` 排除 + curator 命令修正 + Articles 栏目化等改动已完整上线。
- 验证命令：`GH_TOKEN=$(<.github-token) gh run list --workflow="Deploy VitePress site to Pages" --limit 3`
