# Daily — 2026-05-15

## Session Handoff (Session 5 — SOCKS Proxy + Catalog Accuracy Fix + Release)

**Git**: HEAD `c4b39f1` — plus 2 commits pending (code fix + bump)
**Versions**: v0.13.2 → v0.13.3 (pending bump)
**Active**: SOCKS proxy fix + catalog accuracy fix committed, release in flight

### What changed in this session

1. **`probeConnectivity` SOCKS proxy support** — `mirror.ts` now routes direct probes through `LYTHOS_SOCKS_PROXY` via curl when the env var is set. Previously probe used native `fetch` and completely ignored the proxy, while error messages misleadingly suggested setting it. 4 new tests (VS 8-11) cover proxy success, prefix deduplication, proxy failure, and mirror coexistence.

2. **`findSkillDirectories` recursion fix** — `cold-pool.ts` always recurses into subdirectories, even when a parent dir contains SKILL.md. Fixed the silent omission of 55 skills from standalone repos that also have nested sub-skills (e.g. gstack's 50+ sub-skills).

3. **`scanSkill` frontmatter resilience** — `curator/cli.ts` wraps `YAML.parse` in try/catch. Skills with malformed frontmatter or no frontmatter are now indexed with path-derived defaults instead of being silently skipped. 4 previously-skipped skills now appear in catalog.

4. **Wiki lesson recorded** — `cortex/wiki/03-lessons/2026-05-15-curator-catalog-accuracy-...md` documents both bugs, fixes, and verification.

### State after fixes

| Source | Count |
|--------|-------|
| Filesystem SKILL.md | 439 (4 in hidden dirs) |
| findSkillDirectories | 435 |
| catalog.db | 435 |
| REGISTRY.json | 435 |

### Pitfalls
- `skill-deck.toml` was modified by seed bootstrap subagent (shrunk to 3 skills) — **reverted** before commit
- Curator scan emits noisy YAML warnings for `{ PACKAGE_VERSION }` placeholders in frontmatter — non-fatal but noisy
- `bunx @lythos/skill-curator@latest` runs published version, not project source — use `bun packages/.../cli.ts` for dogfooding

### Next steps
- [x] Commit code fixes
- [ ] Bump version (v0.13.2 → v0.13.3)
- [ ] Publish packages
- [ ] Bunx verification

---

## Session Handoff (Session 3 — Graduation Exam v5 PASS + Prompt Template + White-Box)

**Git**: HEAD `2b04641` — base, plus uncommitted session 3 changes
**Versions**: v0.13.1 → pending bump
**Active**: graduation exam v5 completed, pending publish + bunx verification

### What changed in this session

1. **Prompt-template IoC** — `buildArenaPrompt()` extracted in `runner.ts`. Fixed part injects CWD/deck/output contract/decision-log/ROBUSTNESS/TOOLS instructions; `{brief}` is the variable. Both `single` and `vs` paths aligned.

2. **Agent workdir → `/tmp`** — `cli.ts singleRun` uses `tmpdir()` instead of `process.cwd()`. Eliminates workspace pollution (npm install docx succeeds). Artifacts still copy to `--out`.

3. **Kimi adapter white-box** — `parseKimiStreamJson` now parses `tool_calls` + `tool` messages into `CheckpointEntry[]`. Raw JSONL preserved to `agent-stdout-raw.jsonl` for post-hoc replay.

4. **Graduation exam v5 — PASS** — All success criteria met:
   - `Cookie_Recipe_Report.docx` 125KB with embedded radar chart
   - `radar_chart.png` 111KB
   - `decision-log.jsonl` 9 entries
   - `validate.py` — `All validations PASSED!`
   - Key: 10min timeout + tmpdir isolation + prompt template

5. **Showcase entry** — `showcase/2026-05-15-graduation-exam-v5/` with README.md, reproduce.sh, and all artifacts.

6. **Docs updated** — `packages/lythoskill-arena/README.md` + `skill/SKILL.md` mainstream usage rewritten around `single` mode.

7. **Wiki + AGENTS.md** — `cortex/wiki/01-patterns/2026-05-15-graduation-exam-spec.md` created. `AGENTS.md` Index (Page Table) added.

### Pitfalls
- Old `arena-single-*` dirs in project root (pre-tmpdir) — cleaned up in this session
- 5min timeout insufficient for skill-read + docx-js pipeline — need 600000ms
- kimi stream-json `tool_calls` parsing: `\n` in test data must be escaped as `\\n` for valid JSON

### Next steps
- [ ] Commit session 3 changes
- [ ] Bump version
- [ ] Publish packages
- [ ] Bunx verification run

---

## Session Handoff (Session 2 — Agent-Orchestrated Arena)

**Git**: HEAD `2b04641` — showcase commit
**Versions**: v0.12.0 → v0.13.0 → v0.13.1 (shebang hotfix)
**Epic completed**: EPIC-20260513010237904

### 0. Verify Current State
```
git log --oneline -5             # Confirm recent commits
git status --short               # Real-time working tree
git status --short            # Real-time working tree
git log --oneline -3          # Confirm recent commits
```

### What changed in this session

1. **Cortex state alignment** — 13 probe mismatches fixed (9 task Status History + 1 epic + 3 ADR). 6 empty stub task cards filled with code evidence from subagent verification.

2. **Agent-driven refresh plan-first** — `deck refresh` default changed from direct `git pull` to plan-only output (behind counts + type classification). `--exec` flag for explicit execution. ADR-20260507110332805 updated with scheme E. Dogfood verified: 15 skills scanned, lythoskill + mattpocock repos pulled via SOCKS proxy, `deck link` synced working set → SKILL.md version `0.12.0`.

3. **probeConnectivity wired** into deck add/refresh plan→apply boundary. Deck add probes cloneUrl before fetch; deck refresh probes first git target.

4. **CLI subcommand `--help`** fixed in cortex + deck. `--help`/`-h` anywhere in args triggers help instead of being treated as positional arg.

5. **CI E2E publish-validation gate** added to `scripts/publish.sh` — spawns `bunx` + `bun add` in tmp dir after publish to verify resolution.

6. **Architecture decisions recorded**:
   - `memory/project_arena_pipeline_not_builtin_html.md` — arena visualization = downstream deck composition, not built-in
   - `memory/project_catch_is_agent_facing_not_human_facing.md` — error handling targets agent who can fix, not human
   - `memory/feedback_task_timestamp_as_context_probe.md` — task ID timestamp → daily + git log = most reliable context probe
   - `wiki/01-patterns/2026-05-14-agent-driven-plan-first-architecture.md` — reusable pattern
   - ADR-20260507110332805: added scheme E (agent-driven apply, supersedes heredoc)

7. **Docs updated**: `packages/lythoskill-deck/skill/SKILL.md` + `README.md` reflect plan-first refresh + agent-driven apply + `--exec` flag.

8. **Kanban final**: 7 completed, 2 terminated (HTML report / radar chart — wrong framing, pipeline responsibility), 1 suspended (bun publish eval).

### Pitfalls
- `bunx @lythos/project-cortex@0.10.1` version doesn't exist; use `@latest`
- cortex CLI task search has bugs with long filenames — manually moved files instead
- Cold pool lythoskill repo had local divergence — `git reset --hard origin/main` (safe for mirror) resolved; agent diagnosed where heredoc would have just failed
- `deck refresh` behind count = 688 for lythoskill repos (post-bump expected — cold pool still at old ref)

### Next steps
- Commit then bump version
- Publish packages
- Arena structured output stability (JSON verdict contract) — pipeline foundation

---

## Session Handoff (Session 4 — Seed Bootstrap Graduation Exam v7)

**Git**: HEAD `0168944` — v0.13.2 release commit (clean working tree)
**Versions**: v0.13.2 (already bumped and committed by prior session)
**Active**: graduation exam v7 completed (seed bootstrap), wiki + showcase archived

### What changed in this session

1. **Onboarding + context recovery** — Read daily/2026-05-15.md, found stale (HEAD `0168944` ≠ handoff `2b04641`). Degraded exploration: read cortex/INDEX.md, skill-deck.toml, recent ADR/wiki timestamps. Confirmed v0.13.2 released, no in-progress tasks, 1 active epic (test migration).

2. **Arena agent-orchestrated mode deep dive** — Read `skills/lythoskill-arena/SKILL.md` (Agent-Orchestrated Protocol), `packages/lythoskill-arena/src/runner.ts`, `cli.ts`, `player.ts`. Confirmed: native `Agent` tool replaces `useAgent().spawn()` for internal subagent dispatch. Arena SKILL.md describes agent's ReAct loop AS the runner.

3. **Graduation exam v6 — Native subagent execution** — Ran pre-composed `recipe-report.toml` deck via `Agent` tool (not arena CLI spawn). Isolated `/tmp` workdir, deck link, prompt-template IoC. Result: 186KB docx, 14-entry decision-log, embedded radar chart. Verdict: PASS. Showed Agent tool can replace arena `agent.spawn()` for single-mode tasks.

4. **Skill version drift discovered** — `.claude/skills/lythoskill-arena` is v0.12.0 (cold pool at `3d24f6b`), missing Agent-Orchestrated Protocol chapter. Project build output (`skills/lythoskill-arena/`) is v0.13.2. System prompt loads stale skill. Same for lythoskill-deck (0.12.0 in cold pool).

5. **Cold-pool metadata gap discovered** — `.cold-pool-meta.db` records only 10 skills; curator catalog.db has 346. Cold-pool CLI only tracks explicitly `add`-ed repos, not filesystem state. 336 "ghost skills" exist in cold pool without metadata. Reported to user; may be design-by-intent or scan gap.

6. **Stage 1 v1 — Discovery subagent (web search allowed)** — Timed out after 5min, zero files written. Task too broad; agent thrashed between catalog queries and web search without converging.

7. **Stage 1 v2 — Discovery subagent (cold pool only, focused prompt)** — Succeeded but used `[cards.xxx]` format (hallucinated schema). deck link only resolved 2/5 skills. Exposed bootstrap problem: agent doesn't know deck schema without reading deck skill.

8. **Seed Bootstrap concept formalized** — User coined "seed" = minimal initial deck containing only governance skill (`lythoskill-deck`). Agent reads skill → learns schema → discovers/adds/links other skills → self-expands. Recursive bootstrap: governance skill is the only irreducible dependency.

9. **Stage 1 seed bootstrap — SUCCESS** — Seed deck `[lythoskill-deck]` linked to `/tmp/arena-seed-*/`. Subagent read deck SKILL.md, ran 3 SQL catalog queries, selected `docx` + `deep-research`, rejected `critique` (HTML not docx-embeddable). `deck add` blocked by network probe → workaround: manually edited skill-deck.toml + link. Final deck: 3 skills, correct `[tool.skills.xxx]` schema, zero hallucination.

10. **Stage 2 seed execution — SUCCESS** — Subagent in expanded deck read docx/deep-research skills, identified "no radar-chart skill → matplotlib fallback", generated 178KB docx with embedded PNG, 14-entry decision-log. Verdict: PASS.

11. **Wiki archived** — `cortex/wiki/01-patterns/2026-05-15-seed-bootstrap-pattern-minimal-governance-skill-enables-agent-self-expansion.md` created. Covers bootstrap paradox, verified experiment, when to apply, key insights. Cortex index regenerated.

12. **Showcase archived** — `showcase/2026-05-15-graduation-exam-seed-bootstrap/` with README.md, reproduce.sh, all artifacts (docx, png, decision-log, skill-deck.toml, seed-bootstrap-report.md, scripts).

### Pitfalls
- Subagent timeout on broad discovery tasks — need focused prompts with explicit time budgets
- `[cards.xxx]` schema hallucination when agent hasn't read deck skill — schema must come from skill, not prompt
- `deck add` network probe fails in isolated/network-restricted environments — agent needs fallback path (manual toml edit)
- Cold pool skills lag behind project releases (0.12.0 vs 0.13.2) — system prompt loads stale skill content
- `bunx @lythos/skill-deck validate` without `--deck` flag validates cwd deck, not the specified path

### Next steps
- [ ] Refresh cold pool lythoskill repo to v0.13.2 (so `.claude/skills/` gets latest arena/deck SKILL.md)
- [ ] Consider whether cold-pool-meta.db should scan all filesystem skills (ADR candidate)
- [ ] Evaluate if seed bootstrap should become the canonical graduation exam (replace pre-composed deck)
- [ ] Test seed bootstrap with different task domains (not just docx + radar)
- [ ] Consider adding `deck validate --deck <path>` explicit path support to avoid cwd confusion
