# Daily — 2026-09-10

> 本日三条主线:**①** `adapter-registry` 命名撞车的善后(6 段计划);**②** 由善后过程中暴露的
> 两个**流程缺口**各写一篇 ADR(S5 / S6);**③** 善后卡上两条归 owner 裁决的项落盘
> (B17 CI 版本 pin / B18 名单外目标发声),并在执行中追加**两条边界行为**。
> 前一日(P2 收卡)见 `daily/2026-09-09.md`。

## Session Handoff — 09-10 后半段之二:follow-up 卡收口(B-class 批量 + CI 真红修复)(最新真相,覆盖下方)

### 0. Verify Current State

**Git**: 本段代码 HEAD = `0fa0ad6e`(B19 verdict 落盘)+ `d4249a8d`(本 daily);分支 `main`,
工作树 clean,已推送(`git ls-remote origin refs/heads/main` 已比对)
**Version**: v0.19.1(未 bump)
**Active Task**: 1 —— `TASK-20260910110545092` **review**,验收项**全部勾掉**;0 in-progress
**Tests**: deck `253 pass / 1 skip / 0 fail / 661 expect / 254 tests / 16 files`;
cortex `138 pass / 0 fail / 280 expect / 7 files` @ Bun 1.3.11 / macOS
**CI**: 本段开始前 GitHub CI **自 03:45 起是红的**(见第三节);本地已把 CI 全部步骤复跑至零失败
**Resume**: 无在途 agent / 无 live 会话

### 一、这批到底改了什么(owner 特别要求写清)

**改动之前 `deck link` 是能用的**,默认 deck 从头到尾零警告、行为不变,今天仍然如此。
本批不是"修好了一个坏东西",是三件事:

| 层 | 内容 |
|---|---|
| **真的改变 `deck link` 输出的** | 配置根目标出 warning(`~/.claude` / `~/.config`);goose 全局目录的 data-loss 漏报补上(B2);名单外目标一行 info;警告块前移到收束之前 |
| **不改输出、只改机制** | CI 解释器 pin 1.3.11 + 升级窗口(B17);`probe` 现在能发现空 ADR(B21);pre-commit/CI 各加产物-来源同步门(B20);`decision-log` 契约写明"ts 不是时钟"(B16) |
| **只改"我们说的话是否准确"** | B2/B3/B13(数据)、B8/B11/B12(文档注释)、B9/B10(测试独立性)、B19(场景)、B4-B7(历史限定) |

**边界**:本批**不含任何删除路径的修复** —— 删除边界改归属是**上一张卡**
(`TASK-20260910111600389`,已收 `e2edc52e`),但它是本批"为什么现在敢报"的前提。
本批唯一的 data-loss 项(B2)是**警告漏报**,不是删除行为出错。

### 二、两个"机制自身的静默失效"(本批最值钱的两条)

- **B21**:`probe` 的 empty-shell pattern 三条全是**任务卡形态**(`- [ ] ⚠️ PLACEHOLDER_` /
  `- [ ] 需求N` / `<!-- 填写`),而 ADR 模板的占位是 `**Choice**: ⚠️ PLACEHOLDER_SCHEME` ——
  `adrFiles` 明明传进了 `detectEmptyShells`,**一条也匹配不上**。即:空 ADR 与填好的 ADR
  在 probe 眼里**没有区别**。补两式后**实测**:CLI 新建未填 ADR → `Found 1 empty shell(s)`
  (改前 0)。两条候选**被否决并留档**:裸 `-`(在本仓 112 篇 ADR 里命中 **2 篇填好的** ——
  会制造关于健康文件的覆盖率假象)、按 pattern 数量断言(钉形状不钉行为)。
  EPIC 模板**已实测**无此病(不是推断)。
- **B20**:`.husky/pre-commit` 只在 `packages/*/skill/` 变更时重建,而 `COMMANDS.md` 是
  **跑 `src/cli.ts --help` 生成**的 → 只改 `src/` 的提交会让**发出去的产物**文档化一个已退役的
  flag(`e2edc52e` → 直到 `18a6d1cc` 才跟上)。已扩触发条件 + 陈旧即发声,并在 CI 加一道
  机器可判定的门(`build --all && git diff --quiet skills/`)。
  **这道门第一次运行就抓到一份真陈产物**(本批改的 arena 契约还没重建)。

### 三、GitHub CI 真红(owner 要求确认,已确认并修好)

自 2026-09-10 **03:45** 起 `CI` 一直失败(早于本段全部提交)。根因不是本段改动:
`scripts/check-site-commands.test.ts` 的 **CLI_TABLE drift tripwire** 报
`@lythos/skill-deck: every dispatch label is in the table` → `per-run` 不在守卫表里。
P2 批新增 `per-run` 子命令时漏更新那张**手维护**的表 —— 与 2026-08-28 的 `update` 漏项同一类。
**守卫按设计工作,是表错了**;已补 `per-run` 并注明出处。
本地复跑 CI 全部步骤零失败:`bun --filter='*' run test` 零失败、cortex BDD 13/13、
deck BDD 0/0、example decks 29/29、site snippets 61、align 75 passed。

### 四、B19 的立论被**实测推翻**(记一笔,防后人照抄错的判读)

原文判:"场景 PHASE 2 断言 `.agents/skills/skill-a` / `.kimi/skills/skill-a` 不存在"
与新的**归属**语义相冲。**真跑一遍**(2026-09-10,全量 PHASE 1-4,19 条断言)**全过** ——
因为那些条目**是 deck 自己建的 symlink**,归属判定允许删;相冲的判读把"归属"读成了"包容"。
新增 PHASE 5 钉**真正的新边界**:fan-out 目标里的外来真实目录 / 外来 symlink **必须留下**
并出三件套报告(实测 20-23 全过)。
**✅ verdict 产物已补齐(owner 指示「跑一轮补上,这次落盘」)**:player(零上下文)逐条实测
23/23 → `decision-log.jsonl`;judge(独立调用,不采信 player)自跑整个生命周期复核 → **PASS 23/23**,
`judge-verdict.json` 带 `reviewed_commit`(= `30cda3b6`)与**分层** `independence`
(knowledge 达成 / orchestration **不**达成)。产物落 `also-link-to-bdd/`,沿用 TESTING.md 四文件约定。

**裁判报回三件判据覆盖不到的事,两件已落地**:
①并发会话(就是我)在裁判期间改了工作树 —— 裁判自核 `packages/lythoskill-deck` 干净才绑定结论;
②**归属判据是"形状"不是"出身"**(手工建的、指向本 deck cold pool 的未声明 symlink 会被回收)
 —— 本卡**更正了裁判的一处措辞**(它写 "silently",实测 stdout 有 `🗑️ Removed:`),
 并据 owner 定调「**担心 agent 拿静默美德扩大解释。事实上这个项目更加推崇 HATEOAS 式的 exit message**」
 把它**登记为该 ADR 自己的 Follow-up**(补 what/why/fix 浓度),而不是辩解成"刻意的不对称";
 deck README 里也加了反向配重:**「静默是挣来的」约束的是噪音,不是音量**;
③**`deck add`/`remove` 摧毁 toml 全部注释** → 新卡 `TASK-20260910152029904`,
 owner 已给解法方向:**参考 Cargo(`toml_edit`)/ Poetry(`tomlkit`)的格式保留编辑,定位用 AST、写回用范围替换**。

### 五、顺带落盘:ZK 证据 bundle + 它自己的一条红

`/tmp/arena-p2-adapter-retrial/` 的三份产物(唯一副本,重启即失)**逐字**落盘到
`showcase/2026-09-09-p2-cli-layout-zk-retrial/`,沿用既有 `showcase/` + `reproduce.sh` 协议,
不新发明约定。README 写明它 pin 的是 `c8ebcc76`、以及为什么逐字不改(改了就不再是那个 commit 的证据)。

**落盘时实测出的一件事**:该 ZK 门的 10 条对抗用例对今天的树 **9 过 1 红**;
红的那条 **ADV-7 对它自己所 pin 的 `c8ebcc76` 同样红**(用当时的 `adapter-policy.ts` 复现:
11 ≠ 期望 1,而该函数逻辑自那以来未变)—— 即**门报的 `8.5/10` 没有携带自己套件里的红**。
保留原样不修:修了就等于抹掉这个发现。已补进原卡 Notes 的证伪表。

### Next Steps

- `TASK-20260910110545092` → **review**,验收项**全部勾掉**(B19 的 verdict 产物已补齐)。
  普查报告未持久化部分 = **定位置**(`showcase/` 协议)而非找回原始报告(不可追回),已写明。
- 新卡 `TASK-20260910152029904`:toml 注释被 `add`/`remove` 摧毁;owner 已给方向
  (格式保留编辑 + AST 定位 + 范围写回),待执行。

## 09-10 后半段(B17/B18 落盘 + 两条追加裁决)(历史 —— 状态见上方最新块)

### 0. Verify Current State

**Git**: 本段代码 HEAD = `a7e776ab`;**本段全部 5 笔已推送**(`git ls-remote origin refs/heads/main`
比对 = `a7e776ab`;daily 提交紧随其后)。清单:`e71bdeb5`(B18)、`cc6370d9`(B17)、
`e859ee10`(§5 豁免)、`c44fb700`(规则 10 + 卡回填)、`a7e776ab`(层级错位警告)
—— 前两笔各带一个由 post-commit 钩子自动生成的 ADR accept 收口提交(`4a0353cc` / `1f10ad42`)。
**Version**: v0.19.1(未 bump,本段无 release)
**Deck**: 本段**零** deck 声明变化(改的是 deck 包本身的策略层)
**Branch**: `main`,工作树 clean
**Active Epic**: 1(`EPIC-20260828111425886`,与本段无关)
**Active Task**: **1 in-progress**(`TASK-20260910110545092`),0 in review
**Tests**: deck `244 pass / 1 skip / 0 fail / 615 expect / 245 tests / 16 files`;
cortex `135 pass / 0 fail / 275 expect / 7 files` @ Bun 1.3.11 / macOS
**Resume**: 无在途 agent / 无 live 会话

```bash
git status --short && git log --oneline -3
```

### 一、B17 —— CI 的解释器必须是精确版本(裁决 "基本按 B")

8 处 `bun-version: latest` → `1.3.11`(`test.yml` ×5、`release.yml` ×2、`deploy-pages.yml` ×1)。

**为什么不是装饰**:本仓的纪律是"关于 `node:*` 兼容层的行为断言必须由**在 Bun 下真跑过**的测试钉死"
(B11);`latest` 直接违背它 —— 同一条 dormancy 测试在 1.3 过、在 2.x 挂,而绿的 CI
**说不出它认证的是哪个解释器**。本仓已经拒绝裸 pass 计数(必须写
`244 pass / 1 skip / 0 fail / 615 expect @ Bun 1.3.11/macOS`),同一标准套到 CI 上只剩一个选项。
同 `ADR-20260910113730375`:结论必须指回它成立的条件。

**owner 定调**:「**升级本身就是一个比较严肃的问题。**」→ 升级走四步显式窗口(读 release notes
的 node:\*/test runner/TOML/fs → 全量本地复跑并把新基线写成断言 → 8 处 + `AGENTS.md §9` 同一
commit → 不许折进无关改动),窗口写在 `AGENTS.md §9`,并在**每个 workflow 文件第一处上方**留一行
指针注释(改它的人正在看的那一行)。决策:**ADR-20260910120047160**。

**顺带记账**:judge 记的是 7 处,漏了 `deploy-pages.yml`;另外该文件用 `setup-bun@v1` 而另两个是
`@v2` —— pin 的是解释器不是 action,**不在本条范围**,已写进 ADR 免得下次复核当成新发现。

### 二、B18 —— 名单外目标不再静默(裁决 B + 豁免标记)

`collectTriggerHazards` / `collectDuplicateScans` 都先 `layoutsScanning(t)`;目标不属于任何已知
layout 时返回 `[]` → 两个循环都不进 → **零输出**。于是 `also_link_to = [".some-new-cli/skills"]`
与默认 deck **在输出上完全一样**。而"没有输出"被读成"检查过了,没问题" ——
**缺失的规则被读成了通过的规则**(`no-source-no-rule` 的推论)。

做法:名单外目标默认出 **info 一行**(`<dir>: no layout data — hazards unknown`,措辞说的是
**缺数据**不是**有危险**),静默改由 `[deck] acknowledged_unlisted = [...]` **显式声明**
(gitignore 式:列出即静默)。**静默的出处就是那份声明本身**,而 toml 是 git-tracked、进 diff、
进评审的。决策:**ADR-20260910120047122**。两条边界:豁免**只关掉"无数据"这条**(data-loss 照常
发声,反例测试钉死);豁免模式**不检测陈旧**(记账项,不是自动检查项)。

### 三、两条被自己撤回 / 追加的边界(本段真正的产出)

**① 撤回:去重(owner 现场提醒)。** 第一版给名单外目标加了"按归一化目录名去重",让同一目录写两遍
只报一行。owner 的判据:「**解决非常螺丝壳道场的精细优化的时候先回看这是不是某种极为边缘
『可能不存在实际用户』的场景。如果是,用卫方式先保证头部使用体验,而不是追求一个很复杂的
规则包覆盖全部**」+「**你不故意根本没人这样用来恶心自己,这种就很明显**」。两条撤除理由:
- **正确性**:归一化把 `~/.x/skills`(家目录)与 `.x/skills`(项目内)映射成同一个键 —— 那是
  **两个真目录**,去重会静默吞掉其中一条警告。在一个"为了不静默"的改动里引入一处静默。
- **取向**:它服务的场景代价只有**一行 info**。为它导出 `normalizeDir` + 加测试 + 立规矩 = 螺丝壳道场。

**② 追加:层级错位(owner 二次现场裁决,同一天)。** owner 四句话:

> 「**说到底,deck toml 在预设的主要场景是什么,千万不要搞错这个来故意为难自己。说白了,
> 如果敢配全局目录到 skill 目录,我都觉得首先要警告了**」
> 「**因为现在有 agent,我反而可以认为在有 cli 那个前置知识后,我们是可以发现『看上去意图很奇怪的
> 配置』的**」
> 「**这个类似垃圾邮件原理**」
> 「**不要自己去自作多情/自作聪明去『推测』各种奇葩写法,明明你们 agents 可以帮忙修正回正路到 toml 里**」

**它修的是 B18 自己的一个洞**:名单外目标其实**两类**,表内状态相反 ——

| | 例子 | 表里的状态 | 该说什么 |
|---|---|---|---|
| (a) 表里**没有**这个位置 | `.some-new-cli/skills` | 不知道 | "没有数据"(info,B18 原决策) |
| (b) 表里**有**且写着对的目录 | `~/.claude`、`~/.config` | **知道,而且指错了** | "这不是 skills 目录"(warning) |

把 (b) 塞进 (a) 的 info 行 = **手里有答案却说自己没数据**,这是同一份输出的第二次说谎。

**为什么 (b) 才是最容易发生的那类**:挑目标时**出名的路径是配置根** —— `~/.claude` 是
`settings.json` 的家、`~/.config` 是所有 CLI 配置的家;而 `reconcileTargetDir` 把条目
**直接建在目标目录里面**,差这一层当场可见(skills 和 `settings.json` 混在一层)。
**常态形状是量出来的**(可复跑):
`grep -rh '^\s*working_set\s*=' examples/ showcase/ skill-deck.toml` → 68 条里
**67 条是 skills 目录**,第 68 条是 `"skills"`(与 build output 撞名,已被 `ADR-20260519144445916`
禁止),**配置根 0 条** → 一个廉价信号就够(垃圾邮件原理:不枚举坏样本,只要低误报率的信号;
误报代价一行,漏报代价是 skills 进配置根)。

**判据只用表内数据**:表里某个 `fanOutTargets` 位于目标**之下**(任意深度 —— 只判直接父目录会漏掉
owner 点名的 `~/.config`),绝对路径用后缀对齐认(同 `layoutsScanning`)。**四条边界**:
①豁免不适用(语义相反,给开关 = 一个"我就要把 skills 建在配置根上"的按钮);
②一个目标一个结论(不再同时出 info 行,否则两行互相矛盾且 info 的建议对 warning 无效);
③**不猜写法**——绝对路径目标同时列出两种写法,不替用户挑;④**不判 `~` / `.` / 任意大目录**
(只有故意才写得出来;`working_set` 那侧已在 `link.ts:510` 直接拒绝)——**已知边界,不是遗漏**。

**顺带一处不是新功能的修正**:警告块从收束**之后**移到**之前**。印在几十行 `🔗` 之后的警告
是墓志铭不是守卫。

**判据升格**:`AGENTS.md §2` 硬规则 10 补上两侧分野 —— **笔误 → 守头部(此时表里有答案就在消息里
说)**,**故意构造 → 不写 handler**,并点名"不要自作聪明去推测奇葩写法,修正由 agent 改 toml"。
两侧各有活体实例(不去重 / 层级错位)。

### 四、§5 —— 已 accept 的 ADR 由它自己承载 Follow-up(owner "第二吧")

落点三处:ADR-20260910113534807 §5(规则 + 边界表 + **自己的反例当证据**)+ writing-guide 新增一节
(**SOURCE**,已重建 `skills/`)+ `src/lib/template.ts` 里 `Follow-up:` 行下方的注释
(**落在写这份清单的那一行上** —— 同该 ADR §4 的落点纪律)。证据不体面但真实:那批落地该 ADR
四个提示植入点的改动(5 源文件 + AGENTS.md + CLAUDE.md,2 commit)**当时一张卡都没开**,
今天能追溯全靠那条 ADR 自己的 Follow-up 清单点了名。边界:新义务 / 需自己的计划 / 需 owner 裁决
—— 三者仍开卡。

### 五、本段踩到的坑(两条都是钩子)

- **`pre-commit` 会把 `skills/` 的重建产出卷进"下一笔"提交**:钩子跑 `build --all` + `git add skills/`,
  所以改了 `packages/*/skill/` 的 SOURCE 后,重建产物落在**随后**那一笔里(本段:`writing-guide.md`
  的重建被卷进 B18 那笔,ADR-807 的修订被卷进钩子的收口提交)。不是 bug,是设计 —— 但**它会打乱
  你的提交粒度**,排批次时要把它算进去。
- **`path-safety` 钩子 grep 的是文本,不区分注释**:本条被它拦下一次 ——
  `cli-layout.ts` 的一句**中文注释**里写了 `join(targetDir, alias)` 字面量,被判"unsafe alias/path
  usage"。改写注释即可。**引代码片段进注释会触发静态检查**(ADR/README 里的 `.md` 不在扫描面内)。

### Next Steps

- **`TASK-20260910110545092` 仍 in-progress**,其余项待执行:**B2、B3、B4、B5、B6、B7、B8、B9、
  B10、B11、B12、B13、B16、B19、B20、B21、取证未留痕**。
  - **B2 的改法是两处不是一处**(数据行 `cli-layout.ts:175`(现行号已因本段改动下移)**加**断言行
    `cli-layout.test.ts` 里那条把漏报钉成绿的)—— 测试与实现出自同一处理解时,测试不是第二意见。
- **抽取触发条件**(记录在案,不需现在做):出现**非 deck 包**的第一位 CLI-layout 消费者时抽成独立包。
- 本段新增的两个"已知边界"已写进 deck README 与 ADR-122,**不要再当疏漏补回来**。


## 09-10 善后收口段(早段,历史 —— 当前状态见上方最新块)

### 0. Verify Current State

**Git**: HEAD = `724517fc`(docs(cortex): add B7 + B16 to the follow-up card)
**Version**: v0.19.1(未 bump,本段无 release)
**Deck**: 17 条 deck 声明 / 14 个 skill 在 `.claude/skills/`
**Branch**: `main`,工作树 clean,**已推送**(`git ls-remote origin refs/heads/main` 已比对哈希)
**Active Epic**: 1(`EPIC-20260828111425886`,主题 external agent accessibility,与本段无关)
**Active Task**: 0 in-progress,0 in review,16 backlog
**Tests**: deck `227 pass / 1 skip / 0 fail / 589 expect / 228 tests / 16 files`;
cortex `135 pass / 0 fail / 275 expect / 7 files` @ Bun 1.3.11 / macOS
**Resume**: 无在途 agent / 无 live 会话

```bash
git status --short && git log --oneline -3
```

### 本段交付(22 个 commit,`3d16fcec..724517fc`)

善后计划 `snug-twirling-coral.md` **六段全部完成**,外加一张独立卡(fan-out 删除归属)。

| 段 | 内容 | 落点 |
|---|---|---|
| S1 | 撤 opencode **假 source** 行(不是降档,是撤) | `65b02db7` |
| S2 | 全量改名 `adapter-registry`→`cli-layout`(7 文件,含模块内 6 处自引用) | `96e51a41` |
| S3 | ADR:two-axis taxonomy + 闭数据哲学 | `981d48b9` → accepted `ac859c14` |
| S4 | follow-up 卡回填 + 原卡 Notes 注记 | `dabb35b3` |
| S5 | ADR:提案纪律(架构决策不得藏于实现卡) | `37a55f78` → accepted `ebbd9fad` |
| S6 | ADR:ZK 评审对象须 commit-pinned | `447a6d66` → accepted `24dd71c4` |
| (独立卡) | fan-out 删除改**归属**边界(k8s ownerReferences) | `e2edc52e` |

### 一、命名撞车:根因比"文件名撞车"深一层

**不是 `adapter-registry.ts` 撞 `registry.ts`,是 "adapter" 一词在仓里有两个互不兼容的含义。**

| | agent-adapter 家族(5 包) | deck 的 `adapter-registry.ts` |
|---|---|---|
| "adapter" 指 | **可执行行为** — 带 spawn 逻辑,`registerAgent()` 开放扩展点 | **一行数据** — 全文零 import、零行为 |
| 轴 | player 轴(怎么跑一个 agent) | CLI-layout 轴(一个 CLI 去哪找 skills) |
| 开闭 | 开放注册(第三方实现者) | 闭数据、审过才进(受 no-source-no-rule) |

**deck 的 `CliAdapter` 按仓里自己的定义根本不是 adapter —— 它不 adapt 任何东西。**
两轴开闭不对称是**刻意的**,此前未落盘(被告供认"现场构造")→ 已写成 ADR-20260910113131220。

**owner 裁决(已执行)**:全量改名 `cli-layout` / `CLI_LAYOUTS` / `CliLayout`;
**留在 deck**(抽取触发条件 = 出现非 deck 包的第一位消费者;模块零 import,抽取恒为一个 commit);
**"agent 自己选的那 16 家"是设计不是缺陷** —— 不写"用人工/权威名单替代 agent 普查"。
仅保留的记账项:普查报告未持久化 → 定性为**取证未留痕**(不是"调研方式不可靠")。

### 二、删除边界:从"目录包容"改为"归属"(独立卡 `TASK-20260910111600389`)

- **事故**:`also_link_to` 指向**项目外**目录时,原实现按**目录包容**判定 ——
  可递归删掉 deck **从未创建过**的东西(另一个项目的 `.claude/skills`、用户全局 CLI 配置)。
- **改为 k8s `ownerReferences` 心智**:证明不了"是我建的"就不删 → warn + 给 `rm -r` 建议。
- **同时退役备份路径**:归档不可解(含 `../` 前缀的 tar 成员),"假的安全承诺"与假 source 同类。
- **owner 原话(定调)"**:哪怕是同一个 k8s 协调心智,**只管自己认领的还是好的**。
  破坏性操作太可怕,**尤其备份路径反而心不在焉**。"
- **顺带被证伪**:前一日"六处删除点审计无 data-loss 路径"对默认工作集成立、对 fan-out 目标**不成立**;
  且那次审计**范围本身不全** —— 全仓复扫又在 `add.ts:332` 找到一处六个点都未覆盖的删除。
  已在 `daily/2026-09-09.md` 就地修正(活文档)+ 原卡 Notes 注记(已收卡正文不动)。

### 三、S5 —— 提案纪律(owner 定调的执行方式)

**问题**:同形状事故**发生两次**(2026-05 `feed-adapters.ts` / 2026-09 `adapter-registry.ts`),
两次的架构决策都**只写在实现卡的 `## Technical Approach` 里**。`AGENTS.md:110` 早有规矩
(「I think / 我觉得 = start an ADR」)但**从未被触发** —— 缺的不是规则,是**触发条件**。

**规则**:任何**引入或改变抽象 / 模块边界 / 包边界 / 命名概念 / 封闭数据集**的决策必须落 ADR;
卡的 Technical Approach **可引用,不得是唯一记录**。判据 **C1–C6,命中 ≥2 条**即触发
(事故 2 命中 6/6)。

**执行方式 = 两个落点(owner 补充定调)**:

> 「建议用**提示植入**。做 task 的 cli 那边也提示」
> 「换句话说在 **routine 的几乎必经之路上**做足提醒」

| 必经点 | 载体 |
|---|---|
| 创建卡 | `commands/task.ts` 输出 |
| **写 Technical Approach** | `lib/template.ts` 的模板注释(落在**误行为发生的那一行**) |
| 创建 ADR | `commands/adr.ts` 输出 |
| 每次 commit | `hooks/pre-commit.ts` governance waterline(新增 `packages/*/src/` 模块) |

**为什么"提示"可以、"门"不可以**:门输出的是**覆盖率断言**,误报制造"已检查过"的假象,
假象会被学会绕过 → 比没有检查更差。提示不断言,误报代价 = 一行输出。
**判据:检查若输出覆盖率,必须能机器判定语义;若只输出提醒,形式触发即可。**

**owner 的第二点(已写进 ADR)**:"防止不了 agent 完全绕过 task —— 这种情况下除了调整 agents md 就是增强
『**重要的话说三遍,必须用 lythoskill-project-cortex 做项目级别落盘**』"
→ 该条规矩在 `AGENTS.md` **三处重述**(文件顶 / §0 Boot First / §3 Task Design)+ `CLAUDE.md` 镜像。

### 四、S6 —— ZK 评审对象须 commit-pinned(本日最高价值产出)

**事故**:`8.5/10 PASS` 的评审对象是 `c8ebcc76`;**同一 session** 随后折入 5 条 findings
(`2686e0d4`,**6 文件 / +56 / −8**),卡面仍引 `8.5` 当凭证。**折后 HEAD 从未被任何第三方看过**
(judge 特别裁决二),直到次日的辩论。
**折入的 delta 里带进一条后来被证伪的 claim** —— opencode windows hazard 的"软化"版本
(judge 独立重拉 222 行源文件,grep 零命中)→ 存活到 main,直到 S1 整行删除。

**规则**:评审结论**绑定它被评的那个 commit**;折入 ≠ 已评。折入 diff **非空**则二选一:
① 对 delta 做第二轮独立评审;② 折入 commit **与**卡面**显式标注「折后未评」**并写明 delta 范围
与旧结论覆盖的 commit(**只写四个字不算**)。
**配套 claims-not-scores**:`8.5 @ c8ebcc76` 是引用,裸 `8.5` 不是;`213 pass / 0 fail` 也不是
(须写 `213 pass / 1 skip / 544 expect @ Bun 1.3.11/macOS`)。
**分数不可被反对,断言可以。**

**证据卫生(同时收到 B16)**:`decision-log.jsonl` 文件 mtime(22:52)与内部时间戳(23:20–23:45)
**自相矛盾** → 时序断言只接受**逻辑约束**或**文件系统锚**,冲突时逻辑约束优先。
**此条修订 `ADR-20260518155038335`**:其 decision-log 作为证据链仍有效,**作为时钟无效**。

**落地**:`zk-review.md` 新增协议节 + `AGENTS.md` § ZK Review Gate(双向 ref)+
`cortex task done` 上的非阻断提示(关卡的这一刻 = 评审结论被写进记录当作"通过"的那一刻)。
**刻意不阻断**,理由同 S5。

### 五、意外发现与顺手修

- **B20(实测发现)**:`.husky/pre-commit` 的触发条件是 `grep '^packages/.*/skill/'`,
  但 `build.ts` 生成 `COMMANDS.md` 靠 `bun src/cli.ts --help` —— **只改 `src/` 永远不重建**。
  实例:`e2edc52e` 删了 `--no-backup`,产物面一直挂着旧帮助文本,直到 `18a6d1cc`。
- **`bunx cortex` 拉的是**另一个 npm 包**(`cortex@6.2.3`,崩溃 `ReferenceError: primordials is not defined`)。
  **仓内规范调用**是 `bun packages/lythoskill-project-cortex/src/cli.ts <cmd>`。
  这是命名撞车问题的**活体实例**,已记。
- **改错 skill 的哪一侧**:先改了 `skills/...`(build output),经 `AGENTS.md:246` 纠正为
  `packages/*/skill/`(SOURCE)后重建。本日 S5/S6 的文档落点均按此办理。

### 六、日记体例提醒(本日两次踩到)

- **"计划里假设已存在" ≠ "已存在"**:计划原文假设 follow-up 卡已含 `B6/B7/B16`,实查**只有 B6**。
  B7(8.5 的覆盖范围注记)与 B16(decision-log 时序不可用)在卡里**不存在** → 已补(`724517fc`)。
  **一条只在别处被提到、从未在本卡落地的条目,等同于未记录。**
- **本条正是本日两篇 ADR 的共同主题**:记录的合规度量是**它是否仍指称真实**,不是它是否存在;
  是**发现性**,不是存在性。

### Next Steps

> ⚠️ **本段(早段)的 Next Steps 已被后半段覆盖,此处就地更正而不是留作"历史"** ——
> 事实错误会传播给下一个 agent。**B17 / B18 两条均已裁决并落地**(见上方后半段)。
> 其余项与 B2 的说明仍然有效。

- ~~**owner 裁决项仍挂起** `TASK-20260910110545092`:**B17**…与 **B18**…~~
  → **已裁决(两条都取 B)并落地**:后半段一、二节 + `ADR-20260910120047160` / `ADR-20260910120047122`。
- `TASK-20260910110545092` 其余 B 类项(B2/B3/B4/B5/B6/B7/B8/B9/B10/B11/B12/B13/B16/B19/取证未留痕)
  待执行。**B2 的改法是两处不是一处**(数据行 `cli-layout.ts:175` **加**断言行
  `cli-layout.test.ts:49` —— 后者把漏报钉成绿的)。
- **抽取触发条件**(记录在案,不需现在做):出现**非 deck 包**的第一位 CLI-layout 消费者时,
  把模块抽成独立包(模块零 import,恒为一个 commit)。
- `TASK-20260910111600389` 已 complete;原卡 `TASK-20260909155425926` 保持 `completed` + Notes 注记
  (judge 原话:「注记比改状态诚实」)。
