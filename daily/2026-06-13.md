# Daily — 2026-06-13 (Session 2)

## Ground Truth

**Git**: HEAD = `06543f7` (test(cortex): add unit tests for probe empty-shell filtering)  
**Version**: v0.16.0  
**Deck**: 14 skills linked  
**Branch**: main, clean working tree, ahead of origin by 4 commits (from session 1's 12 → now 16 total)  
**Active Epic**: 0  
**Active Task**: 1 (TASK-20260613185621344 audit empty-shell)  

## Session Handoff — English-only slugs + probe IO separation test + self-referential empty shell resolved

### Completed (本 Session)

1. **English-only slugs 强制实现** (`10c601a`)
   - `hasNonAsciiSlug()` 校验函数加入 `fs.ts`
   - `task.ts` / `epic.ts` 创建时拒绝非 ASCII 标题
   - `generateFileName()` 去除中文支持（只保留 `[a-z0-9-]`）
   - AGENTS.md 和 SKILL.md 同步规则声明
   - 对应任务 **TASK-20260613185808109**（backlog，代码已完成，待测试+文档收尾）

2. **Probe empty-shell 过滤逻辑单元测试** (`06543f7`)
   - 提取 `isEmptyShell(content: string): boolean` — 纯函数，测字符串匹配
   - 提取 `filterEmptyShells(shells, mode)` — 纯函数，测路径数组过滤
   - 导出 `EMPTY_SHELL_PATTERNS` 供测试验证
   - 18 个测试全部通过（7 个 isEmptyShell + 2 个 patterns + 9 个 filterEmptyShells）
   - IO 分离模式对齐 deck/curator/cold-pool 先例：Plan 层纯数据测试，无需文件系统
   - 对应任务 **TASK-20260613190646769**（backlog，内容已填充，测试已完成）

3. **自指空壳任务解决**
   - TASK-20260613190646769 本身创建时是空壳（PLACEHOLDER），probe `--suspicious` 会报告它
   - 填充内容后，probe 不再报告 —— 验证了"填内容优先于改代码"的原则

4. **登记架构债务任务** — probe 全面 IO 分离重构
   - 创建 **TASK-20260613234838986**: Refactor probe.ts into intent-plan-execute IO separation
   - 参考 deck 的 `buildRefreshPlan`/`executeRefreshPlan`、cold-pool 的 `buildReconcilePlan`/`executeReconcilePlan`
   - 当前 `probeStatus()` 是 300+ 行巨无霸，混合扫描/解析/检查/打印
   - 目标：拆分为 `buildProbePlan()` + `executeProbePlan(plan, io)` + 薄包装 CLI 层
   - 优先级 P2：当 probe 需要新增检查类型时先做这个重构

### Key Decisions

- **probe 测试采用 deck 的 IO 分离模式**：`isEmptyShell` + `filterEmptyShells` 是纯函数测试（无文件系统），`detectEmptyShells` 是 IO 层（不测）。这与 `buildSkillDirCandidates`/`findSkillDir` 在 add.ts 中的分离一致。
- **空壳任务不删除，填充或终止**：TASK-20260613190646769 从空壳 → 填充 → 完成，没有删除文件。符合"历史记录保留"原则。
- **架构债务单独登记**：probe 全面重构不混在当前任务里，避免 scope creep。

### Pitfalls

- **export 作用域陷阱**：第一次把 `filterEmptyShells` 放在 `probeStatus()` 内部并加 `export`，导致语法错误（"Unexpected export"）。必须移到模块顶层。
  - **Fix**: 纯函数一律模块顶层导出，不要在函数内部 export。
- **自指任务的尴尬**：一个要求"测试空壳检测"的任务自己就是空壳，probe 会报告它。填充内容后解决。
  - **Fix**: 创建任务后立即填充内容，不要留空壳给 probe 报告。

### Next Steps

1. **推进 TASK-20260613185808109** — English-only slugs 已编码，需：扫描现有中文 slug 并批量重命名、pre-commit/probe 检测、文档更新
2. **推进 TASK-20260613185621344** — audit empty-shell 已完成抽样，需制定批量清理方案（回填 Status History / reopen / 终止）
3. **推进 TASK-20260613234838986** — probe IO 分离重构（P2，等需要新增 probe 检查类型时做）
4. **Accept ADR-20260613190449007** — wiki patterns INDEX.md 方案已选定（方案A），待初始分类 P0/P1/P2
5. **Push 所有 commits** — 本地 main 领先 origin 16 commits

### Temp Artifacts

- 1 proposed ADR 未提交：`cortex/adr/01-proposed/ADR-20260613190449007-wiki-patterns-aaa-pinned-index-dreaming-integration.md`
- Working tree clean, stash empty
