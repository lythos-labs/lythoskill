# Daily — 2026-09-08

## Session Handoff — deck lock-drift hint 收尾 + lock hash 同步

### 0. Verify Current State
> This handoff's SSOT. Run these commands to verify freshness.

**Git**: HEAD = `2ed663e2` (chore(deck): sync lock hashes)
**Version**: v0.19.0
**Deck**: 14 skills linked
**Branch**: main, clean working tree, ahead of origin/main by 4 (push pending at write time)
**Active Epic**: 1 (EPIC-20260828111425886 — external-agent-accessibility-and-onboarding, still drifted)
**Active Task**: 0 in-progress, 0 in review, 9 backlog
**Resume**: nothing live
**Tests**: `bun --filter='*' run test` EXIT=0 (gate log `/tmp/test-gate-20260908.log`)

```bash
# Verify: if output diverges from the fields above, this handoff is stale
git status --short && git log --oneline -3
```

### Completed

1. **lock-drift HATEOAS hint + lock-vs-state policy** (`51b02544`) — resumed the session interrupted after `cc5fe72d` (docs-only commit). `formatLockDriftHint()` in `packages/lythoskill-deck/src/link.ts`: when `deck link` rewrites the lock, print inline triage + commit + verify actions; silent on the happy path (dormancy-tested). 3 new tests (drift actions / dormancy / B2.c idempotent re-run). deck SKILL.md gains "lock vs state — what to commit and why" (ADR-20260616000939948; lock = go.sum role → commit, state = ignore). Deck suite 167 pass.
2. **scribe checklist fix** (`ec4c0822`) — pre-handoff git-status snapshot moves LAST (after any link/refresh/release), same 2026-08-31 incident from the scribe side.
3. **lock hash sync** (`2ed663e2`) — 4 upstream entries (deck, project-cortex, arena, curator) hash-only drift, committed as audit trail per the new policy. Lock sat dirty ~1 week (2026-08-31 release's final link); first dogfood of the new hint caught it.

### Key Decisions

- **Three commits, not one**: feature (code+tests+deck doc) / scribe doc / lock sync. Lock drift is upstream-caused noise — mixing it into the feature commit would blur the audit trail the lock exists for.
- **Hook output-before-source anomaly accepted**: the pre-commit hook rebuilds and stages ALL `skills/` outputs repo-wide, so `51b02544` contains the rebuilt scribe SKILL.md output while its source landed in `ec4c0822`. Content is consistent; history is slightly inverted. Not worth a rebase rewrite of pushed-bound commits — noted here instead.

### Pitfalls

- **Pre-commit hook stages repo-wide skill rebuilds**: `git add <specific files>` then commit → hook appends every rebuilt `skills/<name>/` output to the index. If your commit touches one package's `skill/`, expect siblings' build output to ride along.
  - **Fix**: verify `git show --stat HEAD` after committing skill-source changes; if output got in without source, commit the source immediately (tree stays consistent).
  - **Time wasted**: ~2 min (caught by `git status` right after the commit, not by a later session).

### Next Steps

1. **Push** the 4 ahead commits (`git push`) — standard end-of-session recycle.
2. **Backlog triage (9 cards)**: …402 (kimi probe hardening), …204 (curator add hardcoded), …671 (stale players detect), …479 (dedupe deck-link metadata loop), …502 (curator sqlite wiki index ADR), …558 (ingest GH issue), …615 (PR template task-link validation), …828 (publish adapter support matrix), …918 (extract standalone ZK skill).
3. **EPIC-20260828111425886** still flagged drifted by probe — needs a status decision (revive / close / split).
4. Optional: `arena doctor` adapter matrix work pairs with …828.

### Temp Artifacts

- `/tmp/test-gate-20260908.log` — full test gate output backing the EXIT=0 claim; deletable.
