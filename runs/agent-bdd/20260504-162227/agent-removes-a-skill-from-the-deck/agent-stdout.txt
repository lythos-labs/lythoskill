Done. Here's what was executed:

1. **Created cold-pool skills** at `cold-pool/skill-a/SKILL.md` and `cold-pool/skill-b/SKILL.md`.
2. **Ran `./deck link`** — linked both skills to `.claude/skills/`.
3. **Ran `./deck remove skill-a`** — removed `skill-a` from `skill-deck.toml` and deleted its symlink.
4. **Ran `./deck link`** again — working set now contains only `skill-b`.
5. **Wrote checkpoint** to `_checkpoints/remove.jsonl` with the exact shape requested.

Final state: `.claude/skills/` has only `skill-b` (symlinked); `skill-deck.toml` declares only `skill-b`; cold pool still holds both `skill-a` and `skill-b`.
