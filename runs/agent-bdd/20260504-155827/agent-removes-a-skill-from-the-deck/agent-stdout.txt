All 5 steps completed successfully:

1. **Created cold-pool skills** — `cold-pool/skill-a/SKILL.md` and `cold-pool/skill-b/SKILL.md` with minimal frontmatter.
2. **First `./deck link`** — Working set established with 2 symlinks (`.claude/skills/skill-a` and `skill-b`) and `skill-deck.lock` written.
3. **`./deck remove skill-a`** — Removed `skill-a` from `skill-deck.toml` and deleted its working-set symlink.
4. **Second `./deck link`** — Synced working set; only `skill-b` remains linked.
5. **Checkpoint written** — `_checkpoints/remove.jsonl` contains the exact JSONL shape you specified.

Final state: `skill-deck.toml` declares only `skill-b`, and `.claude/skills/` contains only the `skill-b` symlink.
