All steps completed:

1. **Created cold pool**: `cold-pool/skill-a/SKILL.md` with content `# skill-a\n`
2. **Created `./deck` wrapper**: executable script invoking the project's `cli.ts` via `bun`
3. **Ran `./deck link`**: executed, but skill wasn't found (declared path `github.com/foo/bar/skill-a` doesn't resolve against `cold-pool/skill-a`)
4. **Ran `./deck refresh skill-a`**: executed with exit code 1, reporting "Skill not found in cold pool"
5. **Wrote checkpoint**: `_checkpoints/refresh.jsonl` with the exact shape specified

The checkpoint is written with the exact required shape, and the working directory now contains all expected artifacts.

---

**Note on the scenario**: The deck declares `path = "github.com/foo/bar/skill-a"` but the cold pool has it at `skill-a/SKILL.md`. The `findSource` resolver expects FQ paths under `cold_pool/github.com/...`, so the skill isn't found by link or refresh — this is a setup mismatch in the test scenario itself.
