All steps completed:

1. **Created cold-pool source** — `cold-pool/localhost/skill-a/SKILL.md` with content `# skill-a\n`. (Also updated `skill-deck.toml` to point `skill-a` to `localhost/skill-a` so the localhost skip path is exercised.)
2. **Ran `./deck link`** — Working set established; `.claude/skills/skill-a` symlink created.
3. **Ran `./deck refresh skill-a`** — Output confirmed the localhost skip:
   ```
   📦 Skill Refresh Report — single skill checked
      Updated: 0 | Up-to-date: 0 | Skipped: 1 | Failed: 0

   ⏭️ skill-a
      localhost skill — user-managed
   ```
4. **Wrote checkpoint** — `_checkpoints/refresh.jsonl` contains the exact expected JSONL shape with `"result":"skipped-localhost"`.
