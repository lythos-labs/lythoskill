All four steps completed:

1. **Created** `cold-pool/skill-b/SKILL.md` with `# skill-b\n`
2. **Added** `[tool.skills.skill-b]` section to `skill-deck.toml` with `path = "localhost/skill-b"`
3. **Ran** `./deck link` — it executed to completion (skills not found in this test env, as expected; lock file created)
4. **Wrote** `_checkpoints/add.jsonl` with the exact JSON shape requested
