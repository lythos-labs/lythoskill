{
  "name": "lythoskill-workspace",
  "version": "0.9.40",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky",
    "test": "bun --filter='*' run test",
    "test:bdd": "bun packages/lythoskill-project-cortex/test/runner.ts && bun packages/lythoskill-deck/test/runner.ts",
    "test:all": "bun run test && bun run test:bdd"
  }
}
